# Platinum Rose — Task Board (NFL)
> **Last updated:** 2026-08-29 Cowork (Claude; local HEAD `eb1e312`, `origin/main` `eb1e312`, uncommitted — fixed critical decimal-vs-American odds bug in `agents/metabet-futures-ingest.js`'s multiway path (see B-metabet-decimal below), purged 16,893 corrupted rows and re-ingested 16,927 corrected rows to Supabase `futures_odds_snapshots`; compiled BookMaker + BetOnline futures captures alongside BetUS, ran a 3-way comparison against the corrected Metabet feed — offshore books track in sync with the regulated-market consensus, no red flags. Andy continuing BEO/BKR/BetUS manual captures in parallel for a few weeks as a cross-check before any deprecation decision.)
> **Owner:** PM agent is the sole writer of this file.

---

## 🏗️ IN PROGRESS

| ID | Task | Assignee | Notes |
|----|------|----------|-------|
| DATA-LAYER-LOCKDOWN | Canonical data layer merge + freshness lock-down (P0, blocks all other NFL_Dashboard work) | claude | **2026-08-30 (Andy + Claude/Cowork):** Andy suspected the AI synthesis engine wasn't seeing all the data his team has compiled -- confirmed true, and the mechanism is specific: two large intelligence corpora exist (the Supabase-native pipeline that `agents/portfolio-dossier.js` actually reads, and Antigravity's `docs/antigravity/CANONICAL_EXTRACTION_PIPELINE.md` corpus -- 79 exhaustive master reports, 209 structured recommendations) that have never been connected; the real committee has zero references to the Antigravity corpus, which only reaches the Alpha demo packet. Full audit in `docs/specs/CANONICAL_DATA_LAYER_AUDIT_2026-08-30.md`. **Confirmed the two corpora share the same raw podcast transcripts** (identical filenames in `data/podcasts/m6-diarized/` vs `m6-diarized-all/`) -- this is a re-pointing fix (upgrade Corpus A's rows in place with Corpus B's richer extraction, matched by shared identity), not a conflict-resolution policy between competing opinions. Also confirmed still-live: the podcast-narrative bridge (`docs/podcast-narratives/index.json`) is 5+ weeks stale (last built 2026-07-23) and not covered by the existing 9-lane freshness gate; Twitter/X bookmarks and the hand-authored team reference library have zero bridge into the dossier at all; two latent-risk items from the 2026-07-21 audit (three incompatible `game_id` formats, two possibly-diverging player-season-stats tables) were never resolved. **Andy's decisions (doc §6):** freshness gate warns loudly rather than hard-blocking a build; a scheduled task owns re-running the merge/freshness-check going forward; the merged layer is a manually-triggered snapshot (not continuously auto-refreshing) between runs; and **this work fully blocks everything else on NFL_Dashboard** -- including ALPHA-P3 above, which was approved earlier this same session and is now explicitly held, and the not-yet-started weekly-market/Dashboard-AI-portfolio-UI work -- until the data layer is merged, freshness-verified, and re-verified against a real dossier build. Sequence: (1) expand the freshness gate to cover the missing lanes, (2) re-point Corpus B into Corpus A's tables, (3) bridge Twitter/X and the reference library, (4) resolve the game_id/player-stats latent risks, (5) full re-verification. Nothing in this row has code written against it yet -- audit and sign-off only. **Layer 2 (Verification & Reconciliation) built and now LIVE, 2026-08-30:** `scripts/verify-intel-sources.js` (flow-through mode per doc's four-layer architecture, §7) scores every `research_pick_signals`/`podcast_host_summaries` row for relevance, freshness, corroboration (grouped by team+market within a 21d recency window, not team alone) and conflict. `migration 049_intel_verification.sql` applied by Andy; first `--write` run persisted 713/713 rows to `public.intel_verification` (verified 174, unverified 206, conflicting 57, stale 84, rejected 192). Two real bugs were caught and fixed before/during this rollout, not after: (1) `checkRelevance()` used to default to "relevant" when no keyword matched either list, letting college football/golf/World Cup/tennis noise from the mixed `research_pick_signals` feed sit in `unverified` -- flipped to require positive NFL evidence, with `podcast_host_summaries` exempted (it's sourced from named NFL podcasts only, not a mixed feed, so its terse player+market fragments were false-rejecting under the same filter). (2) the first `--write` failed (`ON CONFLICT DO UPDATE command cannot affect row a second time`) because every future-mention inside one podcast episode's `futures` array reused the parent episode's row id as `source_id` -- fixed by suffixing with the mention's array index. `scripts/triage-unverified-intel.js` splits the flattened `unverified` status into `single_source` (104, ranked by confidence+recency as the actual weekly review queue), `no_team_extracted` (95, genuinely team-agnostic NFL content, not a bug), and `stale_uncorroborated` (5, too small to matter). Commits: `810738a`, `1b5a609`, `1b995e6`, `c7bec28`, `5dec725`, `028ff9e`. Still open: re-pointing Corpus B into Corpus A, bridging Twitter/X + reference library, resolving the game_id/player-stats latent risks, and extending the freshness gate to the missing lanes -- Layer 2's first real pass, not the full lock-down. | **Re-pointing progress (2026-08-31, Andy + Claude/Cowork):** Podcast side of item (1) complete -- shipped a guest-attribution fix (title-derived one-off guest extraction, vocative-address inference, gated roster fallback for 2-host shows, new Fezzik ASR aliases) in `agents/lib/speaker-attribution.js` + `agents/podcast-host-summary.js` + `src/lib/experts.js` (commit `37266e6`), live-verified across 4 target + 13 bonus episodes; ran the 2 clean gap-fill repoints (`scripts/repoint-corpus-b-podcasts.js --write`, 5 rows) and resolved both flagged UPGRADE CANDIDATEs (f25b58b4: stale-row cleanup; c21746b2: 7 non-overlapping futures added under a distinct model tag). Article side Format A complete -- surveyed all 40 Corpus B web-article master reports against `research_intel_notes` by canonicalized-URL hash, found 3 format families (15 structured "Candidate Extraction" files, 22 narrative "Team-by-Team" files, 3 corrupted VSiN files that are pure LLM refusals saved as content). Built `scripts/repoint-corpus-b-articles.js` (new) for the structured family -- content-key dedup against `research_pick_signals` (no natural upsert key), reuses `research-intel-ingest.js`'s own `classifyBetType()` rather than trusting Corpus B's own market-type labels. Andy reviewed all 8 resulting picks by hand before authorizing; live-wrote and DB-verified all 8 (2 of them -- a moneyline pick and a player prop -- the live regex pipeline structurally cannot catch at all). **Explicitly deferred to a fresh session:** Format B (the 22 narrative files, needs a more conservative labeled-fields-only extractor), the 10 unmatched Action Network files (net-new `research_intel_notes` insert -- treating Corpus B as a primary source, a bigger decision Andy hasn't made), and the 3 corrupted VSiN files (likely needs an Antigravity re-run, not a parser fix). Items (2)-(4) of the lock-down sequence remain fully open. Full detail: ATLAS `.atlas/session_log/2026-08-31_session.md`. **Format B complete (2026-08-31, same-session continuation, Andy + Claude/Cowork):** built `scripts/repoint-corpus-b-articles-format-b.js` (new) for the 22 narrative files -- hardcoded to the exact 22-file list (never a glob, so it can't sweep in the 3 corrupted VSiN refusal files or the unrelated Reddit AMA transcript), extracts ONLY from a labeled-fields allowlist (`Betting Pick`/`Best Favorite Bet`/`Best Sleeper Bet`/`Best Bet`/`Pick`/`Lock`/`Lean`) inside the `Betting & Fantasy Rationale` section only, explicitly excluding sibling bolded labels in the same section that read like picks but aren't (`Betting Trends`, `Betting Strategy`, `Justification`) -- confirmed necessary against the Sharp Football file, which has all of the above side by side. Confirmed live that PFF/Sharp Football/VSiN are all active ingested sources (282/173/159 `research_intel_notes` rows) so, unlike Format A's 10 unmatched Action Network files, Format B raised no net-new-insert decision -- every file with real picks matched an existing note. Dry run: 20 of 22 files correctly yielded 0 picks (narrative-only, as expected), 1 had no rationale section at all, 3 VSiN picks were already present (the live regex pipeline already caught those plain spread/total shapes), and exactly 2 genuinely new picks surfaced -- both futures leans from the Sharp Football Super Bowl 61 tracker (Rams as best favorite at +550, Vikings as best sleeper at +5000), correctly classified via title-assisted `classifyBetType()` since the pick text alone carries no futures keyword. Andy authorized the write; both rows live-verified in `research_pick_signals` (ids 528-529). **Item (1) of the re-pointing sequence (podcast + article Format A + Format B) is now fully done.** Still open, unchanged from above: the 10 unmatched Action Network files (net-new-insert policy decision, Andy hasn't made it) and the 3 corrupted VSiN files (likely an Antigravity re-run, not a parser fix) -- both explicitly deferred, not attempted this pass. Items (2)-(4) of the lock-down sequence (bridge Twitter/X + reference library, resolve game_id/player-stats latent risks, extend the freshness gate) remain fully open. **Freshness gate (item 2) done and VSiN closed out, see later entries below.** **Twitter/X + reference-library bridge (item 3) done 2026-09-01 (Andy + Claude/Cowork), after coordinating with Antigravity on the shared file:** confirmed with Antigravity that its only in-flight change to `agents/portfolio-synthesize.js` (a `temperature`-param removal) was safe to build alongside. Investigated the Twitter/X bridge and found it's a dead end -- `x_sharp_tweets` has had zero fresh rows since 2026-06-07 (RSSHub self-hosting on M6 was already tried and killed by X requiring paid API credentials, `docs/NFL_BACKLOG.md`; the documented manual-drop fallback was never actually used, 0 `sharp-intel-*.md` files exist). That investigation surfaced a real live gap instead: Supabase `vault_notes` holds 963 rows -- hand-curated reference guides and 225 per-team files -- that neither `portfolio-dossier.js` nor `portfolio-synthesize.js` ever reads. Added `loadVaultReferenceEvidence()` bridging exactly two non-duplicate pieces: 6 hand-curated reference docs (coaching tendencies, DVOA/EPA glossary, key numbers, ATS framework) as new prompt context, and per-team "Analytical Deep-Reads" narrative summaries (sourced from `research_intel_notes`, which the dossier never otherwise surfaces -- only the structured `research_pick_signals` rows). Deliberately excluded the same note's Injuries subsection (duplicate of `dossier.injuries`/`player_injuries`), raw stat-import file variants, `WeeklySignals.md` (same source, re-aggregated), the fantasy/IDP guide, and stale 2022-2024 season archives. Both queries are best-effort, silently skipped without Supabase creds. Verified via `--prompt-only`: +32,248 tokens (114,812 -> 147,060), well under context limits; confirmed graceful no-op without creds; full existing test suite (31 tests) still passes; eslint clean. Commit `ce72eef`. **Twitter/X ingestion revisited 2026-09-01 (Andy + Claude/Cowork), per Antigravity's proposed $0 cookie-based GraphQL revival:** investigation found the described architecture already exists as `agents/twitter-bookmarks-agent.js` (predates this session), fetching Andy's own X bookmarks via `PERSONAL_TWITTER_AUTH_TOKEN`/`PERSONAL_TWITTER_CT0`. Confirmed live the cookies still work (`--dry-run` pulled 61 real bookmarks) but the agent only ever wrote to `vault_notes`, never `research_intel_notes`/`research_pick_signals` as Antigravity's message described -- so nothing from it reached the committee, matching the same gap as the reference-library bridge above. Also found and fixed an unrelated real bug along the way: `tests/unit/twitterBookmarksAgent.test.js` had no DRY_RUN guard before its static import, so every test run inserted a real row into production `vault_notes` (6 stray `TestSharp` fixture rows found and confirmed gone after the fix, commit `bb03cd5`). Then closed the missing-persistence gap (commit `a37f7bd`): every qualifying bookmark now gets one `research_intel_notes` row (deduped by `url_hash`, a live existence check rather than relying only on the agent's local-file dedup), and `research_pick_signals` rows are created ONLY from Gemini Vision's OCR'd player-prop graphics (already-structured fields) via a new `buildPropSignalRows()` pure function -- freeform tweet text is deliberately not parsed for a pick, matching the labeled-fields-only discipline used throughout this lockdown. 4 new unit tests added, all 7 pass, eslint clean; live-verified insert + dedup-on-rerun, then cleaned up the one test row created. **Flagged, not resolved:** real bookmark activity stopped after 2026-08-22 despite a supposed 2-hour systemd timer -- timer status on its target machine can't be confirmed from this bridge session, needs Andy to check directly. Also flagged plainly: this method replays personal session cookies against X's internal GraphQL endpoints, which is free and only touches Andy's own bookmarks but falls outside X's ToS for automated access and carries some account-risk a paid API wouldn't -- Andy's call, not resolved here. **Antigravity's full pipeline report reviewed 2026-09-01:** confirmed real -- session cookies verified live, author extraction fixed for X's `core.user_results` schema migration (real handles now resolve, e.g. "Connor Allen", "Deepak Chona, MD. SMA"), dedicated "Platinum Rose" ops account confirmed (only follows/bookmarks sports betting content, meaningfully reducing -- not eliminating -- the account-risk flagged above since a suspension there costs a scraper account, not Andy's personal profile), 10 real bookmarks live-ingested (`research_intel_notes` ids 2579-2588), 9 stray `TestSharp` test-pollution rows purged. **But found two real problems in the same commit** (Antigravity's concurrent edit to the shared file landed inside this session's own commit `a37f7bd` without separate review -- a process miss worth naming plainly): (1) their edit ANDed `&& DRY_RUN` into the local-file dedup check, which the test suite couldn't catch (it always runs under DRY_RUN=true) but meant a real unattended run could never dedupe -- every timer firing would re-fetch/re-OCR/re-write every bookmark in the recency window from scratch, forever; (2) 3 of the 10 ingested rows were pure fantasy-draft-strategy content (pick-slot value, ADP-vs-projection framing) with no betting angle, sitting in the committee's evidence pool. Andy's call: player breakout/usage/injury fantasy content IS betting-relevant and should keep bridging; draft mechanics specifically should not. Fixed both (commit `abb6c3b`): dedup pulled into its own `shouldSkipAsAlreadyProcessed()` predicate (4 tests, independent of DRY_RUN state); added `isFantasyDraftMechanics()` to `agents/lib/sportsRelevanceFilter.js`, deliberately phrase/pick-slot-notation-based rather than a bare "adp" keyword match, since real examples from the live run (an injury update, a camp-news post) mention "ADP"/"draft" in passing without being draft-mechanics content -- a naive match would have wrongly excluded both (6 new tests cover this). Verified live with a synthetic draft-mechanics bookmark: correctly kept in `vault_notes`, correctly skipped from `research_intel_notes`. 22/22 tests pass across the 4 affected files, eslint clean. **Still open, unchanged:** the systemd timer's actual running state on its target machine, and the underlying ToS/account-risk tradeoff -- both remain Andy's calls to make, not resolved by this pass. **Item (4) (game_id/player-stats latent-risk cleanup) is the only item remaining in the DATA-LAYER-LOCKDOWN sequence.** **Freshness-gate lane expansion (2026-08-31, same-session continuation, Andy + Claude/Cowork):** per the audit doc's own proposed sequencing (§4), started with this step before the Twitter/X + reference-library bridges. Added a `podcast_narratives` lane to `scripts/lib/dossier-freshness-gate.js`'s `EVIDENCE_LANE_FILES` for `docs/podcast-narratives/index.json` -- confirmed by grep that `agents/portfolio-synthesize.js` reads this file directly pre-synthesis, so it's real committee evidence, not an unused artifact. Confirmed the gap it was silently hiding: the index is still stale at 37 unique episodes (last built 2026-07-23) vs. **47 unique `episode_id`s live in `podcast_host_summaries` today** -- a real 10-episode gap, on top of whatever the 2026-08-31 guest-attribution fix improved for episodes already indexed. Verified live: `node scripts/check-dossier-freshness.js --dossier .nfl/portfolio/dossier-2026-08-29.json` went from `status: pass` (false confidence -- the lane didn't exist to check) to `status: stale` (exit code 1, correctly blocking) after the change. 16/16 existing unit tests (`tests/unit/dossierFreshnessGate.test.js`) still pass, `node --check` + `eslint` clean. **Deliberately did NOT add an `antigravity_corpus` lane** despite the audit doc's §3a suggesting one "immediately" alongside podcast narratives -- grepped `agents/portfolio-dossier.js`/`portfolio-synthesize.js`/`scripts/build-futures-synthesis-context.js` and confirmed none of them read `data/podcasts/actionable_betting_recommendations_2026.json` or any `scratch/*.md` master report; only the separate Alpha demo packet does. Per item (1)'s revised finding (§3b), the committee-facing bridge the audit doc was actually asking for is already delivered by this session's repointing work (Corpus B's richer extraction now lives inside the same `podcast_host_summaries`/`research_pick_signals` rows the committee already reads) -- a separate file-based lane tracking Antigravity's own manifest would monitor staleness of something nothing in the gated pipeline consumes. **Not yet done:** actually rebuilding `docs/podcast-narratives/index.json` itself -- `scripts/build-podcast-narratives.js` reads live from the Obsidian vault at `E:\data\Obsidian\NFL\Podcasts`, outside this session's connected `E:\dev` folder, so a rebuild needs Andy to grant that folder access first; this pass only made the existing staleness visible to a dossier build rather than silently invisible. Items (2)-(3) of the lock-down sequence (bridge Twitter/X + reference library, resolve game_id/player-stats latent risks) remain fully open; item (4) (full re-verification) still pending those. **2026-09-01 (Andy + Claude/Cowork):** attempted the podcast-narratives rebuild after being granted vault access -- surfaced two real blockers: (a) `scripts/build-podcast-narratives.js` bakes bridge-session-relative absolute paths into `source_root`/`file://` URLs when run through the device_bash bridge, corrupting the artifact for use outside the bridge session; (b) even setting that aside, the vault's `_reconciled/` folder (the script's actual upstream input) is itself stale since 2026-07-23 and needs `scripts/publish-reconciled-podcast-notes.js` run first, whose write behavior against Andy's real Obsidian vault hasn't been verified. Corrupted rebuild output was safely reverted (`git status` on `docs/podcast-narratives/` clean). **Andy's decision: defer this sub-task** -- the freshness gate already flags it stale (previous entry), so nothing is silently hidden; revisit later. Also found the repo mid-edit by Antigravity (uncommitted, not touched by this session): `agents/portfolio-synthesize.js`, `HANDOFF.md`, `scripts/triage-unverified-intel.js`, `scripts/verify-intel-sources.js`, `vite.config.js`, `src/App.jsx`/`Header.jsx`/`profiles.js`, new `agents/lib/masterReportGuard.js` + `agents/master-reports-to-vault-sync.js` + `agents/vegas-web-odds-ingest.js`, and (encouragingly) the 3 corrupted VSiN master-report files already being repaired with real content, replacing the LLM-refusal placeholders -- so no spec prompt needed there after all. Since item (3)'s Twitter/X bridge would need to edit `agents/portfolio-synthesize.js`, the exact file Antigravity has in flight, **Andy's decision: pause item (3) until coordinating with Antigravity directly** (no session-to-session channel exists between this Claude session and Antigravity) rather than risk a collision. **VSiN files closed out (2026-09-01):** Antigravity reported back re-extracting all 3 corrupted VSiN master reports in place with real content (matching standard Corpus B structure) and adding `agents/lib/masterReportGuard.js` (refusal-pattern interception, size floor, structural guard, wired into `agents/master-reports-to-vault-sync.js`, 4/4 unit tests) to prevent recurrence -- no spec prompt from Claude was needed. Added all 3 filenames to `scripts/repoint-corpus-b-articles-format-b.js`'s `FORMAT_B_FILES` array; dry run confirms all 3 correctly yield 0 labeled picks (their rationale sections are "Betting Trends"/"Betting Strategy" commentary only, not labeled Pick/Lock/Lean/Best Bet fields) -- no writes needed. Commit `5f779c7`. **Item (1) of the re-pointing sequence (podcast + article Format A + Format B + the 3 VSiN files) is now fully, completely done.** Item (3)'s Twitter/X bridge remains paused pending Antigravity coordination on `agents/portfolio-synthesize.js`; the reference-library bridge sub-item and item (4) (game_id/player-stats cleanup) remain open. **Item (4) closed 2026-09-01 (fresh session, Andy + Claude/Cowork):** re-verified both latent risks live rather than re-auditing from memory. **game_id fragmentation:** confirmed `src/lib/gameId.js`'s canonical-key helper (built S317, option b, Andy-approved 2026-07-27) is still accurate -- re-checked `agents/schedule-ingest.js`'s `makeGameId()` and `packages/shared/src/week-utils.js`'s `buildGameId()` against the module's documented 3 formats, unchanged. No new work needed; option (c) full backfill remains explicitly deferred per Andy's prior call. **player-stats divergence:** live-verified against production Supabase (read-only) that `nfl_player_season_stats` (2020-2024, `nfl-data-py`, 3,016 rows) and `player_season_stats` (2022-2025, `nflverse`, 7,963 rows) really do diverge -- 946 of 1,773 overlapping `(player_id, season)` rows (53%) have mismatched values, including a data-quality bug in the former (a 2022 Tom Brady row with `games=18`, impossible in a 17-game season). But confirmed by full-codebase grep it's a dead-table risk, not a live conflict: nothing reads `nfl_player_season_stats` except its own writer (`scripts/seed-historical-stats.py`, an annual/manual backfill); `player_season_stats` is the one `agents/fantasy-value-report.js` actually consumes. Applied the same option-b-style fix as GAMEID-FORMAT -- documentation + guard, not a live-table rewrite: `supabase/migrations/050_deprecate_nfl_player_season_stats.sql` (comment on table, not yet run -- needs Andy via Supabase Dashboard -> SQL Editor), a matching warning in `seed-historical-stats.py`'s header, and a full PLAYERSTATS-DUP entry in `docs/NFL_AUDIT_BACKLOG.md` mirroring GAMEID-FORMAT's. **Open decision left for Andy** (same risk class as GAMEID-FORMAT's deferred option c): fully retire `nfl_player_season_stats` -- drop it, or repoint `seed-historical-stats.py` to write `player_season_stats` instead -- or leave it deprecated-but-present indefinitely. **DATA-LAYER-LOCKDOWN's full 5-item sequence (§4 of the audit doc) is now complete** -- items (1)-(4) all closed; item (5), full re-verification against a real dossier build, is the natural next step whenever Andy wants to resume other NFL_Dashboard work, since this row was the explicit P0 blocking everything else.
| B-actionnetwork-feed-403 | **Live feed outage:** `research-intel-ingest.js`'s Action Network RSS feed has been silently dead since ~2026-08-25 | P1 | **Found 2026-08-31** while investigating the 10 unmatched Action Network files from the Format A article survey. `research_intel_notes` has carried **zero** Action Network rows since 2026-08-25 -- every other active feed (PFF, VSiN, Sharp Football, BettingPros, Rotowire, Pro Football Talk, Walter Football) kept ingesting normally through 2026-08-31 in the same window. Root cause confirmed live: `https://www.actionnetwork.com/nfl/feed` now returns **HTTP 403** (CloudFront bot-block), tested directly. `fetchFeed()` in `agents/research-intel-ingest.js` treats a non-OK response as a soft per-feed `status: 'unavailable'` rather than throwing -- so the `research-intel-ingest.yml` GitHub Actions workflow has reported **green/success on every run** through this entire window (confirmed via 15 recent runs on the GitHub API), with the outage visible only inside that run's own receipt JSON, which nothing monitors. **Not yet fixed** -- needs one of: retry with different request headers/UA (CloudFront bot-detection may key off the current `User-Agent: NFL-Platinum-Rose-ResearchIntel/1.0`), find an alternate Action Network feed URL/path, or accept the block and route around it (e.g. treat Antigravity's own Action Network scrape as a standing supplementary source for this one feed rather than a one-time backfill). Also exposes a real gap in Layer 2/freshness-gate coverage: a feed silently going `unavailable` for 6+ days produced no alert anywhere -- worth folding into the freshness-gate lane-expansion item under DATA-LAYER-LOCKDOWN (item 2) rather than treating as a one-off. **One-time stopgap applied 2026-08-31 (Andy authorized):** `scripts/backfill-action-network-week3-gap.js` net-new-inserted the 10 Corpus B preseason-Week-3 (Aug 28) game preview files this outage caused the live feed to miss -- 10 new `research_intel_notes` rows + 9 new `research_pick_signals` rows (1 pick, Vikings/Broncos `Over 38.5 - NO 52%`, correctly flagged ambiguous and skipped, needs human read), DB-verified (`research_intel_notes` ids 2556-2565). This is explicitly a one-time backfill for a confirmed, evidenced outage -- **not** a general policy of treating Corpus B as a primary source; the remaining 3 corrupted VSiN files are a separate, unrelated issue (likely an Antigravity re-run, not this feed). **Root cause found + fixed live, 2026-09-01 (Andy + Claude/Cowork):** confirmed via side-by-side test (same URL, same network, same moment) that `curl` gets a clean `200` with real XML (25,670 bytes) while Node's native `fetch()`/undici gets a `202` with an empty `text/html` body for the identical request -- CloudFront is fingerprinting and blocking Node's HTTP client specifically, not filtering on the `User-Agent` string (changing the UA alone did not help; a full browser UA still got blocked under `fetch()`). Fixed by shelling out to the system `curl` binary (present by default on `ubuntu-latest` GitHub Actions runners) for this one feed only, via a new `fetchViaCurl()` helper and a `fetchMethod: 'curl'` flag on the Action Network feed config in `agents/research-intel-ingest.js` -- every other feed is untouched and still uses native `fetch()`. Live-verified end to end (not a dry run): ran the real ingest agent, Action Network came back `status: available`, 23 items fetched, 5 NFL-relevant, 5 candidate notes / 4 candidate signals written -- first real content from this feed since the 2026-08-25 outage began. `node --check` and `eslint` both clean. **B-actionnetwork-feed-403 resolved.** Note: this also confirms the earlier Layer-2/freshness-gate gap flagged below (a feed silently going `unavailable` produces no alert) is still real and separate from this fix -- not addressed here, still worth folding into a freshness-gate lane per the original note. |
| ALPHA-P3 | Alpha Testing Suite Phase 3 (SuperContest tester card + net-new Survivor) -- **Survivor sub-track UNBLOCKED 2026-08-31 by Andy's explicit direction: Antigravity pivots to Survivor contest upgrades now, in parallel with Claude's continued DATA-LAYER-LOCKDOWN work (supersedes the 2026-08-30 full-hold for this sub-track only; SuperContest tester-card sub-track status unchanged, still implicitly held pending the same review)** | antigravity | **Cataloguing gap fix (2026-08-30):** a full repo-grounded implementation handoff already existed (`docs/specs/ALPHA_PHASE3_SUPERCONTEST_SURVIVOR_HANDOFF.md`, committed `a3435de` 2026-08-28, already on main) but was never reflected on this board and ATLAS's HANDOFF.md incorrectly carried it as "not yet spec'd." The handoff was DRAFT, blocked on 3 open decisions. **Andy approved all three 2026-08-30 (via Claude/Cowork), doc updated to APPROVED, see its new §9:** (1) `SuperContestView.jsx` stays the untouched admin line-entry tool; new `SuperContestAlphaCard.jsx` is the separate tester-facing five-pick card, no writes to `nfl_contest_lines`. (2) Email-submission wiring (spec §9/§12.9) deferred to Phase 4; Phase 3 ships local-persistence-only card UIs. (3) `supercontest_demo_lines.lines[]` for the Week 3 demo populated via admin manual entry through the existing `SuperContestView.jsx` sync-live-odds flow, not a script. **Confirmed nothing built yet** — no `SuperContestAlphaCard.jsx`, `src/components/survivor/`, `alphaDeadlines.js`, or `supercontestAlpha.js`/`survivorAlpha.js` exist; packet's `supercontest_demo_lines`/`survivor_demo_slate` both still `not_built_phase_3_pending` with 0 entries. Codex/Antigravity may now begin implementation per the handoff doc's §3 (3a SuperContest) and §4 (3b Survivor). |
| NFL-ATLAS-3 | Podcast host-summary pipeline (replaces Fable re-eval) | andy | **Cataloguing gap fix (2026-07-27): tracked only in ATLAS's `.atlas/domain/atlas/tasks.json` (id `nfl-podcast-host-summary-pipeline-2026-07`, requested 2026-07-20), never reflected in this board despite three full sessions of real work (S290-S292) — exactly the kind of thing that gets lost working in this repo alone. Surfacing here so it's visible going forward; ATLAS's `tasks.json` is the source of record.** Substantial real build, not a stub: real AssemblyAI-diarization-based speaker attribution (`agents/lib/speaker-attribution.js`), per-host futures-only extraction (`agents/podcast-host-summary.js`), migrations 035-037 applied live, 3 real bugs found and fixed by actually reading output (a silent-0-rows upsert bug, an NFL_ONLY_GUARD prompt-leakage bug, ~20 missed golf/other-sport title exclusions), full backfill completed live on M6 (75 episodes diarized+summarized, 0 errors), Obsidian vault notes written (40/40) via a new `--vault-sync` mode with auto-launch-Obsidian fallback. Full history in `docs/PODCAST_HOST_SUMMARY_PIPELINE_PLAN.md`. **Two items still genuinely open:** (1) whether/how to wire this into the weekly ingest cron — Andy's call, not yet made; (2) the deferred Fable-5 comparison pass on the new pipeline — explicitly non-blocking, do whenever. |

---

## 📋 BACKLOG

### Features (open)

| ID | Task | Priority | Notes |
|----|------|----------|-------|
| F-26 | Fantasy Football features — spec + build | P2 | This description was stale — corrected 2026-07-26. A full Phase A "Fantasy Value vs ADP" pipeline already existed (`docs/FANTASY_VALUE_VS_ADP_SPEC.md`, `agents/fantasy-value-report.js`) but had never actually produced a real projection — root-caused 2026-07-26: `player_season_stats` was never seeded (fixed via `agents/player-stats-ingest.js --seasonal-only`, 7963 rows), and once seeded, `fetchSeasonStats()`'s unfiltered query was silently truncated by Supabase's default 1000-row response cap before it ever reached QB/RB/WR/TE rows (alphabetically after the positions that filled the cap) — fixed by adding `.in('position', POSITIONS)` server-side. Yahoo OAuth2 client (`src/lib/yahoo.js` etc.) handshake is complete (tokens saved), but Yahoo now gates real API calls behind a separate approval application (`sports.yahoo.com/developer/access/`) submitted 2026-07-26 — **waiting on Yahoo's review**. Kickers explicitly out of scope per Andy (not meaningfully drafted with strategy). IDP/team-DEF scoring filed separately as F-26b. Fantasy tab shipped 2026-07-26 (`?tab=fantasy`, commit `9b4f584`) — renders the board in-app with position/tier filters and search. Still open: roster/waiver intel + weekly-projections spec (the literal original ask), Phase B (props-derived projections, blocked on the same real-prop-odds source as PROPS-1). |
| F-26c | FantasyPros API integration (ADP + weekly rankings + Phase B projections + injuries) | P2 | **Part 1 (ADP) BUILT & VERIFIED LIVE 2026-08-09** — `agents/fantasypros-adp-ingest.js` + `agents/lib/fantasypros-{client,adp}.js`, unit-tested (8/8), real run loaded 439 rows into `fantasy_adp`, confirmed flowing through `npm run report:fantasy` to a real value board (251 value/72 reach/87 no-proj). Along the way, found+fixed a null-handling bug (regression-tested) and an unrelated pre-existing Windows path-casing bug that silently broke `report:fantasy` and 10 other scripts (`GUARD-FIX`, DONE section). **Part 2 (weekly/draft rankings) backend BUILT 2026-08-09** — migration `046_fantasy_rankings.sql` (applied), `agents/lib/fantasypros-rankings.js` (unit-tested), `agents/fantasypros-rankings-ingest.js` (loops QB/RB/WR/TE with a 1.1s rate-limit delay per position, respects the confirmed 1 req/sec plan limit). `week` deliberately defaults to `0` not `null` for season-long rows (Postgres NULL-uniqueness pitfall — see doc §2). Dry-run verified live (661 real rows, healthy expert counts). **Real upsert failed on first attempt** — `ON CONFLICT DO UPDATE command cannot affect row a second time`, because FantasyPros' response contained 2 entries for the same player within one position call (a real API data artifact, not our bug). Fixed with a `dedupeRankings()` step (keeps the better rank_ecr, logs every collision) wired into every write path, 4 new regression tests. **Re-run confirmed the fix 2026-08-09** — caught the predicted 1 collision (Isaiah Williams, WR), logged it, upserted all 660 deduped rows cleanly. Backend done to the same standard as part 1. **Part 2 UI + Parts 3-4 all BUILT 2026-08-10** (Cowork): `FantasyRankingsPanel.jsx` ships as a toggle inside the existing Fantasy tab, reading `fantasy_rankings` directly via Supabase public-read RLS (no generated-JSON step needed, unlike the Value Board). **Part 3 (Phase B projections)**: migration `047_fantasy_projections.sql`, `agents/lib/fantasypros-projections.js` (unit-tested), `agents/fantasypros-projections-ingest.js`, and a `--source fantasypros` flag on `fantasy-value-report.js` that writes to its own `-fantasypros`-suffixed output files — alongside Phase A, never replacing it (resolves scoping doc §6 Q4). **Part 4 (injuries)**: `fetchFantasyProsInjuries()` added to `scripts/build-player-availability.js` alongside the existing ESPN fetch, both flowing into one `buildAvailabilitySnapshot()` call, no cross-source dedupe (kept as independent corroboration per Q7); `probability_of_playing`/`practice_1-3` carried through as new optional fields on the availability event shape. **Caveat carried forward explicitly, not glossed over: Parts 3-4 are code-complete and unit-tested (plain-node harness, vitest hangs in this sandbox) but NOT live-verified** — confirmed live 2026-08-10 that this Cowork sandbox cannot make any outbound network call at all (same root cause as F-31, re-confirmed independently of that entry). Part 3's field mapping reuses §0's already-live-confirmed fields (`points`/`points_ppr`/`points_half`/`rush_*`/`rec_*`/`fpid`/`name`); Part 4's exact `/nfl/injuries` field names are NOT live-confirmed and were built with defensive multi-name fallbacks instead of a guess — see `agents/lib/fantasypros-injuries.js`'s header. Also found+fixed in passing: `build-player-availability.js` never loaded `dotenv`, so `FANTASYPROS_API_KEY` silently never reached it even with a real key in `.env` (harmless before this session since ESPN's feed needs no key). **Part 4's field mapping LIVE-CONFIRMED 2026-08-10** — Andy ran it natively (`availability:fantasypros:dry`: 187 events, 120 real rows, 0 errors) then a raw-vs-mapped diagnostic dump; every guessed field name matched the real API response except `reported_at`'s source field being a naive/timezone-less datetime string (flagged in code, low-impact — not a join/dedupe key). **Part 3 hit a REAL BUG on its first live dry-run, found and fixed same day**: `npm run ingest-fantasypros-projections:dry` mapped 0/84 rows with no error — a raw-vs-mapped dump showed every stat/points field is nested under a `stats` sub-object (`player.stats.points`), not flat like §0-§2's confirmed endpoints; `fpid`/`name`/`position_id`/`team_id` were flat and correct. Also fixed `pass_ints`/`fumbles` field-name guesses. Fixed in `agents/lib/fantasypros-projections.js`, regression-tested against the real captured payload. **All four parts now fully live-verified end-to-end, mapping AND persistence.** Part 3's real write initially failed — migration 047 had been written but never applied in Supabase (unlike 046). Andy applied it, re-ran: 531 rows upserted cleanly (84 QB/132 RB/190 WR/125 TE), 451/531 resolved a `player_id`, zero dedup collisions. Part 4's real (non-dry-run) run: 189 events (122 FantasyPros + 67 training camp) written to `data/player-availability/latest.json`. **FantasyPros integration (F-26c) is complete.** Andy has a FantasyPros API key, intended as primary fantasy research source. Scoped 2026-08-09 (Cowork), not built: `docs/FANTASYPROS_API_INTEGRATION_SCOPE_2026-08-09.md`. **API access verified live same day** — key works, free-tier public API, real endpoints confirmed via test calls: `/nfl/players` (real ADP, `rank_adp`/`rank_adp_ppr`, distinct from ECR), `/nfl/{season}/consensus-rankings` (weekly + draft ECR), `/nfl/{season}/projections` (pre-computed `points`/`points_ppr`/`points_half` per player, no formula math needed), `/nfl/injuries` (structured status + numeric `probability_of_playing` + Wed/Thu/Fri practice reports). Four parts: (1) ADP — closes the open "manual CSV or scrape/API" question in `FANTASY_VALUE_VS_ADP_SPEC.md` §3, zero schema work (`fantasy_adp.source` already accepts non-'manual' values), source confirmed as `/nfl/players` not `/consensus-rankings`; (2) weekly rankings/start-sit — the literal original F-26 ask, needs a new `fantasy_rankings` table + new panel, free-tier expert-count-per-week worth re-checking closer to Week 1 (saw 5 experts vs. 90 for draft view); (3) Phase B market-derived value-board projections — independent alternate path to the props-odds-blocked Phase B, needs a new `fantasy_projections` table + a `--source` flag on `fantasy-value-report.js`; (4) player availability/injuries — **not a new pipeline, extends the existing one**: `scripts/build-player-availability.js`'s `buildAvailabilitySnapshot()` already accepts a generic multi-source `injuryRecords` array (currently ESPN + training-camp RSS), FantasyPros injury records map onto the same shape and merge in as a third source, no new Supabase table, adds `probability_of_playing`/practice-report fields ESPN's feed lacks; open question on whether to dedupe against ESPN's overlapping reports or keep both as corroboration. Recommended build order: ADP → weekly rankings → Phase B → injuries (smallest/most-blocking-question-closing first, injuries last since it's additive to something already working, not blocking anything). Bonus (not scoped): `/nfl/news` endpoint also confirmed available, could pair with the existing Podcast Fantasy Intel panel later. Remaining open questions in scoping doc §6 before build. |
| F-26b | IDP / team-DEF configurable scoring | P3 | The Fantasy tab (`?tab=fantasy`, commit `9b4f584`) covers QB/RB/WR/TE only. Kicker, individual-defensive-player (LB/DB/DL), and team-defense stat columns all exist in the raw nflverse CSVs (`def_tackles_solo`, `def_sacks`, `def_interceptions`, `fg_made`, etc.) but aren't ingested into `player_season_stats`, and nflverse's own `fantasy_points`/`fantasy_points_ppr` are `0.0` for both kickers and defensive players — there's no universal "standard" IDP/DEF scoring the way PPR is standard for offense, so a hardcoded default would misrepresent most leagues. Andy's call (2026-07-26): build this as a per-league configurable system once Yahoo access is approved, sourcing exact stat-to-point weights from `agents/yahoo-league-settings.js` (extend it to also capture IDP/DEF mappings) rather than guessing at defaults now. Blocked on F-26's Yahoo approval. |
| F-14 | Vault pre-load (reference data) | P2 | `agents/vault-seed.js` + drop dirs built (S158). Coaching tendencies + DVOA seeded. Andy collecting PFF/ATS/splits source files — drop in `data/vault-seed/{pff,ats,splits}/` when ready. |
| NFL-ATLAS-1 | NFL Futures watchlist with expert agree/disagree citations | P2 | **Cataloguing gap fix (2026-07-27): this item lives in ATLAS's own cross-project task registry (`.atlas/domain/atlas/tasks.json`, id `nfl-futures-watchlist-2026-07`, requested 2026-07-20 during ATLAS's S286 ADR-0010 close-out session) and was never reflected here — surfacing it now so it isn't lost working in this repo alone.** Andy wants to "pin" specific futures (SB/conference/division/wins/MVP/etc.) with links to every tracked expert who agrees or disagrees, citing their stats/historical data. Andy's decision (2026-07-20): build both an Obsidian side (one note per pinned future) and a section in this dashboard alongside the futures-intel-report/Value Spots UI. Likely mostly joinable against existing signal data — `research-intel-ingest.js` already parses per-expert author/lean/rationale, `futures-intel-report-v2.js` already renders expert blockquotes — check whether "pin" can be a flag/table over that before building new ingestion. ATLAS's `tasks.json` is the source of record; update both places if this moves. |
| NFL-ATLAS-2 | NFL daily digest redesign — readability + actionability | P2 | **Same cataloguing gap (2026-07-27), id `nfl-daily-digest-redesign-2026-07`, requested 2026-07-20.** Andy wants the daily digest redesigned — current format flagged as hard to parse quickly and hard to act on. **Blocked on Andy** — open questions before starting: what specifically feels hard to read/act on (too much text? no prioritization? wrong delivery channel — still email?), and whether Andy has an example of a digest format he likes. Ask before redesigning. |
| NFL-BACKLOG | Vite dev server returned 503 then became fully unreachable mid-session | P3 | Found 2026-07-27 during F-32's live smoke test (Claude in Chrome, `?tab=agent`). Browser fetch for `public/youtube-futures-agent-intel-summary.json` (file genuinely exists, 39 items) got HTTP 503 from `localhost:5173`; within ~1 minute *every* fetch to the dev server started failing (`TypeError: Failed to fetch`), including the root page itself — the tab stayed alive (cached React state still rendered) but nothing new could load. No front-end file was touched this session, so this wasn't caused by any edit made today — likely the `npm run dev` process itself crashed or hung on Andy's machine. Not investigated further (requires access to the actual terminal running `npm run dev`, which the sandbox doesn't have). Andy's call — check that terminal for a stack trace/OOM/crash next time it's running, or just restart `npm run dev` if it's still down. |
| NFL-BACKLOG | `GAMEID-FORMAT` option (c) — full standardize + backfill | P3 | Deferred. Andy picked option (b) (see DONE section) for now — a shared canonical-key helper, no existing table/cron touched. Option (c), picking one canonical format and rewriting+backfilling `schedule-ingest.js`/`game-odds-ingest.js`/`betting-splits-ingest.js` and all 4 live tables, remains open if ever wanted; real risk to live cron jobs, needs dedicated native-testing time before touching prod. |
| F-36 | Cross-link AI Intel & Picks-Inbox tabs | P3 | Andy's observation (2026-08-29): "AI Intel & Command" (`UnifiedIntelHub.jsx` — AI chat, podcast digests, twitter bookmarks, training camp intel) and "Picks & Inbox" (`official-picks` tab: `OfficialPicksTab.jsx` approval workflow + `PicksTracker.jsx` tracker) feel similar in theme/goals despite being separate top-level tabs. Confirmed they're functionally distinct — Intel is the consumption/research layer (read raw source material, no tracking), Picks & Inbox is the decision/governance layer (Platinum Rose AI candidate approval + general pick tracking) — but there's currently zero cross-linking: nothing in the Inbox links back to the source podcast/tweet/article that generated a candidate, and nothing in Intel surfaces "this already became a pending pick." Andy's ask: don't necessarily merge the tabs, but scope out tighter integration/linking between them. Not started — needs a scoping pass on what the actual link points should be (candidate → source article/tweet/podcast segment? source item → any pick it spawned?) before building anything. |
| NFL-BACKLOG | Futures odds data stale (7-20 days) — blocked first live synthesis run | P2 | Found 2026-08-29 during first live Platinum Rose AI portfolio-synthesize.js test run (single-pass, `claude-opus-4-8`): 17 candidates generated, validator invalidated all 17 against dossier ground truth — 14 of 17 for stale best-price quotes (171.88h / 471.88h old, i.e. ~7-20 days), 2 for genuinely non-positive EV (validator correctly caught a bad model recommendation), 1 for a book-name mismatch (model cited generic "bookmaker" instead of the dossier's actual placeable book). This is the validator working as designed, not a bug. **Corrected 2026-08-29 (superseded the original root-cause framing below, which speculated the GHA cron might not be running — confirmed that's not it):** two separate causes, both now confirmed live. (1) `agents/futures-odds-ingest.js` (TheOddsAPI → `futures_odds_snapshots`) can structurally only ever populate the `superbowl` market — verified via `GET /v4/sports?all=true` that TheOddsAPI does not offer separate sport_keys for conference/division/awards/wins/playoffs/matchup at all; 14 of the 15 configured markets 404 on every run back to 2026-05-17 (`.nfl/receipts/futures-ingest-*.json`), not a regression, not fixable by correcting sport_key names. (2) The markets TheOddsAPI can't cover were instead being fed by Andy's own manual BetOnline/BetUS/Bookmaker screenshot captures in `data/futures-imports/` via `scripts/ingest-beo-screenshots.js`, which upserts into the same `futures_odds_snapshots` table — but those captures stopped (BetOnline last Aug 22, BetUS/Bookmaker last Aug 10), which is exactly what the validator's 171.88h/471.88h stale-quote ages measure. See the new NFL-BACKLOG row below for the manual-capture gap and why it stopped. Separately: in this same run, `claude-fable-5`'s Stage 1 call hit the 16,000-token `--max-output-tokens` cap mid-array and produced unparseable truncated JSON, wasting that call's cost (~$1.90-2) with zero usable output — worth raising `--max-output-tokens` on future runs since Fable 5 appears to run longer than Opus 4.8 for the same prompt (Opus finished in 12,581 tokens, under the cap). |
| NFL-BACKLOG | Manual BEO/BKR futures capture stopped Aug 22/Aug 10 — no working automated replacement exists | P2 | Andy stopped manually capturing BetOnline (BEO) and Bookmaker (BKR) futures screenshots believing a public/scrapable source had been found to replace them. Investigated 2026-08-29: the only candidate is `agents/vegas-web-odds-ingest.js` (targets VSiN's Nevada futures feed for Circa Sports & Station Casinos — not BetOnline/Bookmaker). Confirmed it is **not a working replacement**: (1) uncommitted/untracked — `git log` shows zero history, never merged to main, not listed as done anywhere on this board; (2) never successfully run — its one existing receipt (`.nfl/receipts/vegas-web-odds-ingest-2026-08-26T22-23-42-403Z.json`) is a dry run that extracted 0 rows, meaning the `data-team`/`data-book`/`data-odds` HTML-attribute scrape pattern doesn't match what VSiN's page actually serves (likely JS-rendered); (3) even fixed, it's scoped too narrow to help — hardcoded to only ever tag rows `market_type: 'superbowl'`, nothing for division/conference/wins/playoffs, and it's Circa/Station prices, not BetOnline/Bookmaker; (4) no GitHub Action wires it into any schedule. `docs/archive/FUTURES_DATA_PACKAGE_DEPTH_EXPANSION_2026-07-30.md` (the project's own data-sourcing map, written a few weeks before Andy stopped capturing) still listed BKR/BetUS/BetOnline acquisition as "mostly MANUAL normalization of exports" with no scrapable alternative identified. **Net effect:** until this is resolved, division/conference/wins/playoffs futures pricing in the dossier has no live feed at all — those markets will silently stay frozen at whatever Andy last captured. Immediate options while the research below is pending: (a) resume manual BEO capture + `node scripts/ingest-beo-screenshots.js` (confirmed working), (b) find/confirm a repeatable BKR path (`scripts/ingest_bookmaker_docx.py` exists but wasn't verified as a current, working, repeatable flow this session), (c) BetUS has no dedicated repeatable script found at all — only a one-off `scripts/import-manual-futures.js` from June. |
| NFL-BACKLOG | Research: find/build a real automated public odds source for BEO/BKR-equivalent futures markets — RESOLVED, source found | P2 | **Done 2026-08-29** (sandbox network access opened via Admin capabilities change mid-session, unblocking this). `agents/vegas-web-odds-ingest.js`'s target URL (`data.vsin.com/nfl/futures/`) was simply dead — 301s to `data.vsin.com/error/`, explaining its 0-row dry run; not a JS-rendering or paywall problem. Traced the real page (`vsin.com/nfl/odds/futures/`) to its actual data source: VSiN embeds a **Metabet / AreYouWatchingThis** odds-widget (`go.metabet.io/js/global.js?siteID=vsin`) whose "futures board" widget calls a public static JSON API — confirmed live: `GET https://metabet.static.api.areyouwatchingthis.com/api/sideodds.json?location=NJ&leagueCode=FBP&apiKey=219f64094f67ed78f035f5f7a08840fc` (the apiKey is embedded client-side in VSiN's own public JS, same one their own site uses — not something we obtained specially). One call returned **all 46 NFL futures markets** in a single JSON payload: Super Bowl, AFC/NFC, all 8 divisions, make-playoffs, regular-season win totals, best/worst record, 17-0/0-17, all major awards (MVP/OPOY/DPOY/OROY/DROY/CPOY/Super Bowl MVP), stat-race markets (passing/rushing/receiving yards+TDs, INTs, sacks), and NFL Draft #1 pick — i.e. exactly the coverage gap TheOddsAPI can't fill. Each market carries **per-book prices from 15+ real providers** (DraftKings, FanDuel, BetMGM, ESPN Bet, Fanatics, William Hill, Unibet, Bet365, Sportingbet, Sports Interaction, BetRivers ×11 states, Underdog) plus a `CONSENSUS` line **and Kalshi** (prediction-market pricing already included natively — covers the domain-#1 backlog item from `FUTURES_DATA_PACKAGE_DEPTH_EXPANSION_2026-07-30.md` for free). Does not include BetOnline/Bookmaker specifically (those are offshore books, never going to appear in a legal-market aggregator) — but replaces the *purpose* they served (a non-TheOddsAPI cross-check on the missing markets) with better depth (15+ regulated books instead of 3 offshore ones) and a real consensus line. **Caveat to flag to Andy before building on this:** this is an undocumented, unofficial endpoint (reverse-engineered from VSiN's public widget JS, not a published/contracted API) — same category of fragility as any scrape (could change shape or get rate-limited without notice), just now a working one instead of a dead one. |
| NFL-BACKLOG | Build `metabet-futures-ingest.js` — wire the Metabet feed into `futures_odds_snapshots` | P1 | **Built + live-ran 2026-08-30** (uncommitted). New `agents/metabet-futures-ingest.js`, modeled on `agents/futures-odds-ingest.js`'s shape (dry-run mode, receipt, hasEnhancedFuturesSchema check, same upsert conflict key `market_type,team,book,snapshot_time`, 30-day prune). One GET to `https://metabet.static.api.areyouwatchingthis.com/api/sideodds.json?location=NJ&leagueCode=FBP&apiKey=...`. **22 of 46 markets ingested**: superbowl/conference/8 divisions/best-worst-record/7 player awards via plain American `price` (multiway pools), PLUS **win totals and make-playoffs** (Andy's explicit ask 2026-08-30) via a verified decimal-odds price1(first side)/price2(second side) → American conversion (`decimalToAmerican()`) — cross-checked live against known-good/known-bad teams (Rams/Chiefs favored on win totals, 49ers/Broncos/Packers heavily favored to make playoffs, Cardinals/Dolphins deep underdogs on both) before trusting it, teams formatted as "Team Over/Under <line>"/"Team Yes"/"Team No" to match `portfolio-dossier.js`'s existing `parseWinSideLabel`/`parsePlayoffSideLabel` parsers exactly — no dossier changes needed. 24 markets still unmapped (stat-race leaders, novelty props) — logged per-run, not silently dropped, not currently needed by `ODDS_SIGNAL_MARKETS`. **Live run**: 16,893 valid rows (0 invalid) written to `futures_odds_snapshots`, verified by querying them back from Supabase directly (not just trusting the script's own success log). `node --check` + `eslint` clean. Book normalization routes provider codes through `src/lib/executionVenues.js`'s `canonicalSportsbookKey()` so MGM→betmgm and William Hill→caesars register as genuinely placeable; everything else (DraftKings, FanDuel, Kalshi, ESPN Bet, Fanatics, Bet365, etc.) passes through as market-context-only, exactly as the existing registry already treats them — nothing here silently becomes "placeable" that wasn't already. **Andy's parallel-run plan**: keep gathering fresh BetOnline/BetUS/Bookmaker manual captures for a few weeks and compare against this feed's coverage of the same markets (division/conference/wins/playoffs/superbowl/awards) before deciding whether to deprecate manual capture — this feed covers 2 of Andy's 6 placeable venues directly (betmgm, caesars) plus strong fair-value context from ~13 other regulated books; it does not and cannot cover BetOnline/BetUS/Bookmaker themselves (offshore, never in a legal aggregator). **Not yet done**: GitHub Action for a daily schedule (same pattern as `futures-odds-ingest.yml`) — not added yet, pending Andy's decision after the parallel-run comparison; git commit — not yet approved. |
| NFL-BACKLOG | Compile BookMaker + BetOnline futures captures, run 3-way comparison vs. Metabet feed | P2 | **Done 2026-08-30.** Read BookMaker.eu's full NFL futures menu live via Claude in Chrome (logged in as BKR249157) — 9 sub-tabs, not just "Odds to Win": Wins (most/fewest), Regular Season Wins (alt win-total lines), Make the Playoffs, Undefeated (Last Undefeated/20-0/17-0), Awards (MVP/OPOY/DPOY/Coach/Comeback — no Rookie of the Year), Stage of Elimination (BookMaker-only novelty market). Compiled to `docs/BookMaker_Futures_20260829.md`. BetOnline could not be automated — both Claude in Chrome and the built-in browser hard-block betonline.ag by policy regardless of login state or who initiates navigation (confirmed: same error whether Claude or Andy navigates); no paid-API alternative found either (Betstamp/SureBet both sell BetOnline feeds but gated behind commercial demo/subscription, no free tier). Andy captured BetOnline manually via 9 screenshots (Super Bowl, conference, division, win totals, make-playoffs — 31/32 teams, Buccaneers playoffs line unavailable) — compiled to `docs/BetOnline_Futures_20260829.md`. **Comparison result:** cross-checked Super Bowl favorites (Rams, Bills, Ravens, Chiefs, Seahawks) across Metabet's regulated-book consensus vs. BookMaker vs. BetOnline — all three cluster within normal cross-book spread, no systematic divergence or stale-line red flags. Good early signal for Andy's parallel-run deprecation decision, but this is one snapshot, not the multi-week trend he's tracking. |
| B-metabet-decimal | **Critical:** `metabet-futures-ingest.js` multiway markets stored decimal odds as literal American odds | P0 | **Found + fixed 2026-08-30.** The `price` field on Metabet's multiway markets (superbowl, conference, division, most/least wins, all awards) is DECIMAL odds (payout multiplier) on every provider including CONSENSUS — not American odds, despite the original code's comment claiming otherwise. The two-sided markets (win totals, make-playoffs) already ran `decimalToAmerican()` correctly; the multiway path just did `Math.round(so.price)` and stored the raw decimal as American odds. Caught while comparing the live Metabet feed against BookMaker/BetOnline: Rams' Super Bowl price showed as `+6` in Supabase (nonsense — a near-even-money favorite) when the raw feed value `6` is decimal 6.0 → American **+500**, matching BookMaker (+495) and BetOnline (+550) almost exactly. Queried Supabase directly: **14,467 of 17,149 rows (84%) from the 2026-08-30 live run were corrupted** this way, across every multiway market type. Fix: multiway path now runs the same `decimalToAmerican()` conversion as the two-sided path (removed the now-dead `americanToProb()` helper, implied-prob now computed as `1/decimal` consistently with the two-sided path). Verified: dry-run + live re-run both show Cardinals SB odds correctly in the +50000/+62500/+75000/+100000 range across books (matches BookMaker +50000-70000, BetOnline +50000). **Remediated live**: purged all 16,893 rows at `snapshot_time = 2026-08-29T17:00:00+00:00`, re-ran the fixed agent, wrote 16,927 corrected rows, spot-checked Rams/Cardinals SB odds directly via Supabase query post-write. `node --check` + `eslint` clean. Not yet committed to git — pending Andy's sign-off alongside the rest of this session's changes. |

### Podcast Intel Pipeline (M6) — All Phases Complete ✅
> Phases 1–8 DONE. All ops closed (S239: migrations applied, odds-proxy deployed, share tokens minted). See DONE section + `docs/PODCAST_PIPELINE_PM_HANDOFF.md`.

### Bugs

| ID | Task | Priority | Notes |
|----|------|----------|-------|
| LINT-1 | Repo-wide lint cleanup (212 pre-existing problems) | P3 | Surfaced 2026-08-09 while validating the FantasyPros ADP ingest (F-26c) — `npm run lint` returns 212 problems (36 errors, 176 warnings) across dozens of files, none belonging to the 4 new FantasyPros files (confirmed via grep). Triaged and written up in `docs/LINT_CLEANUP_BACKLOG_2026-08-09.md`: 2 genuine runtime bugs (`FuturesWatchList.jsx:429` conditional `useMemo`; `agents/vault-rebuilder.js:382` calls an undefined `buildPickPerformanceMd`), 1 false-positive config gap (`src/lib/yahoo.js` — Node-only file misfiled under `src/`), rest is `no-unused-vars` (126), `react-hooks/static-components` (37), `react-hooks/exhaustive-deps` (18), and small cosmetic items. Andy's explicit call: dedicated future session, not folded into F-26c. Recommended order in the doc: 2 real bugs first, then quick wins, then unused-vars in batches, then the two hook rules last (highest judgment-per-fix). |
| F-27a | Podcasts tab black-on-black CSS | P3 | Investigated 2026-07-27 (S314) live via Claude in Chrome — drove every rendering branch of `PodcastDigestTab.jsx` (all 5 feed groups, expanded/collapsed, with/without picks, PARTIAL badge, true empty state) plus a DOM-wide computed-contrast scan. **No repro.** Git history shows the file hasn't changed since it was built. Likely environment-specific (forced-dark-mode extension, OS high-contrast mode) rather than a code defect — **blocked on Andy** for a screenshot + browser/OS combo next time it surfaces. |
| BET-1 | DraftKings/FanDuel bet-slip parsers | P3 | TODO stubs at `src/lib/betImport.js:317,326` — only manual import currently works. |
| PROPS-1 | Player-props data source not wired | P3 | `src/lib/propsTools.js:19` stubbed since F-8; props feature has no live data source. |

---

## ✅ DONE

| ID | Task | Completed | Notes |
|----|------|-----------|-------|
| SYNC-0818 | Handoff sync after Yahoo API pause and Claude rolling-doc pass | 2026-08-18 | Current durable checkpoint: `handoffs/2026-08-18-1555-yahoo-and-handoff-sync.md`. Corrects the 2026-08-16/17 rolling handoff: Andy pushed through `d76d309`, but `655e713` and `2b17c75` are local-only as of the sync (`main...origin/main [ahead 2]`). Yahoo Fantasy API is paused pending Yahoo-side Fantasy Sports API access/provisioning; local OAuth token refreshed, but league settings and ADP dry-run still return `oauth_problem="additional_authorization_required"`, and screenshot review showed no visible Fantasy Sports Read permission option. Kalshi/Polymarket normalization remains uncommitted Codex work in the six modified futures files; preserve and re-run focused checks before any commit. |
| SYNC-0816 | Full working-tree sync pass — commit & push 3+ days of accumulated concurrent-session work | 2026-08-16 | Superseded correction from SYNC-0818: pushed state was verified only through `d76d309`; `655e713` was local-only as of the later sync. Original summary follows. Andy asked for a full sync so any session/team member coming online sees a clean, current picture. 5 commits, `main`/`origin/main` `6a0097d` → `655e713`: `70049b8` UI/UX Command Hub sweep WIP (fixed a real react-hooks/rules-of-hooks bug in `AgentChat.jsx` along the way — a `useEffect` sat after an early conditional return and was being skipped on some renders); `eb23751` FantasyPros-injuries-default-on + a real useless-regex-escape eslint fix in `sportsRelevanceFilter.js` + new `scripts/calculate-win-total-probabilities.js`; `086d2ee` data-snapshot refresh (player-availability, training-camp, 3 gmail alert summaries, 3 staged-not-approved picks proposals); `d76d309` committed the previously-untracked 2026-08-13 futures-incident-review paper trail (Codex brief, Claude response, comparison, 4 handoffs) + the Yahoo API agreement PDF; `655e713` gitignored `dist-verify-*/` and undeletable `_TEST_*` reacquisition fixtures. Verification: `npx eslint` clean on every touched file, `npx vitest run` passing on all tests tied to changed code, `esbuild` syntax check on every new/modified `.jsx` — could **not** get a full `npm run build` to complete within this session's tool time limits (pre-existing sandbox/bridge constraint); recommend Andy run it natively before trusting the frontend sweep in production. Deliberately left untouched: `agents/portfolio-dossier.js`, `scripts/build-prediction-market-map.js`, `scripts/lib/futures-evidence-gates.js`, 2 related test files, and new `scripts/bottom-12-analysis.js` — a concurrent session (Codex/Antigravity) was actively editing these during this pass; still dirty/untracked, not this session's to commit. Full detail: `HANDOFF.md`'s top Pick Up Here entry. |
| F-35 | Substack source registration & cited EMR ingestion (THE WINDOW by Matt Russell) | 2026-07-31 | Registered `THE WINDOW (Matt Russell)` in `config/training-camp-sources.json` and `agents/research-intel-ingest.js` with RSS feed `https://mrussauthentic.substack.com/feed` and `confidence: 0.75`. Built `scripts/build-substack-intel-importer.js` to ingest all 32 team EMR entries, win total shifts, and HFA rules from Matt Russell's July 13 post (`200307567`) with complete source attribution metadata (`source`, `author`, `url`, `published_at`). Updated `scripts/build-article-intel-review.js` to support local research notes and re-generated article review reports (`docs/article-intel-review/article-intel-review-latest.html`/`.md`). Added 3 unit tests (`tests/unit/substackIntelImporter.test.js` - 3/3 PASS). Verified `npm run intel:source-audit`. Committed as `99fd0c5`. |
| F-29b | Official Picks tab live approve/reject smoke test | 2026-08-10 | Closed with explicit approval. Created two clearly fake `f29b-smoke-*` active drafts, verified `/api/inbox` saw `active_count: 2` and proposal-ready readiness, exercised the same ledger approve/reject code paths: approve archived one draft and temporarily raised the local paper ledger from 2 to 3 official-paper picks; reject archived the other draft. Verified `/api/inbox` drained to `active_count: 0`. Then restored the pre-smoke ledger and removed the fake archive files so no fake pick remains. Also fixed a real UI smoke bug found during review: Official Picks candidate confidence now renders whole-number stored confidence as `55% conf` instead of `5500.0% conf`; ROI still uses decimal percent formatting. Verification: focused ESLint on `OfficialPicksTab.jsx` passed; temp Vite production build passed. No Supabase writes, no real official-pick proposal, no betting action. |
| GUARD-FIX | Windows drive-letter-casing bug in the "run directly" CLI guard | 2026-08-09 | Found live while verifying F-26c's ADP work: `npm run report:fantasy` (`agents/fantasy-value-report.js`) exited clean with **zero output** — traced to `path.resolve(process.argv[1]) === fileURLToPath(import.meta.url)`, whose two independently-resolved paths can come back with different drive-letter casing on Windows (`E:\` vs `e:\`), making the case-sensitive `===` silently fail and skip `main()` entirely with no error. Not a one-off — the identical pattern existed in **11 files total** (`agents/fantasy-value-report.js` + 10 `scripts/*.js`, incl. `build-player-availability.js` and `build-availability-impact-digest.js`, both needed for the upcoming §4 injuries build). Fixed everywhere by comparing `import.meta.url === pathToFileURL(process.argv[1]).href` instead — both sides go through the same URL-construction codepath, avoiding the casing mismatch. `node --check` clean on all 11; grep confirms zero remaining occurrences of the old pattern repo-wide. **`agents/fantasy-value-report.js` re-verified live 2026-08-09** — `npm run report:fantasy` now runs and produces real output. The other 10 `scripts/*.js` files are fixed but still unexercised live this session — same fix, same confidence, just not individually re-run yet. |
| F-31 | Futures watchlist — live model re-run | 2026-07-27 | Andy approved the live-run cost. Two dead ends surfaced along the way, both real findings not bugs in this feature: (1) the Anthropic account is out of API credit (`--only opus` attempt: HTTP 400 "credit balance is too low") — Andy's call whether/when to top up, not fixed here; (2) the documented `--models gpt-4o` fallback failed with `context_length_exceeded` (159,154 tokens vs. gpt-4o's 128k limit) on the full dossier — the script already had a purpose-built fix for exactly this, an undocumented-in-the-error-message `--shadow-slim` flag that trims the dossier (row caps per market, slimmed team profiles) before building the prompt. Re-ran with `--dossier .nfl/portfolio/dossier-2026-07-23.json --models gpt-4o --skip-committee --no-persist --shadow-slim --out-suffix watchlist-v2` — succeeded (22.7s, 2244 output tokens, 5 stage-1 candidates -> 3 valid/1 flagged/2 invalidated by the code-owned validator, exactly the safety net working as designed). Confirmed all 6 Human Watchlist Review items rendered with real market data end to end (Buffalo Bills anchor, Green Bay Packers anchor, NY Giants win-total-over, Cincinnati Bengals ATB, Saints make-playoffs, KC Chiefs Super Bowl+exactas) — read `.nfl/portfolio/portfolio-2026-07-27-watchlist-v2.md`'s Human Watchlist Review section directly to verify: every row has a real quote/fair-price/edge-gap/sim-gap/status, nothing placeholder or malformed. `--no-persist` honored, nothing written to Supabase. **Also confirmed a new, generalizable sandbox limitation**: this sandbox cannot make any live external API call at all (Node's `fetch`/`dns` don't route through the sandbox's mandatory proxy that `curl` uses automatically) — any future live-model verification needs Andy to run it natively, same as this one. |
| GAMEID-FORMAT | Shared canonical game_id key across the 3 incompatible formats (option b) | 2026-07-27 | Andy chose option (b) from the 3 documented in `docs/NFL_AUDIT_BACKLOG.md` (add a low-risk helper vs. a full standardize+backfill of live tables/crons, which stays deferred — see backlog). New `src/lib/gameId.js`: `parseGamesTableId()`/`parseOddsSplitsId()`/`parseNflverseId()` parse each real format (`nfl_{season}_{seasonType}_w{WW}_{AWAY}_at_{HOME}` for `games`; `{season}_{WW}_{HOME}_{AWAY}` for odds/splits; `{season}_{WW}_{AWAY}_{HOME}` for nflverse) — source must be specified explicitly for the odds/splits vs. nflverse pair since they're the identical string shape with opposite team order and no positional tell; auto-detection isn't offered because guessing wrong silently swaps home/away. `canonicalGameKey()`/`canonicalGameKeyFromAny()` fold any of them into one new in-memory join key (`{season}-t{seasonType}-w{WW}-{HOME}-{AWAY}`), reusing `src/lib/teams.js`'s existing `getTeamAbbreviation()` alt-code table (already covers nflverse's SD/OAK/LA/STL/JAC landmines) rather than adding a 4th ad hoc team-code map — 3 of those already exist independently across `schedule-ingest.js`/`game-odds-ingest.js`/`betting-splits-ingest.js`. 16 new tests (`tests/unit/gameId.test.js`) prove the actual point: the same real game described by all 3 live formats, including an nflverse alt-code case, collapses to the identical canonical key, and a different `seasonType` correctly produces a *different* key (guards the exact week-number collision `seed-game-context.py` hit in production). `eslint` clean; `npx vitest run tests/unit/gameId.test.js` — 16/16 passing natively in this sandbox (pure functions, fast enough to finish inside the ~45s limit, unlike the Monte Carlo test files). **Scope, by design:** no existing table, ingest script, or cron job was touched — this is a new tool for future joins to use, not a migration of what's already running. |
| OPS-2 | Podcast digest recurring render trigger | 2026-07-27 | Investigation first found the spec's §6 "post-run hook" (incremental re-render after each M6 ingest run) was **already fully wired** — `runRegistry.js`'s `startRun()` already accepts/fires `onRunComplete` fail-softly, and `server.js` already injects it (`renderer.renderForEpisode(input.episode_id)`). What was genuinely missing was the periodic **full** re-render sweep: `onRunComplete` only fires for M6's own ingest runs, so picks graded later by `nfl-auto-grade.js` (a separate GHA process M6 never sees run) could leave already-rendered digest pages stale with no healing mechanism. `render-digests.js` writes to M6's local disk (`/var/lib/nfl/digest/`), so a GitHub Actions cron (this repo's usual pattern, e.g. OPS-1) can't reach it — no SSH/remote-trigger path to M6 exists in `.github/workflows/`. Implemented as an in-process periodic timer inside `packages/m6-podcast-service/src/server.js` instead: reuses the same `renderer` instance already built for the `onRunComplete` hook, calls `renderer.renderAll()` on an interval (new `config.digestRenderAllIntervalMs`, default 6h matching the podcast ingest agent's own GHA cadence, `NFL_DIGEST_RENDER_ALL_INTERVAL_MS=0` to disable), fail-soft (catches + logs, never crashes the service), `.unref()`'d so it can't keep the process alive on its own, and cleared on `SIGTERM`/`SIGINT`. Ships with the service itself — no separate systemd unit or M6-side setup beyond the next normal `git pull` + restart. Verified: `eslint`/`node --check` clean on both touched files; the actually-tested surface (`app.js`'s `buildServer`, exercised by `test/server.test.js`) still passes 10/10 — `server.js` itself isn't under test (confirmed pre-existing, by design, per its own "entry point" comment); the exact timer-callback logic (success + fail-soft error path) verified in isolation against a fake renderer (fires repeatedly, logs correctly, survives a simulated failure). Booting the real service with fake Supabase creds to smoke-test end-to-end hangs indefinitely in this sandbox (confirmed this is pre-existing behavior of the *unmodified* `server.js` too — almost certainly a DNS/network hang on the bogus `.supabase.co` host in this sandboxed network, not something introduced here) — a real native boot check on M6 or with real creds would close the loop. |
| F-34 | Fixed a genuinely broken, silently-failing agent: `agents/vault-rebuilder.js` truncated mid-statement | 2026-07-27 | Found while re-running the full suite natively for F-32: `vaultRebuilder.test.js` failed to even parse (`vite:import-analysis` error). The file itself was truncated mid-statement inside `aggregateExpertLedger()` — traced via `git log`/`git diff` to commit `6b61af5` (an old commit, unrelated to this thread), which added `groupByTeam`/`groupByExpert`/`aggregateExpertLedger` plus a `PickPerformance.md` block and a Receipt-logging block to `main()`, but the write was cut off before finishing — losing the rest of `aggregateExpertLedger`, the `byEpisodeDateDesc()` helper both new group functions call (would have thrown `ReferenceError` at runtime even if the syntax error were fixed), and the CLI-invocation guard at the very end of the file (so running the script directly would've silently done nothing — `main()` was never called). This has apparently been broken since that commit and nobody noticed because the full suite hadn't been run end-to-end in a while (exactly the gap F-32 was tracking). Reconstructed all three pieces: finished `aggregateExpertLedger()` (graded/wins/losses/pushes/units/roi/win_rate/clv_avg, values derived from `tests/unit/vaultRebuilder.test.js`'s exact expected numbers — confirmed byte-for-byte match), restored `byEpisodeDateDesc()` verbatim from the pre-truncation version (`git show 57a8949`), and restored the CLI guard. `node --check` clean, `eslint` clean, `npx vitest run tests/unit/vaultRebuilder.test.js` — 22/22 passing (verified natively in-sandbox, one of the rare files light enough to actually finish inside this sandbox's ~45s timeout). No functional/product impact was found beyond the test suite itself — nothing in `.github/workflows/` or `package.json` currently invokes this script, so it wasn't silently failing in production, just sitting broken and unused. |
| F-32 | Full `npm test`/`vite build` native re-run + live YouTube-intel smoke test | 2026-07-27 | **Part 1 (native re-run):** ran natively by Andy, twice. First pass: `npm test` 802/803, 1 test file failed to parse entirely (`vaultRebuilder.test.js` — see F-34), 1 test timed out (`portfolioSimulate.test.js`'s new calibration test — real ~5.5s computation past vitest's default 5000ms test timeout); `npm run build` failed outright — `FantasyValueBoard.jsx` (F-26, S308, commit `9b4f584`) imported `FANTASY_VALUE_BOARD` as a standalone named export from `src/lib/apiConfig.js`, but it's actually a key on the `LOCAL_DATA` object (checked all 13 files importing from `apiConfig.js` — this was the only mismatch, every other consumer already does it right). Fixed all three same-day (F-34 for the truncation, +20000ms timeout on the slow test, corrected the import + its one call site). Confirmed on the re-run: `npm run build` — `✓ built in 14.56s`; `npm test` — **825/825 passing, 40/40 files**. **Part 2 (live smoke test, via Claude in Chrome against Andy's already-running `npm run dev`):** sent two real chat messages to the Agent tab (`?tab=agent`) — "What's the latest YouTube podcast intel/buzz on Super Bowl futures right now?" and "Any YouTube podcast intel on the Chiefs futures?". Confirmed the **live model tool-call loop genuinely works**: the model correctly selected `get_youtube_futures_intel` both times (not a mock — visible `[tool] get_youtube_futures_intel` badge in the transcript) and correctly paraphrased the tool's `status:'no_data'` fallback into natural language. **New finding surfaced by this smoke test, not previously known:** the tool's fallback fired even though `public/youtube-futures-agent-intel-summary.json` genuinely exists with real data (39 items) — network inspection showed the browser's fetch for that file returned **HTTP 503**, and a few minutes later *all* fetches to the dev server started failing outright (`TypeError: Failed to fetch`, including the app's own root path), meaning the local Vite dev server itself had gone down/unreachable mid-session. Not caused by any file this session touched (no front-end files were edited today). Filed as new backlog item below — needs Andy to check his `npm run dev` terminal for a crash/error and reproduce. |
| F-33c | Feature B — HFA/scale calibration step (spec B.2 step 1) | 2026-07-27 | New `divisionFairProbs()` (extracts de-vigged `fair_prob` per team from the 8 `division_*` synthesis_input markets), `simulateDivisionProbs()` (cheap division-winner-only season simulation, reused by the search), and `calibrateGlobalParams()` (3×3 coarse grid over hfa×scale + an 8-neighbor local refinement pass, minimizing SSE vs. book fair_probs — 2 free params over a smooth surface didn't need a full optimizer). Wired into `runSimulation()`: runs automatically whenever the dossier carries division market rows and the caller hasn't pinned explicit `hfa`/`scale`; no-ops (old 0.28/1 defaults) otherwise, so every existing synthetic/round-robin test dossier is unaffected. `sim.meta` now reports `hfa`, `scale`, and the `calibration` diagnostic (chosen params, SSE, mean \|gap\|, eval count) for auditability. CLI gets `--no-calibrate`/`--calibrate-coarse-sims`/`--calibrate-refine-sims`. **Result on real 2026-07-16 book division prices (32 teams, 8 divisions):** uncalibrated mean \|gap\| ≈0.035-0.042 → calibrated ≈0.017-0.021 (several seeds), landing at or just under the spec's <0.02 "calibration honesty" target — chosen params were `hfa≈0.20-0.28, scale≈0.73` (materially different from the 0.28/1 defaults, i.e. the market really was disagreeing with the uncalibrated model). 7 new tests added to `tests/unit/portfolioSimulate.test.js` (16 total): thin-coverage-returns-null, and calibrated-vs-uncalibrated on the real fixture. The separate, permanent limitation noted in F-33b — that the spec's literal 07-16 "known-case fixture" can't be replayed exactly (that dossier predates both Feature A and schedule persistence) — remains documented in the test file; it's a real-data availability gap, not something to build. Verified via a plain-node harness (26/26 checks, ~22s) since `vitest run` doesn't complete inside this sandbox's ~45s timeout (same F-32 constraint) — needs a native `npm test` run to confirm the official green check. `eslint` clean. |
| F-33b | Feature B (coherence sim) — B.6 test coverage gap | 2026-07-27 | `tests/unit/portfolioSimulate.test.js` grown from 1 test to 9, covering all 5 of B.6's named criteria: conservation (extended to also check matchup marginals equal conference-champion probs), consistency (`mean_wins` vs. input μ, on both a synthetic round-robin schedule and the real 2026 schedule/win_dist fit), determinism (same seed → byte-identical JSON; different seed → different output), calibration honesty (measured against real 2026-07-16 book division prices — see F-33c, it doesn't meet the spec's 0.02 target because the HFA/scale calibration step isn't built), and the known-case fixture (real schedule + real win_dist fit + real 07-16 book prices for NYG/Cardinals/Lions, new `tests/fixtures/portfolio-simulate-2026-schedule.json`, checking gap/CI internal consistency and thin-market `value_gap` suppression — the literal historical qualitative finding doesn't replay, also filed under F-33c). Sim counts tuned down from spec-scale (100k) to keep the file fast without flaking (verified stability via a plain-node harness bypassing vitest, since this sandbox can't complete a `vitest run` within its ~45s command timeout — same constraint as F-32; `eslint` clean). |
| F-30c | Training camp RSS scout — first live-network verification | 2026-07-27 | Andy ran `npm run training-camp:scout:live -- --dry-run` natively (Windows, outside the sandbox). Result: 5/6 feeds `available` (ESPN NFL 2 kept, Pro Football Talk 11 kept, PFF 0 kept, Rotowire NFL 1 kept, Sharp Football 0 kept), 1/6 `error` — Football Outsiders, `fetch failed`. 14 items merged (manual + RSS), 32 teams, 10 with intel. PFF/Sharp Football returning 0 kept is expected (fetched fine, no camp-relevant items matched today) — not a feed-health problem. Football Outsiders' `fetch failed` is a genuine connectivity issue on that one domain specifically (not the sandbox restriction — this ran natively); worth a look next time it recurs but not blocking, since the scout already degrades gracefully (skips it, keeps the other 5). `--dry-run` confirmed no snapshot/report files were written. Receipt: `.nfl/receipts/training-camp-rss-scout-2026-07-27T01-54-30-053Z.json`. |
| OPS-1 | stats-to-vault-sync recurring cron | 2026-07-26 | Commit `fc07e44` (S312). New `.github/workflows/stats-to-vault-sync.yml`, modeled on `intel-to-vault-sync.yml`'s Node-agent-plus-receipt-upload pattern. Note: the board's original suggestion to hang this off `nflverse-data-refresh.yml` doesn't apply directly — that workflow only fetches nflverse CSVs + seeds `nfl_team_season_stats`/`nfl_player_season_stats`; it never calls `stats-to-vault-sync.js` (the separate downstream step that pushes those seeded stats into `vault_notes`). Scheduled for 14:00 UTC March 1 — 4 hours after `nflverse-data-refresh.yml`'s 10:00 UTC annual seed, so this always syncs freshly-seeded stats — plus `workflow_dispatch` with `dry_run`/`seasons`/`team` inputs mirroring the script's own CLI flags. Verified the exact CLI invocation locally (`node agents/stats-to-vault-sync.js --dry-run --seasons 2025 --team KC`) — args parse correctly; only failure was Supabase unreachable from this sandbox (same network restriction as F-30c, not a workflow bug). YAML validated with `python3 -c "import yaml; yaml.safe_load(...)"`. |
| F-33 | Win-total distribution fit + schedule coherence sim — board validator | 2026-07-26 | Commit `f44760e` (S311). **Board correction:** this item's backlog description was stale — Feature A (`agents/lib/win-dist.js`) and Feature B (`agents/portfolio-simulate.js`) were already built and wired into `portfolio-dossier.js`/`portfolio-synthesize.js`, with `tests/unit/winDist.test.js` (4/4) already covering A.5's unit + regression-fixture acceptance criteria. The one genuinely missing piece was the spec's mechanical board validator (A.5) — new `agents/lib/board-validate.js` (pure functions, no I/O, same convention as `win-dist.js`): checks book ∈ `BETTABLE_BOOKS`, `n_books >= 3` (the mechanical kill switch for `most_wins`/`least_wins`-style thin markets), the `superbowl_matchup` sim-price-only policy (locked decision #4), a quoted book+line+price existence check, and an independent dossier-edge cross-check (2pt tolerance). Wired into `portfolio-synthesize.js` right after the existing strict validator, **additive not a replacement** — annotate-and-keep per locked decision #3: violations get stamped onto `candidate.validation` without dropping the candidate, and render as a red "Board Validator Flag" badge + violation list in both the HTML and Markdown reports. 25 new unit tests (`tests/unit/boardValidate.test.js`), all passing; `winDist.test.js`/`portfolioSimulate.test.js` re-run clean (no regression); `eslint` clean on all new/touched code (5 pre-existing unrelated errors elsewhere in `portfolio-synthesize.js` left untouched). Follow-up gap found and filed separately: F-33b (Feature B's B.6 test coverage). |
| F-30b | Training camp intel — Phase 2 (live RSS scout) | 2026-07-26 | Commit `46eef75` (S310). New `scripts/training-camp-rss-scout.js`: byte-capped RSS/Atom fetch of 6 approved free feeds (ESPN NFL, Pro Football Talk, PFF, Rotowire NFL, Sharp Football, Football Outsiders — same URLs already live in `agents/research-intel-ingest.js`), camp-keyword prefilter (`--camp-only`, default true), team tagging via the existing `inferTeams()`, dedup by canonical URL merged with manual notes through the same `dedupeItems`/`buildSnapshot` pipeline as Phase 1 (refactored `scripts/training-camp-intel.js` to export `parseManualDirectory`/`toIntelRecord`/etc. for reuse). Network fetch requires an explicit `--live` flag on every invocation (not just this one-time approval) — `config/training-camp-sources.json`'s `network_fetch_default: false` guardrail stays the resting default. Feed health (per-source status/reason/kept-item counts) is now tracked and rendered in both the Markdown and HTML reports. `npm run training-camp:scout` (preview, no network) / `training-camp:scout:live` (live). 15 new unit tests (`tests/unit/trainingCampRssScout.test.js`, all fetch paths mocked) + Phase 1's existing fixture test both pass; `eslint` clean. Live feed connectivity itself unverified in this sandbox (network fetch blocked) — tracked as F-30c. |
| F-27c | Injury data has no live/mock-fallback indicator | 2026-07-26 | Commit `535f469` (S309). `lib/injuries.js` now tracks per-team mock-fallback via `getInjuryDataSourceState()` (persisted to `nfl_injury_source_v1`, mirrors `enhancedOddsApi.js`'s `getOddsQuotaState` isMock pattern) — set at the end of every `fetchAllInjuries()` run. Surfaced in 3 places: `InjuryCenter.jsx` (league-wide yellow banner listing affected teams + a per-team-card warning icon), `InjuryReportModal.jsx` (SIMULATED badge next to either team's header when applicable), `MatchupCard.jsx` (small amber icon next to the injury summary). `fetchTeamInjuries()`'s public signature is unchanged (still returns an array) — the mock flag rides on an internal `_fetchTeamInjuriesWithSource` helper, so no other call sites needed changes. |
| F-27d | PulseModal "Critical Injuries" section is dead | 2026-07-26 | Commit `535f469` (S309). Replaced the hardcoded "No critical tags loaded from API" placeholder with real data: `getInsights()` now walks each game's already-merged `injuries.home`/`injuries.visitor` (the same shape App.jsx builds for `MatchupCard`) and collects OUT/critical-impact players into a sorted list, rendered as name/team/position + status chip (up to 8, worst-first). No new data source or fetch — reuses what `gamesWithSplits` already carries. |
| F-27e | ContestLinesModal "Fetch Official Lines" button is a dead end | 2026-07-26 | Commit `535f469` (S309). Removed the button (and its fake 800ms spinner + `alert()`) entirely; replaced with a short static note next to the still-functional "Sync Live Odds" button explaining official lines aren't available via API and to use manual entry or sync. |
| — | Fantasy value board 0-match join bug root-caused + fixed | 2026-07-26 | Commit `57b2d3c` (S307). Every run since the board was first built (07-16, 07-18) showed 200/200 "no projection" — root cause was two-fold: `player_season_stats` was never seeded in production (fixed, 7963 rows), then `fetchSeasonStats()`'s unfiltered query was silently truncated by Supabase's default 1000-row cap before reaching QB/RB/WR/TE (fixed with `.in('position', POSITIONS)`). Verified: 62 value plays / 42 reaches / 45 no-projection on re-run — first real output this pipeline has ever produced. |
| F-27b | Dashboard matchup cards show wrong game time | 2026-07-26 | Commit `fb316f2` (S306). `Dashboard.jsx`'s `enriched` memo fabricated `commence_time: new Date().toISOString()` for every game, so `MatchupCard.jsx:308` rendered "right now" as the kickoff time on every card. Fixed to use `schedule.json`'s real `kickoff_utc`. Also dropped the unused `status`/`home_score`/`visitor_score` fields riding alongside it — confirmed nothing downstream read them. |
| F-27 | UI QC pass | 2026-07-26 | Audit-only per its own scope — no code changes. Findings written up in full at `docs/F27_UI_QC_FINDINGS_2026-07-26.md`. Checked all 17 tabs for dead routes (none — `VALID_TABS`/`Header.jsx`/`App.jsx` render block all agree), empty click handlers/dead links (none found), and swept for `TODO`/`stub`/`placeholder` markers + native `alert`/`confirm` usage. Real findings spun out as F-27b (P1, Dashboard shows wrong game time on every card), F-27c (P2, injury data has no live/mock indicator), F-27d (P2, PulseModal's Critical Injuries section is a dead placeholder), F-27e (P3, ContestLinesModal's official-lines button is a no-op). Also noted: BET-1/PROPS-1 stubs are already tracked (no new action); 46 native `alert()`/`confirm()` calls across 18 files are clunky but not broken (no dedicated task filed — cosmetic). |
| F-25 | Player Injury UI — dedicated view | 2026-07-25 | Commit `ba00bfe` (S304). Per-game injury UI (MatchupCard badges + InjuryReportModal) was already fully wired -- confirmed by tracing the data path end to end. Built the actual missing piece: `InjuryCenter.jsx` (`?tab=injuries`), a league-wide view of all 32 teams sorted worst-impact-first, with a stats bar, player/team search, status filter chips, and a "hide clear teams" toggle. Extracted the impact-rollup logic into a shared `getTeamImpactSummary()` in `lib/injuries.js` (also fixes a pre-existing cosmetic bug where zero-injury teams were mislabeled "Minor" instead of "Clear"); `InjuryReportModal.jsx` now uses the same helper. No new data source — reuses the existing `useSchedule` injuries fetch. |
| F-29 | Platinum Rose AI official picks tab | 2026-07-25 | Commit `179b3cc` (S303). New lazy-loaded `OfficialPicksTab.jsx` (`?tab=official-picks`) wires the existing local inbox server (127.0.0.1:8787) + ledger scorecard report into the dashboard: server-availability probe with offline/retry state, stats bar, candidate cards with readiness badges and Approve/Reject actions, and an embedded ledger-report iframe. Added CORS + OPTIONS preflight to `official-pick-inbox-server.js` so the browser can call it cross-origin. Does not generate proposals — approve/reject only; no Supabase writes. Approve/Reject buttons not yet exercised against a live draft (inbox currently empty) — tracked as F-29b. Migration 044 remains schema-only, not applied. |
| S302 | FUTURES/BETTING agent wiring — local YouTube/Gemini intel | 2026-07-25 | Commit `95cca82`. New `get_youtube_futures_intel` tool in `PODCAST_INTEL_TOOLS` (`src/lib/agentTools.js`) — team/market/lane filters, `review_flags`/`supporting_quote`/`source` preserved verbatim. `scripts/build-youtube-futures-agent-intel-summary.js` now syncs a public copy for the browser-side tool to fetch. Both agent manifests + `FuturesAgentChat.jsx` updated. `tests/unit/agentTools.test.js` 94/94 (89 pre-existing + 5 new). Full `npm test` not yet re-run — see F-32. |
| S300-S301 | YouTube/Gemini local intel pipeline + Gemini 3.5 Flash shadow harness | 2026-07-24/25 | Commit `a169d09`. OAuth + discovery (`scripts/youtube-oauth-setup.js`, `youtube-account-discovery.js`, `youtube-podcast-sweep.js`); 11 futures-eligible episodes processed; human review gate -> local export -> agent-intel summary (39 items promoted, 1 bad DET `division_winner +1500` rejected, `det_bad_leaks=0`). `scripts/gemini-podcast-shadow-harness.js` refactored into `--simulate`/`--live-shadow` modes; real Gemini 3.5 Flash API verified (non-circular 7-dim match scoring, ~20.9s-32.8s latency, ~$0.002-$0.004/run). Architecture specs in `docs/antigravity/` (Antigravity-authored). Local-only; no official picks, no Supabase writes. |
| F-30 (Phase 1) | Training camp intel snapshots — local-first, 32-team | 2026-07-24 | Commit `984a52b`. `scripts/training-camp-intel.js`: manual Markdown/text/JSON importer, snapshot writer (`data/training-camp/2026/`), report generator (`.nfl/training-camp/`); guarantees all 32 teams appear; deterministic signal classification. `scripts/test-training-camp-intel.js` + fixtures prove 32-team coverage. Free/manual sources only per Andy's decision; Phase 2 live RSS explicitly deferred (F-30b). |
| S296-S298 | Futures-agent reasoning layer + portfolio construction + official-picks ledger | 2026-07-21/22 | Commit `c3e8bd6`. S296: 4 new data domains wired into FUTURES/BETTING agent tools. S297: 3-stage Analyst Committee reasoning (Market+Football/Skeptic/Risk-Editor) + a full Codex external-review-and-fix cycle. S298: hedge-basket/parlay-ladder portfolio-construction layer; fixed a context-budget bug (team_profiles dedupe) and a matchup-mislabeling bug found on the first live dossier/synthesize run. Also lands the durable human futures watchlist (`data/futures-imports/futures-watchlist-2026.json`) and its deterministic Human Watchlist Review report section (2026-07-23; live re-verification tracked as F-31), plus a schema-only official-picks paper-ledger migration (`044_platinum_rose_ai_official_picks_and_team_profiles.sql`, not yet applied — feeds F-29). |
| — | Podcast speaker-attribution, transcript deep-dives, diarization exports | 2026-07-22 | Commit `26d9463`. BettingPros co-host attribution regression fixed (real-person roster matching, exact-alias priority, adjacent-intro tie-break). New offline transcript deep-dive report builder (50 episodes, sentence-level beat splitting, ad-read filtering). NFL-relevance filtering hardened against cross-sport hallucination and non-future leakage. M6 diarized transcript exports + Obsidian reconciled-notes publishing. |
| F-28 | Podcast digest pages return `not_found` | 2026-07-01 | **Two independent causes, both fixed.** (1) Deployment gap (S243): `/var/lib/nfl/digest/` had been empty since creation Jun 2 — 59 `status='done'` episodes were never rendered, because neither intended render trigger (incremental post-run hook nor cron/CLI backfill, `docs/PODCAST_PHASE7A_RENDER_SPEC.md` §6/§7) was ever wired to a running cron/systemd unit. (2) Real code bug (S244, found only by actually running the backfill): `render/index.js`'s `EPISODE_SELECT` queried `extraction_model`/`extraction_quality_score` as top-level `podcast_episodes` columns, but migration `023_podcast_pipeline_v2.sql` only ever added those columns to `podcast_transcripts` — every backfill attempt failed with `column podcast_episodes.extraction_model does not exist` regardless of path/env fixes. Unit tests never caught it because the fake Supabase test client's `.select()` is a no-op passthrough. Fixed: `EPISODE_SELECT` now nests the fields inside the `podcast_transcripts(...)` embed; `renderEpisodeBody` reads them off the normalized transcript object; test fixtures updated to match. Also fixed the backfill script path confusion (`packages/m6-podcast-service/scripts/render-digests.js`, not repo-root `scripts/`). Commit `9089759`, M6 pulled, confirmed live: `written 68 files (59 episodes, 4 experts, 1 weeks)`, `curl localhost:5060/digest/episodes/<id>.html` → `200`. **Still open, not yet done:** no recurring render trigger exists yet — new episodes still require a manual `render-digests.js all` until a systemd timer or the post-run hook is actually deployed (see spec §6/§7). Podcasts tab black-on-black CSS spun out as F-27a — confirmed unrelated now that files render. |
| F-24 | Debug Tailscale always-on URL | 2026-06-30 | Root cause: stale dist bundle had dead `atlas.tail1e459d.ts.net` frozen in. Real machine is `atlas-m6`. `nfl-podcast.service` healthy on port 5060 since Jun 24. Tailscale Funnel correctly configured (`https://atlas-m6.tail1e459d.ts.net → :5060`). Fix: rebuilt dist with correct `VITE_M6_BASE`, deployed via scp to M6 `/var/www/nfl-dashboard/`. CI fix: added `VITE_M6_BASE` + `VITE_M6_FUNNEL_BASE` to `deploy.yml` (commit `39db924`). Merged remote schedule update + pushed `0a82e5d`. M6 needs `git pull`. |
| F-0 | Phase 1: Governance Foundation | 2026-04-02 | SOUL.md, RULES.md, WORKING-CONTEXT.md, TASK_BOARD.md, AGENTS.md |
| F-1 | Phase 2: Contexts + Hooks + Rules | 2026-04-02 | contexts/ (5), hooks/hooks.json, rules/ (4) |
| F-2 | Phase 3: Dev Agent Architecture | 2026-04-02 | agents/dev/ — 15 adapted YAML-frontmatter prompts |
| F-3 | Phase 4: Product Agent Layer | 2026-04-02 | BETTING.md, INTEL.md (tier1), betting.manifest.json |
| F-4 | Phase 5: CLAUDE.md Consolidation | 2026-04-02 | Orchestration directives, session protocols, commands |
| F-5 | Phase 6: NFL-Specific Additions | 2026-04-02 | ANTI_PATTERNS.md, HANDOFF_PROMPT.md, AGENT_LOCK.json, GOTCHAS.md, gen_resume.js |
| F-6 | Agent Chat POC (BETTING agent) | 2026-04-02 | anthropicClient.js, agentTools.js, AgentChat.jsx — Agent tab live |
| F-7 | DFS Lineup Optimizer | 2026-04-02 | DFSOptimizer.jsx — DK/FD platform toggle, greedy optimizer, lock/exclude, save lineups |
| F-8 | Props Agent | 2026-04-17 | propsTools.js (7 tools), PropsAgentChat.jsx, PROPS.md + manifest, Props tab — stubbed data sources flagged |
| DS-2 | Build 2026 season schedule spine (`games`) | 2026-05-17 | `agents/schedule-ingest.js` + migration 007 applied; 272 games (weeks 1–18) in Supabase + `public/schedule.json`; deterministic `game_id`; `toolAnalyzeMatchup` now uses `normalizeTeam` for exact abbreviation lookup; receipt writing added. |
| DS-3 | Expand futures ingest breadth (`futures_odds_snapshots`) | 2026-05-17 | `agents/futures-odds-ingest.js` covers 10 markets (SB + conf + division + 6 awards); migration 008 applied (selection/price/captured_at/season columns); conf/division/awards return 404 from TheOddsAPI offseason — handled gracefully; Bookmaker manual snapshot imported 96 rows; live SB snapshot 96 rows written today. |
| DS-4 | Research intel ingest v1 (`research_intel_notes`, `research_pick_signals`) | 2026-05-18 | `agents/research-intel-ingest.js` + migration 009 applied; Action Network, VSiN, ESPN NFL RSS feeds active; NFL offseason keyword filter (36 terms); non-NFL contamination fix (title-only for NON_NFL_HINTS); GHA workflow (`.github/workflows/research-intel-ingest.yml`) runs 09:00 + 21:00 UTC daily; BETTING agent now loads intel at session start via `getRecentResearchIntelNotes`/`getRecentResearchPickSignals` injected as `### Research Intel` block in `buildSystemPrompt`; 60+ notes + 34+ signals live in Supabase. |
| F-9 | Sunday Slate Briefing mode (BETTING agent proactive entry) | 2026-05-18 | Season-aware `PROACTIVE_BRIEF_PROMPT` (offseason/regular/playoffs branches); `buildSystemPrompt` now passes `phase` + tool-use guidance block; `react-markdown` + `remark-gfm` render markdown in `AssistantMessage`; `AgentStatusBar` phase color (green in-season, grey offseason); offseason empty-state suggestions (futures/open-picks). |
| F-10 | Performance feedback loop | 2026-05-18 | `get_performance_stats` tool added to `agentTools.js` (8th tool); `buildCalibrationSummary()` helper computes all-time record, units, ROI, last-10, high-conf win rate from local picks; injected as `### Performance` block in `buildSystemPrompt`; ack instruction updated; 3 new tests (68 total). |
| F-11 | Intel search tool (`search_intel`) Phase 1 | 2026-05-18 | `searchResearchIntel(query, opts)` added to `supabase.js`; `search_intel` (9th BETTING tool) in `agentTools.js` with full `toolSearchIntel()` impl; `ilike` on title+summary, optional source/hours/limit params, returns notes + attached pick_signals; system prompt tool list + `ToolCallCard` label updated; 4 new tests (72 total). |
| F-11 | Intel search tool Phase 2 (FTS + body scraping) | 2026-05-18 | Migration 011 adds `body text` + `tsv tsvector` + GIN index + auto-trigger to `research_intel_notes`; `searchResearchIntel` upgraded to Postgres FTS (`textSearch` via tsvector) with ilike fallback pre-migration; ingest agent adds `fetchArticleBody()` (8s timeout, HTML strip, 4000-char cap) + `INTEL_FETCH_BODY=true` in GHA env; back-fill trigger covers existing rows. |
| DS-5 | `game_odds_snapshots` — game-level odds time series | 2026-05-18 | Migration 010: append-only table with (game_id, season, week, home/away team, book, market, spread, total, home/away price, captured_at); GIN indexes on game_id+market and week; RLS public read. `agents/game-odds-ingest.js`: TheOddsAPI spreads+h2h+totals, team normalization map, week derivation from commence_time, chunked Supabase insert, TTL prune, receipt. GHA `.github/workflows/game-odds-ingest.yml` runs 4×/day offseason, hourly on game days (Thu/Sun/Mon). `getGameOddsForWeek(week, season)` added to `supabase.js`. |
| F-15 | Props auto-grade GHA agent | 2026-05-18 | `.github/workflows/props-auto-grade.yml` mirrors nfl-auto-grade.yml schedule (hourly Sun/Thu/Mon, 4h other days); manual --week override; `props-auto-grade.js` updated to use `SUPABASE_SERVICE_ROLE_KEY` + `dotenv/config` (was incorrectly using ANON_KEY). Degrades gracefully when `player_stats` table absent. |
| DS-1 | 2026 Data Sprint Kickoff (scope + sequencing) | 2026-05-20 | All sub-tasks complete: DS-2 (schedule spine), DS-3 (futures breadth), DS-4 (research intel), DS-5 (game odds snapshots). All 5 tables live in Supabase. GHA ingest agents active. |
| F-13 | X/Twitter sharp-account ingestion via RSSHub | 2026-05-18 | `config/sharp-accounts.json` (8 accounts: Warren Sharp, Action Network, VSiN, PFF, etc.); migration 013 (`x_sharp_tweets` table — FTS + RLS; applied ✅); `agents/x-sharp-ingest.js` (GHA agent; RSSHub RSS; dedup via url_hash); GHA workflow every 4h + hourly game days; `searchSharpTweets` + `getRecentSharpTweets` in supabase.js; `search_sharp_tweets` tool (12 tools total); sharp tweets injected as `### Sharp Account Tweets` block in system prompt. 84/84 tests passing. Commit `4d1125b`. |
| F-12 | NFL Betting Vault (Obsidian + Supabase dual backend) | 2026-05-18 | Migration 012 applied (`vault_notes` table: FTS/tsvector, GIN indexes, auto-updated_at trigger, RLS, 4 seeded reference stubs). `src/lib/vaultClient.js`: dual-backend VaultClient (ObsidianRESTBackend + SupabaseVaultBackend), switch via `VITE_VAULT_BACKEND` env. `agentTools.js`: `read_vault_note` + `write_vault_note` tools (11 tools total; path-scoped to `NFL/` for safety). `AgentChat.jsx`: vault reference notes pre-loaded at session start, injected as `### Vault Reference Notes` system-prompt block (3000-char cap). `agents/obsidian-vault-sync.js`: one-shot Obsidian → Supabase sync for production sharing. Flip `VITE_VAULT_BACKEND=supabase` when sharing with betting partners. 80/80 tests passing. |
| F-19 | Player injuries ingest (`player_injuries` table) | 2026-05-21 | Migration 016: `player_injuries` table (espn_injury_id upsert key, Out/Doubtful/Questionable/IR/PUP statuses, captured_at). `agents/injury-ingest.js`: ESPN NFL injury report API + team roster loop, dedup on espn_injury_id, receipt. GHA `injury-ingest.yml` daily + pre-game days. |
| F-22 | Injuries context injection (BETTING agent) | 2026-05-21 | `getRecentPlayerInjuries(168, 100)` in supabase.js; `buildInjurySummary()` helper; `### Recent Injuries` block in `buildSystemPrompt`; loaded in `Promise.all` at session mount. Commit `36e3c3d`. |
| F-23 | Current game lines pre-load (BETTING agent) | 2026-05-21 | `getLatestWeekOdds(week, season)` in supabase.js with DraftKings-first dedup; `buildOddsSummary()` helper; `### Current Lines` block in `buildSystemPrompt`. Commit `36e3c3d`. |
| F-21 | Action Network betting splits (Pillar 2) | 2026-05-21 | Migration 017: `game_splits` table (upsert on game_id; spread/total/ML home % for bettors + money). `agents/betting-splits-ingest.js`: AN public API, multi-shape field extraction, --dry-run/--dump flags, graceful offseason exit. GHA `betting-splits-ingest.yml`: 2h game days, 3×/day game week. `getGameSplitsForWeek(week, season)` in supabase.js. `get_betting_splits` tool (#13) in agentTools.js. `buildSplitsSummary()` + `### Betting Splits` context block in AgentChat.jsx. 84/84 tests. Commit `36e3c3d`. |
| F-15/F-16 | nflverse PBP seed + formation cols + stats-to-vault bridge | 2026-05-22 | Commit `5025af4`. Migration 015 (3 formation cols on `nfl_team_season_stats`, applied ✅). `scripts/seed-historical-stats.py` (direct nflverse Parquet CDN via httpx; `shotgun_rate`/`no_huddle_rate`/`pass_rate`; `--no-pbp`/`--cache-dir`/`--dry-run`). 192 rows (32 teams × 6 seasons) seeded. `agents/stats-to-vault-sync.js` writes per-team + league-wide vault notes. **Note: vault sync NOT yet run live.** (F-15 ID collides with Props auto-grade — distinguish by hash.) |
| F-17 | Analytical RSS ingest into vault | 2026-05-22 | Commit `8d7c34e`. Analytical RSS feeds + Atom parser + `source_type` split in vault. Articles (Football Outsiders, etc.) flow into `research_intel_notes` for vault sync. |
| F-19 | Player injuries ingest (`player_injuries` table) | 2026-05-21 | Commits `44419cf`/`982d712`/`fa5058b`. Migration 016 + 018 (public read RLS). `agents/injury-ingest.js`: ESPN injury API + roster loop, dedup on espn_injury_id, GHA daily. |
| F-20 | Futures intel report + vault export | 2026-05-22 | Commit `01618bc`. Futures intel report, vault export, cron fixes. |
| OPS | NFL daily brief email agent | 2026-05-22 | Commits `d595a9e`/`8a51e5f`/`37d36c6`. `agents/nfl-daily-brief.js` + GHA workflow; gmail + hotmail recipients. |
| AUDIT | Security & Quality tri-audit (30/30) | 2026-05-23 | S139→S152. All CRITICAL/HIGH/MEDIUM/LOW items closed. See `docs/NFL_AUDIT_BACKLOG.md` + receipt `docs/AUDIT_RECEIPT_2026-05-23.md`. CI gate (`ci.yml`) live. |
| UI | 12-tab nav + odds/routing fixes | 2026-05-23 | `b64b0a7` compact 12-tab bar (no scrollbars) + OddsCenter sub-tab fix; `8d9f1d3` LiveOddsDashboard reads `game_odds_snapshots`, clears isMock on hit; `68d5873` brief→dashboard deeplinks + URL tab routing; `6ecb316` game-odds-ingest ESM/season fixes; `c1898b2` removed legacy VSiN scrape pipeline. |
| PODCAST-M6 | Podcast intel pipeline v2 (Phases 1–6) | 2026-06-03 | Commits `64b279d`→`df020a4`. P1 schema migration (M6 paths/quality/share tokens), P2 Fastify service skeleton (HMAC/runs/systemd) in `packages/m6-podcast-service/`, P3 Python transcription, P4 Python extractor + quality gate, P5 vault-rebuilder agent (fence-guard auto-sections), P6a 6 Supabase query helpers (12/12), P6b `PODCAST_INTEL_TOOLS` (6 tools) + executor, P6c `agents/manifests/futures.manifest.json`, P6d `FuturesAgentChat.jsx` + `?tab=futures-agent` (spec divergence: `?tab=futures` kept for FuturesPortfolio), P6e podcast tools in `betting.manifest.json`. See `docs/PODCAST_PIPELINE_PM_HANDOFF.md`. |
| FUT-TOOLS | Futures-specific agent tools | 2026-06-05 | Commits `896455c`, `a48cfb5`. Implemented `analyze_futures_hedge` (3-scenario hedge model: hold/partial/full lock with line-appreciation context), `project_division_paths` (division winner odds + implied probs from Supabase), `track_award_race` (MVP/OPOY/DPOY/OROY/DROY/CPOY/COY ranked leaderboard). FUTURES_TOOLS exported; FuturesAgentChat wired; futures.manifest.json updated. 578/578 tests. |
| vault-seed | Vault reference data ingestion agent | 2026-06-05 | Commit `a48cfb5`. `agents/vault-seed.js`: CSV/JSON/MD ingestion into vault_notes. Auto-detects schema (PFF, ATS, splits, DVOA, nflverse). Writes league-wide + per-team notes. `data/vault-seed/{pff,ats,splits,dvoa,nflverse,manual}/` drop dirs. README with column specs. 607/607 tests. |
| PODCAST-P7-P8 | Podcast pipeline Phases 7a/7-serving/7b/7c/8 | 2026-06-04 | Commit `51ac195`. P7a: static digest renderer (`render/` — 4 files, 10/10 tests). P7-serving: `/digest/*` Fastify routes + `src/digest.js` (14/14 tests). P7b: `PodcastDigestTab.jsx` + `?tab=podcasts` lazy route + `apiConfig.M6`. P7c: `fetchTopPodcastPicks` + `renderTopPodcastPicks` in `nfl-daily-brief.js`. P8: signed `/share/*` partner surface + `src/share.js` + `scripts/share-token.js` (10/10 tests). Migrations 019+023 applied. ai-proxy Edge Fn deployed. GPT-4o auto-fallback wired. 64/64 tests. All pending ops closed (S158/S239): `VITE_M6_BASE` set, share tokens minted for Patrick + Amanda, `odds-proxy` deployed. |
| B-1 | `oddsIdempotent` tests (futures upsert path) | 2026-06-03 | Root cause: `f1e6f19` reverted S152 upsert path to delete-then-insert. Restored `.upsert(..., { onConflict: 'market_type,team,book,snapshot_time' })` in `agents/futures-odds-ingest.js`. 552/552 green. Committed. |
| PODCAST-L1-L5 | Local diarization pipeline (S235–S238) | 2026-06-30 | L1-L3 (`68da421`): `diarize.py` (pyannote 4.x), `speaker_map.py`, `show_hosts.json`, `experts_roster.json`; `transcribe.py` patched with `--diarize`/`--show-name`; 54 tests. Six pyannote 4.x compat fixes (generator API, DiarizeOutput unwrap, audio pre-load, PLDA patch). L4 (`5586a56`): `extract.py` gains `labeled_transcript` param; `prompts.py` adds speaker attribution. L5: `vault_note.py` — `build_vault_note` + `upsert_vault_note`, 32 tests. |
| PODCAST-B1-B4 | Vault Bridge — podcast episode vault notes (S238–S239) | 2026-06-30 | B1: schema designed (`docs/LOCAL_PIPELINE_SPEC.md §8`). B2: `vault_note.py` — 32 tests (`5586a56`). B3: `backfill_vault_notes.py` — 59 done episodes backfilled (`5586a56`). B4 (`13fb03f`): `search_episode_vault_notes` added to `PODCAST_INTEL_TOOLS`; `betting.manifest.json` updated (14 tools); `futures.manifest.json` gets `search_episode_vault_notes` + `read_vault_note`. |
| S239-OPS | Production ops close-out | 2026-06-30 | Commit `13fb03f`. Migrations 018/019/021/022 confirmed applied (all return "already exists"). `odds-proxy` Edge Fn deployed (was never deployed). Phantom `clever-endpoint` duplicate deleted. `supabase secrets list` confirms ANTHROPIC/OPENAI/ODDS keys set. |
| S240-RECOVERY | NTFS recovery + LF enforcement | 2026-06-30 | Commit `2c0fb68`. 10 NTFS-truncated working-tree files restored from git HEAD objects (futures-intel-report-v2.js, experts.js, agentTools.js, research-intel-ingest.js, diarize.py, extract.py, prompts.py, vault_note.py, test_extract.py, backfill_vault_notes.py). `.gitattributes` (`* text=auto eol=lf`) committed via git plumbing. Known index quirk: `.gitattributes` not in git index (plumbing bypass) — cosmetic only, file is in HEAD tree. |

---

## 🚫 BLOCKED

| ID | Task | Blocker | Notes |
|----|------|---------|-------|
| — | (none) | — | — |

---

## Legend
- **Priority:** P0 (critical path) · P1 (high) · P2 (medium) · P3 (low/backlog)
- **Prefixes:** F- = feature, B- = bug
