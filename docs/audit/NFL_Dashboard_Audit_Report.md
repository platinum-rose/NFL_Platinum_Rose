# NFL Dashboard (Platinum Rose) — Audit Report

*Audit date: 2026-07-07 · Auditor: Claude (Fable), full-codebase audit per Fable_Project_Audit_Prompt*

## Executive summary

NFL Platinum Rose is a well-run project that has already fixed its scariest historical problem — API keys in the public browser bundle — with a clean server-side proxy pattern, and it shows strong engineering hygiene (centralized API config, a localStorage key catalog with permanence levels, receipts on agent runs, an anti-patterns doc). The most significant open issue is a guardrail one: the Obsidian→Supabase vault sync copies note content into a cloud table readable by betting partners **without checking sensitivity tiers**, so a mis-filed or unlabeled note would be exported when the fail-safe rule says it must not be. The biggest structural facts to plan around: all automation (19 workflows) runs on GitHub Actions' cloud scheduler, the app is hosted on public GitHub Pages, and "critical" user-entered picks live in browser localStorage. None of these are wrong today; all three should be staged toward owned infrastructure as the project matures.

## Findings table

| # | Issue | Location | Category | Severity | Recommended action | Status | Plain-language explanation |
|---|-------|----------|----------|----------|-------------------|--------|---------------------------|
| 1 | Vault→cloud export has no sensitivity-tier check: `obsidian-vault-sync.js` copies every note under the `NFL/` prefix from the local Obsidian vault into the Supabase `vault_notes` table (readable via the public anon key by design, for betting partners). No `sensitivity:` frontmatter is read; the fail-safe rule (missing/invalid → treat as 🔴, content never leaves) is not applied. NFL notes are *probably* all shareable — but the guardrail exists precisely for the mis-filed exception. | `agents/obsidian-vault-sync.js` (no `sensitivity` handling anywhere in the file) | security / guardrail | **high** | Parse frontmatter before export; skip (and log) any note that isn't explicitly `green` — or explicitly `yellow` if you decide partner-visible betting IP is acceptable, but that's your call to make, not the sync's default. Flagged, not fixed, per guardrail rules. | **flagged for review** | A copying machine moves your NFL notes from your private vault to a shared shelf your betting partners can read. It never checks the privacy label on a note before copying. Today everything in that folder is probably fine to share — but the whole point of labels is the day something isn't. |
| 2 | All automation lives on GitHub Actions: 19 workflow files run the ingest/grading/reporting agents on GitHub's cloud cron (game-day hourlies, 4-hour cycles, weekly reports). If GitHub is down, rate-limits, or the repo becomes private-billing-constrained, every automated data flow stops. | `.github/workflows/*.yml` (19 files) | local-first / architecture | med | Keep GHA for now (it's free and reliable); write the equivalent systemd timers for M6 so the agents *can* run at home — most already read env vars and run via plain `node`, so this is mostly packaging. Tag `[Local-First]`, defer full cutover. | flagged (design) | Every robot that fetches odds, grades picks, and writes reports lives in GitHub's building, not yours. They're good tenants — but you don't own the building, and the agents themselves would run fine from the M6 box at home with minor setup. |
| 3 | Stale docs could reintroduce a fixed vulnerability: `CLAUDE.md`'s environment section still instructs setting `VITE_OPENAI_API_KEY` and `VITE_ODDS_API_KEY` — the exact pattern that bakes secret keys into the public browser bundle, which `src/lib/apiConfig.js` deliberately eliminated (keys now live server-side behind `ai-proxy`/`odds-proxy` edge functions, and the config file says "must never appear in the browser bundle"). | `CLAUDE.md` env-vars section vs `src/lib/apiConfig.js:10-27` | security / docs | med | Update CLAUDE.md to document only `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` for the browser, and point to the proxy pattern for everything paid. Flagged rather than auto-fixed: this audit's editing tool rewrites whole-file line endings (see ATLAS report Finding 2/3), which would bury a two-line doc fix in noise on a governance-critical file. | flagged for review | The instruction manual still teaches the old, dangerous way of storing keys — the way that would publish your OpenAI key to the internet, since the app itself is hosted publicly. The code was fixed; the manual wasn't. Someone following the manual in good faith would re-open the hole. |
| 4 | "Critical" user data in browser localStorage: the storage catalog marks expert-consensus and picks data `permanence: 'critical'` ("must never be accidentally wiped — user entered data"), yet it lives in localStorage — erased by a browser-profile reset, "clear site data", or switching machines. A manual backup/restore feature exists but relies on remembering to use it. | `src/lib/storage.js` (PR_STORAGE_KEYS catalog) | data-ownership | med | Sync critical-permanence keys to Supabase (the infrastructure is already there) with localStorage as cache; or at minimum auto-download a backup file weekly. Tag `[Data-Ownership]`. | flagged for review | Your hand-entered picks — the data the code itself labels "must never be lost" — are stored in the browser's short-term memory. One overzealous "clear browsing data" click and a season of records is gone. The database that could hold them safely is already running. |
| 5 | Dead/superseded files: `agents/futures-intel-report.js` (v1) is referenced by nothing except v2's "Supersedes" comment; `agents/podcast-e1011-test.js` is a one-off episode test script left in the agents directory. | `agents/futures-intel-report.js`, `agents/podcast-e1011-test.js` | dead code | low | Delete v1 (git history preserves it); move the episode test under a `agents/dev/` or tests directory. Not auto-fixed: file deletion was not possible through this audit environment's file layer (mount blocks unlink), and flagging keeps the removal reviewable. | flagged for review | An old version of the futures-report robot is still parked next to the new one, and a one-time test script never got cleaned up. Harmless, but the next person has to figure out which robot is real. |
| 6 | `VITE_BYPASS_AUTH === 'true'` skips the auth gate if set at build time. Fine as a dev convenience; dangerous only if it ever leaks into the production build command. | `src/components/auth/AuthGate.jsx:151` | security | low | Add a build-time assertion in `deploy.yml` that fails if `VITE_BYPASS_AUTH` is set. One-line CI guard. | flagged for review | There's a "leave the front door unlocked" switch for testing. It's off in production — worth adding a tripwire so it can never be accidentally left on when the house ships. |
| 7 | Vulture flagged `style` parameter in `_log()` as unused — **false positive in practice**: the parameter absorbs legacy Rich-markup call sites after the rich dependency was removed; removing it breaks callers. Documented so future audits don't "fix" it. | `scripts/fetch_nflverse_data.py:48` | deps-tests | low | Optionally rename to `_style` to silence analyzers. No behavior change made. | no change | An automated tool suggested deleting something that looks unused but is actually a shock absorber for older code. Noted so nobody rips it out. |
| 8 | Security posture (positive finding): no hardcoded secrets in `src/`, `agents/`, or `scripts/`; paid keys confined to GitHub Actions secrets and Supabase function secrets; the browser gets only the public anon key; X/Twitter ingest goes through RSSHub (self-hostable) rather than scraping or paid API; agent runs write JSON receipts to `.nfl/receipts/`. | codebase-wide sweep | security | — | Maintain. The apiConfig deprecation shims (`OPENAI_API_KEY = ''`) can be deleted once AgentChat imports are updated. | no change needed | We looked for the classic mistakes — passwords in code, secret keys reaching the public app — and found the project has already engineered them out properly. |
| 9 | Python utility scripts are clean: 10 files, one false-positive lint hit (Finding 7), no secrets, no vault writes. JS agents write vault *content* only to the Supabase `vault_notes` table — actual Obsidian file writes happen via the Local REST API plugin (localhost), not raw filesystem writes, so ATLAS's atomic-write discipline is not bypassed. | `scripts/`, `agents/` | architecture | — | None. | no change needed | The helper scripts are tidy, and — important given the family vault rules — this project doesn't write vault files directly; it goes through Obsidian's own local doorway, which is the polite way in. |

## Local-first & offline-autonomy readiness

Overall: **furthest from local-first of the three** — by deliberate design (free cloud tiers everywhere), which is fine today. The map:

| Dependency | Where | Behind abstraction? | Local alternative | Do now / defer | What breaks if it vanished (plain language) |
|---|---|---|---|---|---|
| GitHub Actions (scheduler) | 19 workflows | agents are plain `node` scripts ✓ | systemd timers on M6 | **Do now, cheap:** write the timer units (agents already env-driven); defer cutover | All automatic data updates stop; every agent still runs by hand from any machine. |
| GitHub Pages (hosting) | `deploy.yml` | standard Vite build ✓ | Caddy on M6 over Tailscale (Rosie already does exactly this) | Defer; copy Rosie's pattern when wanted | The public dashboard URL dies; `npm run build` + any web server revives it in minutes. |
| Supabase | odds/intel/grading tables, `vault_notes`, edge-function proxies | standard Postgres ✓ | self-hosted Supabase | **Do now, cheap:** nightly `pg_dump` (same task as Rosie's — one backup habit, two projects) | Live odds history, intel, and the AI proxy stop; data survives only if dumps exist. |
| OpenAI GPT-4o | pick extraction, transcript analysis (via `ai-proxy`) | ✅ proxied — one server-side call site | local model behind the same proxy URL | **Defer** models; the proxy already gives you the swap point | AI pick-extraction and podcast analysis pause; hand-entry still works. |
| Groq (Whisper) → AssemblyAI | podcast transcription fallback chain | ✅ priority chain already exists | whisper.cpp on M6 — realistic on current hardware | **Do now if desired** — local Whisper is one of the few local-AI swaps that's already practical | Podcast ingest pauses; local Whisper could take priority-1 slot today. |
| TheOddsAPI (500 req/mo free) | odds ingest agents | endpoint config centralized ✓ | none — odds are inherently external data | n/a — cache aggressively (already does, 10-min TTL) | Live odds stop; there is no "local" source for sportsbook lines — this is a true external dependency. |
| ESPN / nflverse | injuries, stats | nflverse is open data ✓ | nflverse files can be mirrored locally | Do now, trivial: keep the downloaded CSVs (already in repo/public) | Stats refresh stops; historical copies remain. |
| RSSHub (X ingest) | `x-sharp-ingest.js` | ✅ URL-configurable | self-hosted RSSHub instance | Defer; already swappable | Sharp-account tweet feed stops until pointed at another instance. |
| Obsidian Local REST API | vault reads (`obsidian-vault-sync`, `vaultClient.js`) | ✅ already local | — | already local ✓ | Nothing — this is your own machine. |

## Data ownership & vendor lock-in `[Data-Ownership]`

Formats are healthy everywhere: `public/*.json` in the repo (plain JSON), Supabase (open Postgres), vault notes (markdown), nflverse (open CSVs). The exposures are *locational*, not format: **GitHub** — repo, hosting, automation, and secrets all in one provider; if the account were locked tomorrow, code survives in local clones but the site, the robots, and their secrets all stop at once (plain language: eggs, basket). **Supabase** — same story and same fix as Rosie: a scheduled dump makes this migration-pain instead of data-loss. **Browser localStorage** — the one place *user-entered* data lives outside your control perimeter entirely (Finding 4). **TheOddsAPI/ESPN** — feeds, not custodians; nothing of yours is stored there. Identity: GitHub login and Supabase auth; no Google/Apple sign-in dependencies found.

## Evolution roadmap

1. **Add sensitivity-tier filtering to the vault→Supabase export** (high, small diff). *(Finding 1)* *Plain language: teach the copying machine to read privacy labels before it copies — and to treat unlabeled notes as private, per the house rule.*
2. **`[Data-Ownership]` Move critical-permanence localStorage keys into Supabase** with local cache (med). *(Finding 4)* *Plain language: your hand-entered picks deserve the same database the odds get.*
3. **Fix CLAUDE.md's env documentation** to match the proxy architecture (med, 10 minutes). *(Finding 3)* *Plain language: correct the manual before it teaches someone to re-open a fixed security hole.*
4. **`[Local-First]` Package the agents as systemd timers on M6** alongside their GHA twins (med — mostly packaging). *(Finding 2)* *Plain language: give the robots a bedroom at home, even if they keep working from GitHub's office most days.*
5. **`[Data-Ownership]` Nightly Supabase `pg_dump`** — combine with Rosie's backup job (do now, cheap). *Plain language: one backup habit covers both family projects.*
6. **`[Local-First]` Swap podcast transcription priority-1 to local whisper.cpp** — the rare local-AI move that's practical on today's hardware (optional, do when convenient). *Plain language: turning podcasts into text is the one AI job your current computer can already do itself, free.*
7. **Delete superseded v1 agents and the one-off test script; add the `VITE_BYPASS_AUTH` CI tripwire** (low). *(Findings 5–6)*

## DataFlow chart

**No structural changes were found** — no modules, agents, workflows, or data paths were added or removed by this audit (no code changes were made — see Change list). `E:\data\Obsidian\NFL_Dashboard_DataFlow.html` (last updated 2026-07-01, with a `.mermaid` source alongside) was **left untouched**, per the audit brief.

## Change list

**No code changes were made to NFL Dashboard.** The two mechanical candidates both failed the safety bar: the vulture hit is a false positive (Finding 7), and the stale-file deletions could not be performed reviewably in this environment (Finding 5). The CLAUDE.md doc fix, though within the "outdated comments" fix-directly category, was deliberately deferred because the available editing tool rewrites whole-file line endings (demonstrated in the ATLAS portion of this audit), which on a governance file would create a misleading diff; the exact replacement text is specified in Finding 3 for a 2-minute manual apply.
