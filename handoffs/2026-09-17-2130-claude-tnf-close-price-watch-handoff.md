# Handoff — 2026-09-17 — TNF Bug Fixes, Futures Team Grouping + Price Watch, TNF Tickets Closed & Synced to Bankroll

**Session type:** Claude (Cowork), via `mcp__remote-devices__device_bash` bridge to this Windows machine, live during and immediately after the Week 2 TNF game (DET @ BUF, final **BUF 41, DET 31**).

## What shipped this session (all uncommitted working-tree edits — see Git section below)

### 1. Game Day Tracker bug fixes (`scripts/generate-live-tracker.mjs`)
- **Double-tab launch bug**: `scripts/toolbox-app-server.mjs`'s `runTask()` was spawning a second OS-level browser open on top of the client's own tab-open logic for both `launch-dashboard` and `launch-gameday`/`launch-sunday`. Removed the redundant server-side `openBrowser()` calls; `/api/open-external` and the `--open` CLI flag (which legitimately need it) are untouched. **Andy needs to restart the toolbox server for this to take effect** (same class of issue as the 09-15 Human Review "Not Found" bug — stale long-running process).
- **Goff pass TDs / Bills team total reading 0**: two-layer root cause in `updateAllLegPacingGrades` and the two other copies of the same stat-matcher (`renderPlayerCheatSheetStats`'s own gauge loop, and its "direct update" loop for main ticket cards) — (a) the generic touchdown/`td` catch-all matched *before* the specific pass-TD branch across all 3 duplicated matchers, and (b) even after reordering, the real market string `passing_touchdowns` doesn't contain literal `"td"` so the specific branch's own condition missed it too. Fixed all 3 occurrences. Also added a dedicated `isTeamTotal` branch (previously nonexistent) so `team_total` legs get live pacing at all — SSR classification (`isPropLeg`/`isTotalLeg`) updated to match.
- **"Over 54" showing "On Pace" instead of "Hit" once already cleared**: `isTotal` (game total) live-pacing branch had no "already mathematically clinched" shortcut. Since NFL scores are non-decreasing within a game, once the live combined score crosses the line the outcome is locked — added that shortcut so it shows `✅ Hit`/`❌ Busted` immediately instead of a pace projection.
- **Bookmaker.eu SGP #738851934 "$70.02 payout looked wrong"**: turned out to be correct math, wrong label — `potential_payout_usd` (total return incl. stake) was being shown under "Payout" while Andy's own ticket/sportsbook convention is profit-only ("To Win"). Andy chose "relabel to Win/profit-only" via `AskUserQuestion`. Fixed at all 4 rendering sites (SSR bullet badge, SSR card body, `renderCustomCard()`, and the live `render()` recalc) as a display-time subtraction — `potential_payout_usd`/round-robin combo math itself is untouched, since other consumers depend on the total-return convention.
- **Player Cheat Sheet not following Ticketboard's own Hit logic** (cards with clearly-Hit legs not collapsing via "⚡ Collapse Fulfilled"): the Cheat Sheet's own `.sub-gauge-item` fulfillment loop had drifted out of sync with the two other copies of the same stat-matcher — it was missing the `pass_attempts` branch entirely (current value stuck at 0 forever for that market) and used the generic defensive `stats.int` instead of `stats.passInt` for `interceptions_thrown` legs. Both fixed to match the other two loops exactly. Also excluded open-slot placeholder legs from the fulfillment ratio (mirroring the Ticketboard's own "Hits" counter, which already excludes them) — an unfilled slot can no longer permanently block a card from collapsing.
- All fixes verified against real live ESPN data pulled mid-session (`curl` to `site.api.espn.com`), not assumed.

### 2. Futures Portfolio tab — grouped by team + new Price Watch (`scripts/generate-live-tracker.mjs`)
- Futures cards (Super Bowl, matchups, win totals, etc.) are now grouped into per-team collapsible `<details>` sections instead of one flat grid. A Super Bowl matchup exacta correctly appears under *both* teams it involves (real exposure to both sides). Applied to both the live and resolved futures sections.
- New **"📉 Price Watch"** section (monitor-only, not positions) tracking Buffalo Bills and Chicago Bears SB Win + all-opponent Exacta Matchup pricing, watching for a dip before Andy buys in. Reconstructs price history from the existing dated per-book snapshot files already in `data/futures-imports/` (`bookmaker-*.json`, `betonline-*.json`, `betus-*.json`) — no new data source. Per Andy's explicit answers: SB Win tracked against Bookmaker.eu + BetOnline equally; Exacta tracked against BetUS + Bookmaker.eu (BKR currently has zero exacta rows in the local snapshot files, so BetUS is the effective source until that changes); all ~15 opponent pairings tracked per team, sorted biggest-mover-first; "dip" = % drop in implied probability since first tracked, default 25% threshold, flips a "📩 Dip Alert" badge. Config lives in the new `data/futures-imports/price-watch-list-2026.json` — add/remove teams or change threshold there, no code change needed. Refreshes on every tracker regen, same as the rest of the board (no new automation/cron added).

### 3. TNF tickets closed out and synced to Bankroll
Final: **BUF 41, DET 31** (full boxscore pulled from ESPN and used to grade every leg by hand, not guessed):
- 5 multi-leg prop/spread parlays **LOST** (each missed 1–3 legs — despite most parlays hitting 5–7 of their legs, 14 of the 18 unique player-prop legs across the whole TNF card hit clean). The misses were razor-thin, not blowouts: Jared Goff's 0 INT (a clean game, needed 1+), Khalil Shakir missing his receiving-yards line by 6.5 yards (38 vs. 44.5) and his receptions line by exactly 1 catch (3 vs. 4 needed), and Jameson Williams going scoreless (needed 1+ TD, got 0).
- Bookmaker.eu SGP #738851934 (BUF -5 & Over 54) **WON both legs** — $20.12 → $70.02.
- The standalone DET +5.5 freebet **LOST** (Lions lost by 10, didn't cover).
- The 11-team ML/spread parlay (`bet_20260917_738825741_bkr_11team_ml_parlay`) has 10 more legs on Sunday/Monday games — only its BUF ML leg (leg 1) was graded WON; the ticket itself correctly stays PENDING as a whole.
- The Bills-to-win-SB future was left untouched (season-long, unrelated to tonight).
- **Net P/L on tonight's fully-settled TNF tickets: +$17.98** ($52.04 risked across 6 settled tickets, $70.02 returned) — Andy is in the black for the night.
- All grading written directly into `data/official-picks/user-placed-wagers-2026.json` (`status: SETTLED`, `result: win/loss`, `graded_at`, per-leg `status`/`actual_stat`).
- Ran `node scripts/sync-placed-wagers-to-bankroll.mjs` (real run, not dry-run) — synced all 35 wagers to Supabase `user_bankroll_bets` and copied the updated JSON to `public/user-placed-wagers-2026.json` for client hydration. This is the standing "Supabase writes need per-change authorization" constraint being satisfied by Andy's own explicit "send them to Bankroll for processing" instruction this session.
- Regenerated `public/live-tracker-sunday.html` and `docs/tracked-wagers/live-tracker-sunday.html` (`node scripts/generate-live-tracker.mjs --week 2`) so the Ticketboard reflects the settled tickets.

## Open items / next session should start with
1. **Andy needs to restart the toolbox server** for the double-tab-launch fix to take effect (stale long-running `node.exe` process, same pattern as the 09-15 Human Review bug).
2. **Kick off the Friday intel cadence for the Sunday/Monday Week 2 slate** — this is the next natural pipeline step now that TNF is closed out:
   `node scripts/toolbox.mjs --cadence friday`
   which runs, in order: final injury/practice-status pull (`build-player-availability.js --live-injuries`), projected starters (`build-projected-starters.js`), secondary matchup vulnerability matrix (`build-secondary-matchup-vulnerability.js`), Sunday SGP models/curated parlays (`build-player-props-intel.js`), and the Alpha Data Packet compile (`build-alpha-data-packet.js`). This was intentionally **not run this session** — Andy asked to "prepare for the next session to start processing intel," not to run the paid/intel pipeline tonight.
3. **No paid synthesis was run and no authorization was requested this session** — if Sunday/Monday intel processing is expected to include an `agents/portfolio-synthesize.js` run, that still needs Andy's own explicit per-run authorization per the standing constraint below.
4. Nothing was committed or pushed this session — working tree still has the large pre-existing dirty state noted in the 09-15 handoff, plus tonight's edits on top. Git guardrail is repealed (normal judgment applies) but no commit was made — still needs the deliberate triage pass called out in the 09-15 handoff before a broad commit.

Standing constraints (unchanged): Supabase writes need per-change authorization (satisfied tonight by Andy's explicit "send to Bankroll" instruction — do not treat this as blanket future authorization); no paid synthesis runs without authorization.
