# Claude Session Handoff — Frontier Portfolio Synthesis Initial Run (rev 4 REJECTED by Codex, 3rd round)

**Created:** 2026-09-11 18:40 PDT (2026-09-12 01:40 UTC)
**Author:** Claude (Cowork session, this thread)
**Target:** Claude (fresh session), Andy
**Branch:** `main` (HEAD `8deab48`, 3 commits ahead of cached `origin/main` `103d3ef`, not pushed)
**Status:** NOTHING HAS RUN. No code written. No commits. No paid model calls. No Supabase writes. Working tree has a large pre-existing dirty state from other sessions (Antigravity/Codex) — preserved, not touched by this thread.

---

## Standing rules for this project (read this before doing anything)

1. **Codex reviews every plan before any commit, stage, write, or paid model run.** Andy relays Codex's reviews verbatim into chat.
2. **Never accept a Codex finding on faith — independently verify every single one against live code/data before acting on it or reporting it to Andy as confirmed.** This has been the pattern the whole engagement, and it has caught real things both ways (Codex has been right on nearly everything so far; I have also caught my own errors this way, and once found a discrepancy in Codex's own numbers worth flagging rather than silently accepting).
3. **No paid `portfolio-synthesize.js` call, no `signal-normalize.js` run, and no live Supabase writes happen without Codex's sign-off AND Andy's separate, explicit authorization.**
4. Report findings to Andy honestly, including your own mistakes — this collaboration has an established norm of "here's what I got wrong" reporting, not just "here's what Codex found."

## What this thread is trying to do

Andy has a hard deadline: get Win Totals + Playoffs recommendations for all 32 NFL teams out via `agents/portfolio-synthesize.js` before the rest of Week 1 kicks off, using a **frozen pre-kickoff snapshot** — explicitly NOT live/actionable for the 4 teams that have already played (LAR/SEA/SF/NE), but not excluding them either (Andy's words: "this is supposed to be a pre-week 1 opening portfolio synthesis"). A second pass next week will use real Week 1 data and updated market conditions.

## Spec document (single source of truth, keep updating this)

Artifact URL (Codex reads/reviews this): `https://claude.ai/code/artifact/06a38c9b-a559-488c-8b32-abcaaa59473f` ("Frontier Synthesis Initial Run"). Currently at rev 4 / Artifact version 5. **Rev 4 was just REJECTED by Codex's third review round** (this session ran out of room to write rev 5 — that's the next session's first job).

## History (so you don't repeat mistakes already found)

- **Rev 1** (my original plan): REJECTED. 5 P1 + 4 P2 findings, ALL independently verified true, including my own error (wrongly told Andy prior-season analytics needed a code fix — it doesn't, already exists via `is_current_season`/`seasons_behind`/`staleness_note`).
- **Rev 2**: surfaced that the intel-source-integrity audit's BLOCKED entry says verbatim "Do not run frontier synthesis" — a genuine hard-stop self-flag from the tool, not a staleness caveat.
- **Rev 3**: fixed a scope bug (had wrongly excluded LAR/SEA/SF/NE entirely — Andy corrected this, all 32 teams stay in for Wins/Playoffs). Read past the intel-audit's summary counts into the actual flagged articles before asking Andy to decide; Andy explicitly overrode the block (`ALLOW_BLOCKED_INTEL`) as an informed accepted-risk call. Still REJECTED — 7 more P1s from Codex's second round, all independently verified true (default `--watchlist`/`--promotions` inject SB/division/conference content regardless of scope; `prediction_markets` is hardcoded into `board-validate.js`'s `TIER1_FIELD_ROOTS`, a genuine code-level contradiction with the audit's context-only rating of that source; the 4 played teams' Wins/Playoffs quotes are all pre-kickoff timestamps flagged "current" only because the freshness check is purely time-based; etc).
- **Andy's architecture decision**: presented the live-vs-frozen fork, Andy chose **frozen pre-kickoff snapshot** for all 32 teams.
- **Rev 4**: incorporated the frozen-snapshot decision + all of round-2's fixes (strip `prediction_markets` from the scoped dossier; empty watchlist/promotions overrides; explicit `--dossier` to preflight; unique output paths; expanded run-instructions). Chose `.nfl/portfolio/dossier-2026-09-10.json` as the "frozen" dossier and proposed sim-patching it.
- **Andy's Kalshi follow-up question** (worth reading if you need context on why `prediction_markets` gets stripped): Andy asked why Kalshi data being cheaper isn't actionable. Verified: `team_profiles.*.prediction_markets` only carries implied PROBABILITY fields (`playoff_prob`, `market_implied_win_total`), never an actual contract price/ticker, and Kalshi isn't in the placeable-books list the wins/playoffs price-shopping logic checks — so there's currently NO code path for the model to recommend an actual Kalshi trade, only to use Kalshi's implied probability as corroboration for a SPORTSBOOK bet. Andy asked to scope real Kalshi-as-execution-venue support as a **follow-up task for next week**, not this run. Recorded in project memory (`nfl_dashboard_pipeline_blockers.md`, follow-up #7).

## Rev 4's rejection — Codex's third review (just received, NOT yet incorporated into a rev 5)

Verdict: **"Changes requested — do not proceed to the paid run."** I independently verified the load-bearing claims below before writing this handoff (do NOT re-trust these blindly either when you pick this up — but they check out):

1. **P1 — `.nfl/portfolio/dossier-2026-09-10.json` is NOT pre-kickoff.** Confirmed directly: `meta.generated_at: "2026-09-10T22:16:47.607Z"`, kickoff was `2026-09-10T00:20:00Z` — **~22 hours AFTER kickoff, not before.** I picked this dossier in rev 4 because it was the most recent one, without checking its generation time against kickoff — a real mistake. **The correct pre-kickoff dossier is `.nfl/portfolio/dossier-2026-09-09.json`** (`meta.generated_at: "2026-09-09T14:38:29.650Z"` — genuinely pre-kickoff). Also confirmed: `dossier-2026-09-09.json` already has `meta.sim_version: "coherence-v1"` — **it's already sim-patched, so the sim-patch step in rev 4's plan is unnecessary** for this dossier.
2. **P1 — Rev 4's proposed command is missing 3 required override flags and would exit before any paid call.** Confirmed by reading the code: `ALLOW_BLOCKED_INTEL`, `ALLOW_STALE_DOSSIER`, and `ALLOW_EXPIRED_EVIDENCE_LANES` are three SEPARATE flags from `ALLOW_UNSAFE_PREFLIGHT`, each gating its own `process.exit(1)` earlier in the script (before the full-preflight-gate block that `--allow-unsafe-preflight` covers). Rev 4's proposed command only included `--allow-unsafe-preflight` — it would have hard-exited at the intel-audit or freshness-preflight check, before spending a cent. Also correctly caught: rev 4's own text still listed 3 dispositions (`named_status_review`, `odds_execution_validation`, `youtube_freshness`) as "Andy's call still open" — meaning even if the flags had been added, they weren't yet actually authorized.
3. **P1 — Run-instructions cannot reliably suppress `correlated_week1`/hedge/parlay/scenario sections.** Confirmed by reading the SYSTEM_PROMPT/user-prompt template directly: the user-prompt-building code (`buildUserPrompt()`, near the end of the prompt) contains a hardcoded, unconditional instruction: "...build hedges where correlation lets you lock value or cut variance...propose hedge_baskets/parlay_ladders plus portfolio_strategy where the dossier genuinely supports a playoff scenario book." This is baked into the template at a structurally higher priority than a run-instructions file (which just gets prepended/injected as one more block of text) — a lower-priority instruction telling the model NOT to do this is not a reliable mechanism. Codex's proposed fix: either a scoped code mode (a flag that actually removes this instruction from the template) or a fail-closed POST-generation rejection (check the model's JSON output for non-empty `correlated_week1`/`hedge_baskets`/`parlay_ladders`/`scenario_review` and reject/regenerate if present) — NOT just hoping the instruction wins out.
4. **P2 — The internal preflight spawn doesn't forward `--dossier`.** Confirmed: `portfolio-synthesize.js`'s own internal call to `agents/portfolio-preflight.js` (`spawnSync('node', ['agents/portfolio-preflight.js', '--json', '--warn-only'], ...)`) does NOT pass `--dossier`, so it auto-discovers whichever dossier sorts last by filename — not necessarily the scoped file being used for the actual synthesis call. `--allow-unsafe-preflight` bypasses the resulting BLOCK, it doesn't fix the mismatch (the internal preflight is checking the wrong file, or the wrong watchlist/promotions, and you're just not looking at what it says).
5. **P2 — More freshness drift on the CORRECT (9/9) dossier than rev 4 recorded for the 9/10 one.** Codex found 3 stale lanes (`articles`, `availability`, `projected_starters`) plus 5 expired lanes when reconciling against the actual 9/9 dossier — rev 4's "2 stale lanes" figure was computed against the wrong (9/10) dossier. **Next session needs to re-run the freshness-gate check against dossier-2026-09-09.json specifically**, not reuse rev 4's numbers.
6. **P2 — The scoper script (still unwritten) needs fail-closed invariants**, spelled out by Codex: distinct input/output paths (already planned), preserve source hash/provenance, assert exactly 32 unique teams present in both retained markets, reject unexpected market keys or missing rows, confirm zero `prediction_markets` fields survive, and never mutate the source dossier file.

**Codex's direct answers** (paraphrased, see the full verbatim message in this conversation's transcript around this handoff if you need exact wording):
1. Use `dossier-2026-09-09.json`, not `dossier-2026-09-10.json`. It's already sim-patched — don't re-run `portfolio-simulate.js` against it. If simulation is EVER rerun for any reason, it must write to a derived copy — `portfolio-simulate.js` defaults to `PATCH_DOSSIER = !argv.includes('--no-patch-dossier')`, which I confirmed **overwrites the input dossier file in place by default**. Never run it without either `--no-patch-dossier` or a copied input path, or you will silently destroy the pristine 9/9 snapshot.
2. `{"items":[]}` and `{"promotions":[]}` are confirmed correct/minimal shapes for the watchlist/promotions override files — no dedicated `--no-watchlist`/`--no-promotions` flag exists.
3. Rev 4 (paraphrased): still missing the override flags, a real mechanical scenario-lane prohibition, exact internal-preflight reconciliation, and fail-closed scoper assertions.
4. Yes — step 8's dispositions are genuinely incomplete (3 items left "Andy's call open"), which alone is enough to block approval regardless of everything else.

**Unrelated git/Phase-4 note from Codex** (informational only, not part of this thread's blocking issues): confirms `8deab48` (BettorDay retirement) is the current branch tip and correctly closes the earlier clean-checkout P1; notes the Phase 4/Site 5 (`nfl_trench_ratings`) migration proposal is now superseded since BettorDay's loader/writer are gone, though the remote workflow is still live since `8deab48` isn't pushed. No action needed on this from the frontier-synthesis thread specifically.

## What rev 5 needs to do (next session's task list)

1. **Switch the frozen dossier to `dossier-2026-09-09.json`.** Update every reference in the spec. Do NOT re-run `portfolio-simulate.js` against it (already `sim_version: coherence-v1`) — drop that step from the plan entirely, or turn it into a defensive check-only step ("confirm sim_version is present, do nothing if so").
2. **Re-run the dossier-freshness-gate check against `dossier-2026-09-09.json` specifically** (not 9/10) and update the step-8 lane dispositions with real numbers — Codex says 3 stale + 5 expired, verify this yourself rather than copying their figure.
3. **Fix the run command to include all four override flags** (`--allow-blocked-intel`, `--allow-stale-dossier`, `--allow-expired-evidence-lanes`, `--allow-unsafe-preflight`) — but only after Andy has actually, explicitly authorized every gate each flag bypasses. Rev 4 left 3 dispositions open ("Andy's call") — go back and get real answers before adding the flags, don't just add them because they're technically required to avoid an exit code.
4. **Solve the scenario-lane suppression problem for real**, per Codex's two suggested approaches: either (a) find/add a flag or code path that actually removes the hedge/parlay/scenario instruction from the prompt template for this run, or (b) build a post-generation fail-closed check that inspects the model's JSON output for non-empty `correlated_week1`/`hedge_baskets`/`parlay_ladders`/`scenario_review` and rejects the run (or strips those sections) rather than trusting the model to comply with a lower-priority instruction. This is the single most structurally important open item — a run-instructions-only fix has now been rejected twice.
5. **Forward `--dossier <scoped path>` to the INTERNAL preflight spawn inside `portfolio-synthesize.js` too**, not just the standalone explicit preflight check — this may require a small, Codex-reviewed code change to `portfolio-synthesize.js` itself (passing the CLI's own `--dossier` value through to the internal `spawnSync` call), or confirm whether an existing flag/env var already does this before assuming a code change is needed.
6. **Write `scripts/scope-synthesis-dossier.js` with Codex's fail-closed invariants baked in**: explicit `--in`/`--out` args, source-hash/provenance preserved in output metadata, hard assertion of exactly 32 unique teams in both `wins` and `playoffs` arrays, hard rejection of any unexpected market key or missing team row, hard assertion that zero `prediction_markets` fields survive anywhere in `team_profiles`, and the script must never write to its own `--in` path (a distinct-paths check, not just a convention).
7. Re-verify the empty watchlist/promotions file shapes (`{"items":[]}` / `{"promotions":[]}`) against the loader code yourself before writing them — Codex confirmed them but the standing rule applies to Codex's confirmations too, not just rejections.
8. Republish the corrected rev 5 to the SAME Artifact URL (`https://claude.ai/code/artifact/06a38c9b-a559-488c-8b32-abcaaa59473f`) and update `nfl_dashboard_pipeline_blockers.md` project memory the same way prior revisions were tracked.
9. Only after Codex approves rev 5 AND Andy gives separate, explicit authorization does any paid call happen.

## Where to find full context

- Project memory: `nfl_dashboard_pipeline_blockers.md` (via the project-memory tool) has the full rev 1→4 history, all lane dispositions, and the Kalshi follow-up — read this first, it's more complete than this handoff's summary.
- This conversation's own transcript has the full verbatim Codex reviews if you need exact wording beyond what's paraphrased above.
- Repo: `E:\dev\projects\NFL_Dashboard` (reached via the device bridge, mounted at `$HOME/mnt/dev/projects/NFL_Dashboard` in `device_bash`).
