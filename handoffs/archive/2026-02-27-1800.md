# Handoff — 2026-02-27 18:00
Session: ~2h | Model: Claude Sonnet 4.6

## CRITICAL (mid-flight / broken / blocking)
- None — app is stable, all features working, dev server runs on port 5173 (or 5174 if 5173 occupied).

---

## DONE THIS SESSION

### OddsCenter Tab Badges — Dynamic (this session)
- **File**: `src/components/odds/OddsCenter.jsx`
- Replaced hardcoded `badge: '3'` (Arbitrage) and `badge: '5'` (Steam Moves) with dynamic counts computed on mount via `useEffect`
- **Arbitrage badge**: reads `localStorage('cached_odds_data')` → `findArbitrageOpportunities(games)`; falls back to `findArbitrageOpportunities(generateMockMultiBookData())` — mirrors exact logic of `ArbitrageFinder.loadOpportunities()`
- **Steam Moves badge**: calls `getLineMovements(24).length` — mirrors exact logic of `SteamMoveTracker.load()`
- Badge hides entirely (renders `null`) when count is 0
- Added imports: `findArbitrageOpportunities`, `generateMockMultiBookData`, `getLineMovements` from `../../lib/enhancedOddsApi`
- Added `useEffect` import to React, `arbBadge` and `steamBadge` state

### Prior sessions (already committed or staged — summarised for context)
- **Node.js v24.14.0** — installed via winget; PowerShell execution policy set to RemoteSigned for CurrentUser
- **Header WEEK 17 hardcode** → `getNFLWeekInfo()` in `src/lib/constants.js`; dynamic week label (WILD CARD, DIVISIONAL, CONFERENCE, SUPER BOWL, OFFSEASON)
- **BankrollSettingsModal.jsx** — NEW: full bankroll/unit-size/risk config modal wired into App.jsx
- **useModals.js** — added `bankrollSettings` to initialState
- **Bankroll button / MatchupWizardModal bug** — introduced `betEntryGame` state separate from `selectedGame` to prevent wizard firing on bankroll actions
- **MatchupWizardModal X button z-index** — fixed: `z-20`, solid `bg-slate-800`, `shadow-lg`, `pr-10`
- **ArbitrageFinder.jsx** — NEW full component replacing "Coming Soon" stub
- **SteamMoveTracker.jsx** — NEW full component replacing "Coming Soon" stub
- **OddsCenter.jsx** — removed inline stubs, added real component imports (prior session)
- **Analytics suite** — `analyticsEngine.js`, `analyticsFormatters.js`, `BetTypeBreakdown.jsx`, `BettingPatterns.jsx`, `EdgeAnalysis.jsx`, `KeyMetricsGrid.jsx`, `RiskAnalysis.jsx`, `TeamPerformance.jsx`, `TrendChart.jsx` all new
- **Hooks** — `useBettingCard.js`, `useExperts.js`, `useModals.js`, `useSchedule.js` extracted to `src/hooks/`
- **Lib** — `apiConfig.js`, `openai.js`, `storage.js` added
- **Large cleanup** — deleted NCAA files, stale modals, old lib files, test scripts, audit docs

---

## PENDING
- None required — app is feature-complete for current scope

## POTENTIAL NEXT STEPS (not committed to)
- Re-trigger OddsCenter badge refresh when user clicks "Sync" on LiveOddsDashboard (currently only runs on OddsCenter mount). Could lift state or use a custom event / zustand.
- Add `localStorage` change listener in OddsCenter so badge updates live when `cached_odds_data` is written.
- Grade pending picks in PicksTracker against fetched scores.

---

## BLOCKERS
- None

## OPEN DECISIONS
- None

---

## GOTCHAS DISCOVERED (carry forward)
- `selectedGame` triggers MatchupWizardModal (`{selectedGame && <MatchupWizardModal />}`) — any code setting `selectedGame` for another purpose opens the wizard. Always use a dedicated state var.
- Background terminals don't inherit PATH. Must prepend `$env:PATH = [Machine PATH] + [User PATH]` to every `isBackground: true` command.
- TheOddsAPI free plan: 500 req/month. Auto-refresh and startup fetch are DISABLED. Only fetches on explicit user click.
- Vite base path is `/platinum-rose-app/` — never use hardcoded `/filename.json` for public files; use relative `./filename.json`.

---

## RESUME COMMAND
Read `CLAUDE.md` for project context. Node.js is on PATH — start the dev server with:
```
$env:PATH = [Environment]::GetEnvironmentVariable("PATH","Machine") + ";" + [Environment]::GetEnvironmentVariable("PATH","User"); npx vite --host
```
from `D:\Platinum_Rose\NFL_Platinum_Rose`. Then open http://localhost:5173/platinum-rose-app/.

All current features are complete and stable. Pick up from PENDING / POTENTIAL NEXT STEPS above.
