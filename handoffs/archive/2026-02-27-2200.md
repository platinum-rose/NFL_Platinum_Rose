# Handoff — 2026-02-27 22:00
Session: ~2h | Model: Claude Sonnet 4.6

## CRITICAL (mid-flight / broken / blocking)
- None. All features shipped clean, no console errors, all green on dry-runs.

## DONE
- `agents/nfl-auto-grade.js` — ESPN scoreboard poller; upserts `game_results` to Supabase; idempotent by espn_id
- `.github/workflows/nfl-auto-grade.yml` — hourly Sun/Thu/Mon, every 4h otherwise; dry-run trigger
- `.github/workflows/odds-ingest.yml` — changed cron to once daily (offseason futures); `0 12 * * *`
- `src/hooks/useAutoGrade.js` — polls PENDING AI Lab + PENDING expert picks; single Supabase query; 5-min interval while pending exist
- `src/lib/supabase.js` — added `getGameResultsByIds()`, `getLineHistoryDB()`, `getActiveGameKeys()`
- `src/components/odds/LineHistoryChart.jsx` — recharts stepAfter multi-book line chart; spread/total/ML tabs; opening/current/net-move summary; demo Eagles@Chiefs fallback; book color badges
- `src/components/odds/OddsCenter.jsx` — 6th "Line History" tab added
- `src/lib/expertStats.js` — grading engine + `computeExpertStandings()` + `getAllExpertPicks()` + `gradeExpertPicksFromResults()`
- `src/components/dashboard/ExpertLeaderboard.jsx` — replaces static Standings; live W-L-P/win%/units; expandable pick rows; filter/sort; empty state
- `src/hooks/useExperts.js` — `handleConfirmPicks` now stores `id`, `result: 'PENDING'`, `isHomeTeam`, `home`, `visitor`, `gameDate`, `addedAt` on every confirmed pick
- `src/App.jsx` — Standings tab now renders `<ExpertLeaderboard>`; `refreshKey` wired with `autoGraded` counter
- `CLAUDE.md` — Line History chart test plan (11 items) added; Priority 2 items 5+6 marked done; agent table updated

## PENDING
- **Priority 2 #7** — Bet outcome tracking dashboard
- **Priority 2 #8** — Advanced filtering & sorting
- **Priority 2 #9** — Performance analytics (ROI by sportsbook, timing analysis)
- `INITIAL_EXPERTS` still imported in App.jsx (no longer used for Standings) — minor cleanup opportunity

## BLOCKERS (waiting on external)
- Supabase SQL schema (001_init.sql) — **confirmed run by user this session** ✅
- GH Actions dry-runs for both Odds Ingest + NFL Auto Grade — **both green** ✅
- Real Supabase line movement data: won't exist until odds-ingest agent accumulates a few days of snapshots (runs daily at noon UTC)

## OPEN DECISIONS (need user input)
- When to switch odds-ingest back to `0 */4 * * *` — currently set for August when weekly lines post
- ExpertLeaderboard: should pick detail rows show moneyline value or just spread/total line?
- `INITIAL_EXPERTS` import in App.jsx is now unused — safe to remove?

## GOTCHAS DISCOVERED
- `handleConfirmPicks` dep array must include `schedule` — needed for game lookup to enrich picks with `home`/`visitor`/`gameDate`
- `recharts` `<Line type="stepAfter">` is correct for odds data (not `"monotone"`) — lines should jump, not curve
- Expert pick `gameId` matches `espn_id` in `game_results` table — both come from ESPN; no translation needed

## RESUME COMMAND
Resume NFL Platinum Rose dashboard. Stack: React + Vite + Tailwind + Supabase + recharts. Workspace: E:\dev\projects\NFL_Dashboard. Dev URL: http://localhost:5173/platinum-rose-app/. Last commit: `6509d8f` — expert picks accuracy tracking. Priority 2 items 5+6 are done; next is #7 Bet outcome tracking dashboard (pops from Bankroll `nfl_bankroll_data_v1` + picks `pr_picks_v1`) or #8 Advanced filtering. CLAUDE.md in repo root has full context.
