# Handoff — 2026-02-27 20:03
Session: ~3h | Model: Claude Sonnet 4.6

## CRITICAL (mid-flight / broken / blocking)
- **Run SQL schema in Supabase** — `supabase/migrations/001_init.sql` has NOT been run yet. Tables (`odds_snapshots`, `line_movements`, `game_results`) do not exist. Agent will crash on first real run until this is done.
- **GitHub Actions dry-run not yet triggered** — secrets are set but workflow hasn't been manually tested. Do a dry-run first (Actions → Odds Ingest → Run workflow → check "Dry run") before enabling real cron.

## DONE
- `src/components/odds/BetValueComparison.jsx` — new component, compares `nfl_my_bets` vs `cached_odds_data`, per-bet "beat the close" delta, beat/behind/no-move summary stats
- `src/components/odds/LineMovementTracker.jsx` — refactored from hardcoded mock to real `getLineMovements()` + `getLineMovementsDB()` (Supabase primary, localStorage fallback); `buildAlerts()` generates personalized bet-aware alerts
- `src/components/odds/SteamMoveTracker.jsx` — updated load() to try Supabase first via `getLineMovementsDB()`
- `src/components/odds/OddsCenter.jsx` — added 5th "Bet Value" tab wired to BetValueComparison
- `src/lib/supabase.js` — new browser Supabase client (anon key, read-only); `getLatestOddsSnapshot()`, `getLineMovementsDB()`, `getGameResults()`
- `agents/odds-ingest.js` — OddsIngestAgent: fetches TheOddsAPI → diffs against last snapshot → writes odds_snapshots + line_movements to Supabase → 7-day TTL cleanup
- `.github/workflows/odds-ingest.yml` — GitHub Actions cron every 4h; manual dry_run trigger supported; uses SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, ODDS_API_KEY secrets
- `supabase/migrations/001_init.sql` — schema for odds_snapshots, line_movements, game_results; RLS read-only for anon key
- `src/lib/apiConfig.js` — added SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE constants
- `.env` — VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY added
- `CLAUDE.md` — updated env vars section, API integrations table, agent status table, localStorage key clarifications, Supabase client notes
- `@supabase/supabase-js` — installed (npm)
- Priority 1 COMPLETE — all 4 items done (Arbitrage Finder, Steam Move Tracker, Bet Value Comparison, Line Movement Alerts)
- 3 GitHub Actions secrets confirmed set by user (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, ODDS_API_KEY)

## PENDING
- **Run Supabase SQL schema** (`supabase/migrations/001_init.sql`) — paste into Supabase Dashboard → SQL Editor → Run
- **Test dry-run of odds-ingest workflow** — Actions tab → Odds Ingest → Run workflow → enable dry_run first
- **First real ingest run** — after dry-run passes, trigger without dry_run to populate first `odds_snapshots` row
- **NFLAutoGradeAgent** — next HIGH priority agent; polls ESPN NFL scoreboard, grades `pr_picks_v1` picks automatically, writes to `game_results` table
- Priority 2 enhancements: historical line charts, expert picks accuracy tracking, advanced filtering, performance analytics (ROI by sportsbook)
- Priority 3: testing suite (Jest + Cypress)

## BLOCKERS (waiting on external)
- Supabase SQL schema must be run manually before agent data can flow into React

## OPEN DECISIONS (need user input)
- **ESPN scoreboard polling frequency** for NFLAutoGradeAgent — during games: every 5 min? after games: once? Schedule to define.
- **Offseason cron** — should odds-ingest.yml pause during offseason (Apr-Jul) to conserve the 500 req/month budget?

## GOTCHAS DISCOVERED
- `Add-Content` in PowerShell wraps long JWT strings with line breaks — always write `.env` values via Python `open(path, 'w')` or directly in editor, never via shell append for long tokens
- GitHub Actions YAML linter flags `secrets.XXX` as "might be invalid" when secrets don't exist yet — this is expected and won't block the workflow after secrets are added
- `.env` terminal display wraps long keys but file is correct — verify with `python -c "open('.env').read()"` not PowerShell `Get-Content`

## DATA FLOW (for orientation)
```
GitHub Actions (cron 4h)
  └── agents/odds-ingest.js
        ├── TheOddsAPI → processGame() → games[]
        ├── Supabase: read last odds_snapshots → detect line movements
        ├── Supabase: INSERT odds_snapshots (games JSONB)
        └── Supabase: INSERT line_movements (one row per detected change)

React browser
  └── LiveOddsDashboard → getLatestOddsSnapshot() → Supabase odds_snapshots
  └── SteamMoveTracker  → getLineMovementsDB(24)  → Supabase line_movements
  └── LineMovementTracker → getLineMovementsDB(hours) → Supabase line_movements
  (all fall back to localStorage → API if Supabase unavailable)
```

## RESUME COMMAND
Resume NFL Platinum Rose dashboard. Stack: React + Vite + Tailwind + Supabase. Workspace: `E:\dev\projects\NFL_Dashboard`. Dev URL: http://localhost:5173/platinum-rose-app/. Repo: https://github.com/andrewlrose/NFL_Platinum_Rose. **IMMEDIATE ACTION REQUIRED**: Run `supabase/migrations/001_init.sql` in Supabase Dashboard SQL editor (project: aambmuzfcojxqvbzhngp), then trigger a dry-run of the "Odds Ingest" GitHub Action. Next agent to build: NFLAutoGradeAgent (polls ESPN scoreboard → grades `pr_picks_v1` picks → writes to Supabase `game_results`). CLAUDE.md is in repo root with full project context.
