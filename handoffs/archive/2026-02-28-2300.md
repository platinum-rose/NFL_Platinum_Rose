# Handoff — 2026-02-28 23:00
Session: ~3h | Model: Claude Sonnet 4.6

## CRITICAL
- None. Build is green. Deploy is live.

## DONE
- `supabase/migrations/003_podcast.sql` — Creates podcast_feeds / podcast_episodes / podcast_transcripts tables; seeds 4 RSS feeds (Sharp or Square, Even Money, Action Network, Warren Sharp)
- `agents/podcast-ingest.js` — Full PodcastIngestAgent: RSS poll → audio download (24MB cap + partial fallback) → Groq/Whisper transcription → GPT-4o pick+intel extraction → Supabase write; dry_run + max_per_run env flags; dedup by RSS guid; exponential backoff retries
- `.github/workflows/podcast-ingest.yml` — GHA cron every 6h; manual trigger with dry_run + max_per_run inputs; GROQ_API_KEY optional secret
- `.github/workflows/deploy.yml` — Fixed: added VITE_ env secrets to build step so API keys are baked into production bundle
- `src/components/modals/PodcastIngestModal.jsx` — Browse processed episodes grouped by show; expand picks + intel; one-click import to Picks Tracker; refresh; empty state; partial audio badge
- `src/lib/supabase.js` — Added getPodcastEpisodes() with 3-table join (episodes + feeds + transcripts)
- `src/App.jsx` — podcastModalOpen state; PodcastIngestModal render; onOpenPodcastModal prop to PicksTracker
- `src/components/picks-tracker/PicksTracker.jsx` — "Podcast Intel" teal button in header; Mic icon added
- `src/lib/picksDatabase.js` — Exports summary updated; build fix: PodcastIngestModal now imports addPick directly (not addExpertPick) to resolve Rollup static analysis error
- `docs/AGENTS.md` — PodcastIngestAgent moved to Built; OPENAI_API_KEY + GROQ_API_KEY secrets documented; 3 new Supabase table rows
- `docs/TESTING.md` — 18-case Podcast Ingest test plan added (agent + UI + data layer sections); core smoke test updated
- `docs/ROADMAP.md` — PodcastIngestAgent marked ✅ Built in agent table
- VS Code settings.json advice — fixed unquoted GEMINI_API_KEY JSON error; added alwaysAllowedTools + runTasks always

## PENDING
- **Podcast dry run test** — user has not yet run dry_run=true workflow to confirm RSS fetching; Supabase migrations ran green but agent has not been tested end-to-end
- **Groq account** — user had Hotmail signup issue at console.groq.com; workaround: try Google OAuth in incognito or email support@groq.com. GROQ_API_KEY secret not yet added to GitHub. Agent falls back to OpenAI Whisper without it.
- **Deploy secrets** — 4 VITE_ secrets need to be added to GitHub Actions secrets for production build to have working API keys: VITE_ODDS_API_KEY, VITE_OPENAI_API_KEY, VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
- **GitHub Pages source** — verify Settings → Pages → Source = "GitHub Actions" (not gh-pages branch)

## BLOCKERS (waiting on external)
- Groq account creation blocked by Hotmail restriction; resolve via Google OAuth or support email

## OPEN DECISIONS (need user input)
- AssemblyAI as fallback transcription provider if Groq remains inaccessible ($50 free credit, ~500h audio)
- max_per_run value for production: default is 3 episodes/run; recommend 1 during NFL offseason to minimize OpenAI costs

## GOTCHAS DISCOVERED
- **Rollup static analysis on `const` exports**: `export const addExpertPick` existed in picksDatabase.js and worked in dev, but Rollup's production build flagged it as not-exported when imported by PodcastIngestModal.jsx. Root cause likely: the function was added mid-session and the module's static export surface wasn't updated. Fix: import the underlying `addPick` directly; always verify Rollup resolves imports with a local `npm run build` before pushing.
- **Supabase schema cache**: after running DDL migrations, PostgREST caches the schema and won't see new tables for several minutes. Fix: Dashboard → Settings → API → Reload schema cache, or wait 5 minutes.
- **Wrong Supabase project**: user has multiple Supabase projects; migration was accidentally run in the wrong one. Always confirm the project URL matches VITE_SUPABASE_URL in .env (aambmuzfcojxqvbzhngp).
- **Sensitive keys in chat**: .env file was attached to a message, exposing VITE_OPENAI_API_KEY, VITE_ODDS_API_KEY, GEMINI_API_KEY, and VITE_SUPABASE_ANON_KEY in plaintext. All should be rotated.

## RESUME COMMAND
Continue NFL Platinum Rose (NFL_Dashboard). Session complete: PodcastIngestAgent pipeline built (RSS→Groq Whisper→GPT-4o→Supabase), PodcastIngestModal UI, GitHub Actions cron every 6h, deploy workflow fixed. Build green, deploy green. Pending: run podcast dry-run test in GHA, add VITE_ secrets to GitHub Actions, verify GitHub Pages source=Actions, resolve Groq account (Hotmail blocked — try Google OAuth in incognito). Workspace: E:\dev\projects\NFL_Dashboard, dev at http://localhost:5173/platinum-rose-app/
