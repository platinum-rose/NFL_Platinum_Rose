# Handoff — 2026-02-28 23:55
Session: ~2h | Model: Claude Sonnet 4.6

## CRITICAL
- None. Build green, deploy green.

## DONE
- **API key rotation** — VITE_OPENAI_API_KEY, VITE_ODDS_API_KEY, GEMINI_API_KEY all rotated and updated in .env + GitHub Actions secrets
- **GitHub Actions secrets** — All 5 required secrets now set: VITE_ODDS_API_KEY, VITE_OPENAI_API_KEY, VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
- **Podcast dry-run confirmed** — 4 feeds loading (Sharp or Square, Even Money, Action Network, Sharp Football Analysis); 0 errors; dedup working
- **Podcast cron schedule** — Changed from every 6h → Friday 8am UTC (offseason); `.github/workflows/podcast-ingest.yml`
- **`supabase/migrations/004_user_data.sql`** — New tables: `user_picks` + `user_bankroll_bets`; permissive RLS (anon read+write); confirmed run in Supabase
- **`src/lib/supabase.js`** — Added `syncPick`, `deleteSyncedPick`, `syncBet`, `loadUserPicks`, `loadUserBets`
- **`src/lib/picksDatabase.js`** — Fire-and-forget Supabase sync on addPick, deletePick, gradePick, gradeGame
- **`src/lib/bankroll.js`** — Fire-and-forget Supabase sync on addBet, updateBetResult, updateBet
- **`src/App.jsx`** — `hydrateFromSupabase()` boot effect restores picks + bets missing from localStorage (multi-device / browser-clear recovery)
- **`vite.config.js`** — manualChunks for recharts/supabase/lucide; main bundle 1.28 MB → 692 kB (46% smaller); vendor chunks browser-cached independently

## PENDING
- **Groq account** — Hotmail blocked at console.groq.com; try Google OAuth in incognito. Agent falls back to OpenAI Whisper without it (~$0.50-0.80/episode).
- **Testing suite** — Jest + Cypress; roadmap item 12; not started
- **React.lazy() tab splitting** — Main app chunk still 692 kB; lazy-loading individual tabs would push below 500 kB; low priority

## BLOCKERS (waiting on external)
- Groq account — try Google OAuth at console.groq.com in incognito, or email support@groq.com

## OPEN DECISIONS (need user input)
- AssemblyAI as Groq fallback? ($50 free credit, ~500h audio, drop-in swap in podcast-ingest.js)
- React.lazy() tab splitting — worth doing for initial load speed?
- Planned agents: PickExtractionAgent or TwitterIngestAgent for next feature push?

## GOTCHAS DISCOVERED
- **Vite 7 inlines small packages**: React, react-dom, and papaparse cannot be split into manualChunks — Vite 7 inlines them. Specifying them produces empty 0-byte chunks. Only split packages that are visibly large (recharts, supabase, lucide).
- **Never attach .env to chat**: Keys pasted into chat are stored in Anthropic logs. Use terminal execution instead — Node processes inherit .env automatically without exposing values to the AI.

## RESUME COMMAND
Continue NFL Platinum Rose (NFL_Dashboard). Session complete: API keys rotated, podcast dry-run confirmed (4 feeds OK), Supabase cloud sync built for picks + bankroll (migration 004 run, fire-and-forget in picksDatabase.js + bankroll.js, boot hydration in App.jsx), bundle split 1.28MB → 692kB. Build green, deploy green. Pending: Groq account (try Google OAuth incognito at console.groq.com), testing suite, optional React.lazy() tab splitting. Workspace: E:\dev\projects\NFL_Dashboard, dev at http://localhost:5173/platinum-rose-app/
