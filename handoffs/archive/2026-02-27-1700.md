# Handoff — 2026-02-27 17:00
Session: ~1.5h | Model: Claude Sonnet 4.6

## CRITICAL (mid-flight / broken / blocking)
- None — app is stable and dev server is running at http://localhost:5173/platinum-rose-app/

## DONE
- **Node.js v24.14.0** — installed via winget; PowerShell execution policy set to RemoteSigned for CurrentUser
- **Dev server** — running via `npx vite --host` with PATH refreshed at session start (`$machinePath + $userPath`)
- **Header WEEK 17 hardcode** — replaced with `getNFLWeekInfo()` in `src/lib/constants.js`; now shows WEEK 1–18, WILD CARD, DIVISIONAL, CONFERENCE, SUPER BOWL, or OFFSEASON dynamically
- **BankrollSettingsModal.jsx** — NEW: bankroll amount, currency, unit-size slider, risk tolerance picker, config summary, save/reset; wired into App.jsx via `openModal('bankrollSettings')` (was no-op `() => {}`)
- **useModals.js** — added `bankrollSettings` to initialState reducer
- **Bankroll button on matchup cards** — was triggering MatchupWizardModal as side effect; fixed by introducing `betEntryGame` state separate from `selectedGame`
- **MatchupWizardModal X button** — was covered by home team logo (click intercepted); fixed: `z-20`, solid `bg-slate-800`, `shadow-lg`, `pr-10` on header flex row
- **ArbitrageFinder.jsx** — NEW: replaces "Coming Soon" stub; reads `cached_odds_data` → `findArbitrageOpportunities()` → falls back to realistic demo data; adjustable total stake with live P&L calc per leg
- **SteamMoveTracker.jsx** — NEW: replaces "Coming Soon" stub; reads `lineMovements` localStorage → `getLineMovements(24)` → demo fallback; type + signal filters; significance auto-classified from movement magnitude
- **OddsCenter.jsx** — removed inline stub functions, added imports for the two real components above

## PENDING
- **OddsCenter tab badges** — Arbitrage tab shows hardcoded `badge: '3'`, Steam Moves shows `badge: '5'` in OddsCenter.jsx tabs array; should be dynamic counts from actual data

## BLOCKERS
- None

## OPEN DECISIONS
- None

## GOTCHAS DISCOVERED
- `selectedGame` is used as the trigger for MatchupWizardModal (`{selectedGame && <MatchupWizardModal />}`) — any code that sets `selectedGame` for a different purpose opens the wizard. Always use a dedicated state var for non-wizard game context.
- Background terminals don't inherit PATH from prior non-background sessions. Must prepend `$env:PATH = [Machine PATH] + [User PATH]` to every `isBackground: true` terminal command until Node is properly on the persistent user PATH.

## RESUME COMMAND
Read CLAUDE.md for project context. Node.js is on PATH — start the dev server with: `$env:PATH = [Environment]::GetEnvironmentVariable("PATH","Machine") + ";" + [Environment]::GetEnvironmentVariable("PATH","User"); npx vite --host` from `D:\Platinum_Rose\NFL_Platinum_Rose`. Then open http://localhost:5173/platinum-rose-app/ and fix the OddsCenter tab badges: the `tabs` array in `src/components/odds/OddsCenter.jsx` has hardcoded `badge: '3'` (Arbitrage) and `badge: '5'` (Steam Moves) — make them dynamic by reading from the same data sources the child components use (`findArbitrageOpportunities` + cached odds for Arbitrage; `getLineMovements(24)` for Steam Moves).
