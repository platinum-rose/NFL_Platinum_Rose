# Handoff — 2026-03-02 15:30
Session: ~1h | Model: Claude Sonnet 4.6

## CRITICAL (mid-flight / broken / blocking)
- None — all commits pushed, dev server running at http://localhost:5173/platinum-rose-app/

## DONE
- `agents/pick-extraction.js` — Added NFL-only filter: picks where neither team resolves to an NFL abbreviation (UFC/NBA/etc.) are skipped with a `totalSkipped` counter in run summary
- `src/App.jsx` — React.lazy() applied to 7 non-landing tabs (standings, devlab, bankroll, analytics, odds, picks, futures) with `<Suspense>` fallback; index.js dropped from ~700KB to 466KB
- `CLAUDE.md` — Documented React.lazy anti-pattern (don't revert to static imports)
- Verified GHA: Smoke Tests #1 (f3aac88) ✅ and #2 (3c6b17c) ✅ both green
- Fixed esbuild "service was stopped" crash — root cause was 8 stale node.exe processes from prior sessions holding esbuild IPC socket; fix: `Stop-Process -Name node -Force`
- Latest commits pushed: a3335f8 (lazy + filter), 64af622 (CLAUDE.md docs)

## PENDING
- **GHA Secrets for smoke tests** (manual, 2 min): Go to GitHub → NFL_Platinum_Rose → Settings → Secrets and variables → Actions → add `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`. The smoke-test.yml is already wired to consume them — without them, AgentStatusModal uses null Supabase client but tests still pass (tested).
- **Futures Odds Ingest #3 failed** today at 2:40 AM — not investigated. Likely no futures odds data returned during off-season. Check the run logs at https://github.com/andrewlrose/NFL_Platinum_Rose/actions/runs/22572277962

## BLOCKERS (waiting on external)
- Off-season: schedule.json is empty/stale until NFL schedules release (~May). Smoke tests pass but the Dashboard shows no games.

## OPEN DECISIONS (need user input)
- None

## GOTCHAS DISCOVERED
- **Stale node processes kill esbuild IPC**: Multiple `npm run dev` sessions left zombie `node.exe` processes holding esbuild's socket. Vite 7 starts but crashes dep-optimization with "The service was stopped". Fix: `Stop-Process -Name node -Force` before starting dev. Add to startup routine.
- **React.lazy tabs are in CLAUDE.md anti-patterns** — the lazy chunk split must not be reverted. Dashboard stays eager (landing page). All 7 tab components are now async chunks.

## RESUME COMMAND
Continue NFL Platinum Rose (NFL_Dashboard). Pipeline fully operational. Latest commits: a3335f8 (React.lazy 7 tabs, index.js 700→466KB; NFL-only pick filter in pick-extraction.js) and 64af622 (CLAUDE.md docs). Two immediate TODOs: (1) manually add GHA secrets VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY at GitHub → repo → Settings → Secrets; (2) investigate Futures Odds Ingest #3 failure at https://github.com/andrewlrose/NFL_Platinum_Rose/actions/runs/22572277962. Dev at http://localhost:5173/platinum-rose-app/, workspace: E:\dev\projects\NFL_Dashboard.
