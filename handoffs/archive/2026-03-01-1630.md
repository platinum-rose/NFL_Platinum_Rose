# Handoff — 2026-03-01 16:30
Session: ~3h | Model: Claude Sonnet 4.6

## CRITICAL (mid-flight / broken / blocking)
- **Podcast ingest run failed** — latest GHA run (max_per_run=2, dry_run=false) failed. Root causes to investigate:
  1. `OPENAI_API_KEY` GHA secret is stale (rotated key) — GPT-4o extraction returns 401
  2. Groq rate limit hit on large episodes (45MB, 32MB) — should now fall through to AssemblyAI, but `ASSEMBLYAI_API_KEY` GHA secret may not have been added yet
  3. Logs needed: paste full run output to diagnose which step failed
- **Migration 005 may not have been run** — `podcast_transcripts.picks_promoted_at`, `user_picks.rationale/expert/units` columns may be missing from Supabase. Run migration before next agent run.

## DONE
- **`agents/pick-extraction.js`** — new PickExtractionAgent: reads unpromoted `podcast_transcripts`, matches team names to schedule.json, upserts into `user_picks` (source='EXPERT'), marks `picks_promoted_at`
- **`.github/workflows/pick-extraction.yml`** — auto-triggers after podcast-ingest succeeds, manual dispatch
- **`supabase/migrations/005_pick_extraction.sql`** — adds `picks_promoted_at` to `podcast_transcripts`; adds `rationale`, `expert`, `units` to `user_picks`
- **`src/lib/supabase.js`** — `syncPick()` + `loadUserPicks()` now map `rationale`/`expert`/`units`
- **`agents/podcast-ingest.js`** — 3 bug fixes:
  - Picks up existing `pending`/`error` episodes from DB (was only processing newly discovered ones)
  - `RateLimitError` class — Groq rate limit no longer retried, falls through to AssemblyAI inline
  - AssemblyAI transcription fallback — URL-based, no file download, polls until complete
- **`GROQ_API_KEY`** — GHA secret confirmed set and working (hit hourly rate limit = key works)
- **`docs/TESTING.md`** — PickExtractionAgent + AssemblyAI fallback test cases added
- **`docs/AGENTS.md`** + **`CLAUDE.md`** — updated with new agent, Groq/AssemblyAI priority chain

## PENDING
- **Add `ASSEMBLYAI_API_KEY` GHA secret** — get from assemblyai.com → API Keys → New, add to GitHub repo Secrets
- **Update `OPENAI_API_KEY` GHA secret** — current key returns 401; update at platform.openai.com/account/api-keys
- **Run migration 005** — Supabase Dashboard → SQL Editor → paste `supabase/migrations/005_pick_extraction.sql` → Run
- **Trigger podcast run after fixes** — `max_per_run=2`, `dry_run=false`
- **Testing suite** — Jest unit tests + Cypress E2E (not started)
- **React.lazy() tab splitting** — optional, low priority

## BLOCKERS (waiting on external)
- AssemblyAI API key not yet added to GHA secrets
- OpenAI API key needs rotation in GHA secrets

## OPEN DECISIONS (need user input)
- None blocking

## GOTCHAS DISCOVERED
- **Groq free tier is 7200 sec/hr** — two 45-min episodes = ~5400 sec, leaving little margin. With AssemblyAI fallback now wired, this is handled.
- **`USE_ASSEMBLYAI` flag logic** — currently `!!ASSEMBLYAI_KEY && !GROQ_KEY` (AssemblyAI only selected when Groq absent). When both are set, Groq is tried first and AssemblyAI catches the `RateLimitError` inline. This is intentional.
- **`podcast-ingest.yml` `dry_run` default was string `'false'`** — fixed to boolean `false` (YAML type validation).

## RESUME COMMAND
Continue NFL Platinum Rose (NFL_Dashboard). Session ended with podcast ingest run failing. IMMEDIATE: (1) update OPENAI_API_KEY GHA secret (current key returns 401), (2) add ASSEMBLYAI_API_KEY GHA secret from assemblyai.com — both needed for full pipeline. (3) Run migration 005 in Supabase if not done. Then investigate failed run logs. New this session: PickExtractionAgent built (agents/pick-extraction.js + pick-extraction.yml), AssemblyAI transcription fallback added to podcast-ingest.js (Groq→AssemblyAI→OpenAI priority chain), bug fix for pending episode pickup. Workspace: E:\dev\projects\NFL_Dashboard, dev at http://localhost:5173/platinum-rose-app/
