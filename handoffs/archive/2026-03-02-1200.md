# Handoff — 2026-03-02 12:00
Session: ~3h | Model: Claude Sonnet 4.6

## CRITICAL (mid-flight / broken / blocking)
- None. Pipeline is fully operational.

## DONE
- **`agents/podcast-ingest.js`** — added missing `transcribeWithAssemblyAI()` function body (was called but never defined); fixed `speech_model` → `speech_models: ['universal-2']` (AssemblyAI API breaking change). Full Groq → AssemblyAI → GPT-4o chain now confirmed working end-to-end.
- **`agents/pick-extraction.js`** — committed for first time (was sitting untracked on disk); PickExtractionAgent fully working, promoted 2 UFC picks to `user_picks`
- **`.github/workflows/pick-extraction.yml`** — committed for first time (same issue — untracked)
- **`supabase/migrations/005_pick_extraction.sql`** — committed + run in Supabase
- **`src/lib/supabase.js`** — committed rationale/expert/units mapping (was untracked)
- **`src/components/modals/AgentStatusModal.jsx`** — new modal: shows active feeds, episode pipeline status (pending/done/error), transcript pick counts, promoted status, expert picks total. Accessible via purple Bot icon in Header.
- **`src/components/layout/Header.jsx`** — added Bot icon button + `onOpenAgentStatus` prop
- **`src/components/ErrorBoundary.jsx`** — new class-based error boundary with `data-testid="error-boundary"` for Playwright detection; stack trace visible in dev
- **`src/main.jsx`** — wrap App in ErrorBoundary
- **`playwright.config.js`** — Chromium-only, vite preview webServer, screenshots on failure
- **`tests/smoke.spec.js`** — 9 smoke tests: app boot, all 9 tabs, 7 header modals; tracks uncaught JS errors via `page.on('pageerror')`
- **`.github/workflows/smoke-test.yml`** — runs on every push/PR; uploads playwright-report artifact on failure
- **`src/components/modals/ReviewPicksModal.jsx`** — fixed Context field reading `pick.analysis` (undefined) → `pick.rationale`
- **`CLAUDE.md`** — added 2 anti-patterns: `speech_models` array required; GHA commit-timing race

## PENDING
- **GHA Smoke Tests** — workflow will run on next push; check Actions tab for green/red
- **Optional GHA secrets for Supabase-connected CI tests** — `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` (tests pass without them)
- **UFC picks in Picks Tracker** — 2 picks promoted are UFC (no NFL game match). Filtering non-NFL picks is easy but low priority.
- **Podcast ingest schedule** — currently Friday-only. Consider bumping to 2x/week during off-season.
- **React.lazy() tab splitting** — index.js is 700KB. Low priority.

## BLOCKERS (waiting on external)
- None.

## OPEN DECISIONS (need user input)
- Filter non-NFL picks out of pick-extraction? (UFC, NBA picks get promoted to user_picks but won't auto-grade)
- Add `VITE_SUPABASE_*` GHA secrets so smoke tests run with live Supabase data?

## GOTCHAS DISCOVERED
- **Critical: new agent files must be `git add`-ed explicitly** — `pick-extraction.js`, `pick-extraction.yml`, `005_pick_extraction.sql`, and `supabase.js` changes were all created locally in the previous session but never committed. Always verify `git status` before ending a session that created new files.
- **AssemblyAI `speech_models` is required and is an array** — `speech_model` (singular) deprecated; must use `speech_models: ['universal-2']`
- **GHA commit-timing race** — triggering a workflow immediately after pushing may use the pre-push commit. Wait for push to complete, verify commit SHA in the "Checkout" step.
- **Playwright strict-mode selector clash** — `getByText('Agent Status')` matched both the button tooltip and the modal heading. Scope heading checks to `page.locator('h2, h3').filter({ hasText: ... })` to avoid strict-mode violations.

## RESUME COMMAND
Continue NFL Platinum Rose (NFL_Dashboard). Pipeline is fully operational — podcast ingest (Groq→AssemblyAI→GPT-4o), pick extraction, and Playwright smoke tests all working. Check GHA Actions tab: smoke-test workflow should have run on the last push (f3aac88); verify it's green. Optional todos: (1) add VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY as GHA secrets for connected smoke tests, (2) filter non-NFL picks from pick-extraction (UFC/NBA picks get promoted but won't grade), (3) React.lazy() tab splitting (index.js is 700KB). Dev at http://localhost:5173/platinum-rose-app/, workspace: E:\dev\projects\NFL_Dashboard.
