# Handoff — 2026-02-27 1800
Session: ~4h (cumulative across 2 continuations) | Model: Claude Opus 4.6

## CRITICAL (mid-flight / broken / blocking)
- **Runtime verification still pending** — Node.js is not available in the VS Code terminal (not installed or not on PATH). All refactored code passes zero lint/diagnostics but has NOT been smoke-tested in a browser. This should be the first thing done in the next session.

## DONE (Session 1 — Hook Extraction)
- `src/lib/storage.js` — NEW — shared `loadFromStorage`/`saveToStorage` helpers
- `src/hooks/useModals.js` — NEW — single `useReducer` replacing 15 modal `useState` booleans
- `src/hooks/useSchedule.js` — NEW — boot sequence, schedule/stats/splits/injuries, `findGameForTeam`, `handleBulkImport`
- `src/hooks/useExperts.js` — NEW — expert consensus CRUD, AI transcript analysis, staged picks
- `src/hooks/useBettingCard.js` — NEW — `myBets` state + bet handlers
- `src/App.jsx` — rewired to use 4 custom hooks (591 → 110 lines)
- `CLAUDE.md` — Phase 4 architecture docs, `/handoff` command, File System Memory section

## DONE (Session 2 — AnalyticsDashboard Split)
- `src/components/analytics/analyticsEngine.js` — NEW — pure calculation logic (no React), ~220 lines
- `src/components/analytics/analyticsFormatters.js` — NEW — `formatCurrency`, `formatPercent` helpers
- `src/components/analytics/KeyMetricsGrid.jsx` — NEW — P&L, Win Rate, ROI, Total Bets KPI cards
- `src/components/analytics/BetTypeBreakdown.jsx` — NEW — spread/ML/parlay/etc performance grid
- `src/components/analytics/TeamPerformance.jsx` — NEW — per-team record, profit, ROI
- `src/components/analytics/RiskAnalysis.jsx` — NEW — volatility, streaks, std deviation
- `src/components/analytics/BettingPatterns.jsx` — NEW — day-of-week win-rate/profit
- `src/components/analytics/TrendChart.jsx` — NEW — weekly trend cards
- `src/components/analytics/EdgeAnalysis.jsx` — NEW — Kelly criterion + risk alerts
- `src/components/analytics/AnalyticsDashboard.jsx` — rewritten as thin orchestrator (895 → 145 lines)

## PENDING (priority order)
1. **Runtime verification** — install Node.js or fix PATH, then `npm run dev` and smoke-test all 8 tabs
2. Fix Header "WEEK 17 • LIVE" hardcode — derive from schedule data
3. Wire up Bankroll Settings (currently no-op)
4. OddsCenter stub tabs — Arbitrage Finder + Steam Move Tracker
5. Web Worker for Monte Carlo simulation (DevLab)

## BLOCKERS (waiting on external)
- Node.js not installed / not on terminal PATH — cannot run `npm run dev`, `npm run lint`, or `npm run build`

## OPEN DECISIONS (need user input)
- None

## GOTCHAS DISCOVERED
- `handleBet` previously called `setSelectedGame(null)` — moved to App.jsx JSX callback to avoid cross-hook coupling
- `handleConfirmPicks` refactored to use functional updater for `setExpertConsensus` to eliminate stale closure on `expertConsensus`
- `useExperts` needs `openModal`/`closeModal` as params so `handleAIAnalyze` can toggle audio→review modals without circular deps

## RESUME COMMAND
"Read CLAUDE.md for project context. Node.js is not on the terminal PATH — fix that first, then run `npm run dev` and smoke-test all 8 tabs at http://localhost:5173/platinum-rose-app/. After verifying, fix the Header 'WEEK 17 • LIVE' hardcode to derive from schedule data, then wire up Bankroll Settings."
