# Week 3 Saturday handoff — intel health check + parlay synthesis prep

**Session window:** 2026-09-25 (Fri, resumed from the 2nd Claude team's TNF-grading/tracker-fix handoff) through 2026-09-26 (Sat AM).
**Git:** HEAD `3a7eb35` (2026-09-25 17:57:00 -0700), still a **sibling commit** of `origin/main` (`bf60e47`, 2026-09-25 04:09:38 +0000) — same parent (`fc1b7b2`), identical commit message, byte-identical file trees (`git diff --name-status HEAD origin/main` empty). Re-verified today, unchanged, still unresolved. Benign (no data loss) but flagged for Andy each session until he decides how to reconcile or a future commit merges it away. **No repo commits made this session** — nothing else to reconcile.
**Branch:** `main`, still the only working branch.

## What happened this session

1. **Reviewed the 2nd Claude team's handoff** (`handoffs/2026-09-24-2110-claude-week3-tnf-graded-tracker-fixes-handoff.md`), verified HEAD state matched its claims, confirmed tracker fixes `c717932`/`fc1b7b2` are in the tree.
2. **Delivered the Week 3 pick'em card** (`reports/pickem/2026-w03-pickem.md`, via `npm run pickem:card` off `data/pickem/pools-2026.json`) to Andy.
3. **Sourced Friday injury designations** (web search, official team reports): Nacua **Doubtful** (groin), Kinchens **Doubtful** (hamstring), Parkinson **Questionable** (knee) — all LAR; Darnold **fully cleared**, off SEA's injury report entirely. Cross-checked against the automated Friday-cadence player-availability build (below) — Nacua/Kinchens matched; Darnold shows as an unclassified `unknown/status_update` in the FantasyPros feed, consistent with "removed from report," not a contradiction.
4. **Got Andy's status check-in:**
   - Ticket **739211245** (8-team open parlay, $10, BKR): ATL +5 WON, DET/CIN/BUF/SEA/SF ML legs pending Sunday, **2 open spots still open**. Andy asked for recommendations from this week's slate or to triage against a stale earlier-season OPEN parlay if one's still alive.
   - Checked every OPEN-leg ticket in the wagers ledger. Only two candidates besides 739211245: **738490212** (Week 1 "Melbourne Season Opener," 7-team) is already dead — legs 5/6 (TB +4, CAR +3.5) graded LOST back in Week 1, but its top-level `status` field is still wrongly showing `PENDING` (**tracker/data-hygiene bug**, not fixed, just flagged — a parlay with any LOST leg should roll up to LOST). **739213766** (this week's GB−4/BUF−6.5) is also dead — GB−4 lost Thursday. **Nothing to triage against.**
   - Recommended filling 739211245's 2 open spots with **PHI ML (−228)** and **BAL ML (−174)** — both vetted in the Wednesday 9/23 card build, neither shares a game with the 5 already-locked legs. Logged this recommendation into the ticket's `progress_notes` in the repo wagers JSON. **Andy has not yet confirmed a fill or pasted a slip — still open.**
   - Bills $10 credit: **not placed**.
   - No other Week 3 weekend ticket was placed — Slots 1/3/4/5/6 (Master RR, Morning/Afternoon parlays, Hybrid, SuperContest) from `reports/bets/2026-w03-card.md` are all **not placed**. Andy also confirmed the ML-underdog parlay that would've included ATL didn't get placed (a miss, since ATL won outright).
5. **Rebuilt the recommendation ledger from scratch.** Resolved that it is NOT a repo file — it's a doc in Andy's claude.ai Project **"DEV"** at path `claude/recommendation-ledger-2026.md` (per `agents/dev/WEEKLY_SYNTHESIS_SESSION_PROMPT.md` §9). The old content (D1–D12) was unrecoverable — the attached Project showed 0 docs at the start of this session. Wrote a fresh version covering all of Week 3 (proposed vs. placed for every TNF ticket, a new D1–D5 divergence log including the still-open PHI/BAL recommendation as D5, agreements logged alongside disagreements per Andy's standing instruction, and a note in case the old D1–D12 turns up elsewhere to merge back in). **This doc now exists — future sessions should read and update it, not recreate it.**
6. **Ran the Friday cadence** (`node scripts/toolbox.mjs --cadence friday`, run by Andy) — completed clean in 60.9s, 6 steps, only a benign Node child_process deprecation warning. Reviewed the outputs:
   - Player availability rebuilt (1152 events, 32 teams) — matches the manually-sourced injury findings above.
   - **New player-props-intel dossier** (`docs/player-props-intel/player-props-intel-latest.md`): 31 props / 5 curated SGP stacks from 441 scanned articles. 4 of 5 stacks are legitimate Sunday-window material (LAC@BUF, NYJ@DET, TEN@NYG, HOU@IND). **Found a pipeline bug**: the 5th stack is still recommending a Thursday-night ATL/GB SGP for a game that's already final — `build-player-props-intel.js` isn't filtering out completed games. Not fixed, just flagged.
   - Secondary matchup vulnerability matrix refreshed (22/32 medium-high) and alpha packet recompiled (394 synthesized recommendations) — not reviewed in depth this session, available for the next synthesis pass.
   - Prediction markets refreshed (4,932 contracts) — 0 execution-eligible on the coherence check, informational only.

## Open items for the next session

1. **Codex is capturing fresh lines Saturday** — once that lands, do a full intel-source health/currency check (see below) before synthesizing a new basket of parlay recommendations for the remaining Week 3 slate (Sunday early/late window, SNF LAR@DEN, MNF).
2. **Intel source health check to run first:**
   - Podcast re-extraction pipeline was **blocked on OpenAI billing** (zero credits) as of 2026-09-04/09-15 per prior audit (`nfl_dashboard_intel_pipeline_gaps.md` in memory) — verify current status before trusting podcast-sourced picks/intel as complete (166/167 transcripts were truncated at 12k chars pre-fix; only 6 re-extracted before the billing wall hit).
   - Fix or at least filter around the player-props-intel stale-game bug (item 6 above) before using its output.
   - Re-check freshness timestamps on all `[available]`/`[fresh]` sources the Friday cadence reported, plus anything not touched by that cadence (futures/division/conference data — see item 4).
3. **739211245's 2 open spots** — still unfilled. Claude's recommendation (PHI ML −228, BAL ML −174) is logged in the ticket's `progress_notes` and as D5 in the rebuilt recommendation ledger, but unconfirmed by Andy and the prices are now 3 days stale. Re-verify current board odds before Andy fills these, especially given fresh Saturday line captures incoming.
4. **Futures §8 review and Circa re-price** — both still blocked on Andy sending fresh screenshots (division/conference/playoff/win-total data stale since 9/08–09).
5. **Bills $10 credit** — still not placed.
6. **Git HEAD/origin divergence** — still unresolved, still benign, Andy's call on whether/how to reconcile.
7. **Optional, not fixed:** template-literal regex-escaping bug in `scripts/generate-live-tracker.mjs` — 17 affected lines across 9+ call sites in the client `<script>` block (lines 4604–9753), including `\d`/`\.` corruption in two score/spread-parsing regexes not mentioned in the original bug report. Real-money production tracker; scope now known to be larger than originally described. Ask Andy before touching it.
8. **Data-hygiene notes (not fixed, just flagged):** ticket 738490212's top-level `status` should be `LOST` (currently `PENDING` despite 2 lost legs); ticket 739211245's `progress_notes` carries a leftover line describing a different ticket's leg 1 (copy/paste residue).
9. **Recommendation ledger** now lives and is current at claude.ai Project "DEV" → `claude/recommendation-ledger-2026.md`. Read it via the Projects tool at the start of any session that will propose or grade picks — don't recreate it.

## Standing constraints (unchanged, still in force)
No `git add -A`; never `reset`/`clean`/`stash`; Supabase writes need per-change OK; no bet placement/account actions (Claude proposes, Andy places); Andy pushes to git unless he explicitly asks Claude to; `docs/Futures_Odds/` stays untracked; never use team power ratings as evidence; $6.81 free/bonus money still unassigned; local scheduled tasks run from this exact checkout; paste-slip check on every ticket before treating it as placed; keep file scans scoped (`timeout`, `maxdepth`, targeted greps — the device_bash bridge stalls on broad recursive scans); git lock-file deletion on the E: mount needs explicit permission — request once or hand Andy the commit command.
