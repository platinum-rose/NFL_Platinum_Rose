# NFL Platinum Rose Handoff - 2026-07-22 16:35

## Session Focus

Offline futures report UX, podcast-source attribution, and podcast narrative summaries.

No live model/API calls were made. No Supabase writes were made. No open parlay slots were modified.

## What Changed

### Futures Portfolio Report UX

- `agents/portfolio-synthesize.js`
  - Report now renders final recommendations once, grouped by team, instead of duplicating the same full card under every ranking category.
  - Added a top `Quick read` summary.
  - Added per-team quick-read blurbs inside collapsible team sections.
  - Team sections are collapsible with `<details>`.
  - `Parlay ladders`, `Hedge baskets`, `Passed / killed`, and `Watch list` are now collapsible sections.
  - Recommendation cards carry report tags such as `Strongest Math` and `Strongest Thesis`.
  - Fixed the Lions longshot misclassification. The old longshot bucket treated `stake_tier === speculative` as longshot, which put Detroit Lions `-200` Make Playoffs under Longshots. Longshot now requires `type === longshot`, `edge_type === longshot`, or a high positive price.
  - Podcast source links now prefer per-episode narrative summaries under `docs/podcast-narratives/` when available, falling back to the aggregate futures-picks summary.

Current visible example:

```text
Detroit Lions matches Strongest Math and Strongest Thesis; it is currently a pass/wait but appears most useful as portfolio or exacta coverage rather than a clean standalone bet.
```

This appears both in the top Quick read and inside the Detroit Lions collapsible section.

### Podcast Narrative Summaries

- `scripts/build-podcast-narratives.js`
  - New offline builder that reads local processed host-summary vault notes from `E:\data\Obsidian\NFL\Podcasts`.
  - Writes episode-level narrative summaries to `docs/podcast-narratives/`.
  - Output includes:
    - NFL-specific narrative summary.
    - Diarized experts / attributed speakers.
    - Best Bets / clear leans.
    - Full extracted NFL futures table.
    - Representative quotes.
    - Source links back to original local vault notes.
  - Generated 33 episode-level summaries:
    - 33 `.md`
    - 33 `.html`
    - `index.html`
    - `index.json`

- `package.json`
  - Added:

```powershell
npm.cmd run podcast-narratives
```

### Upstream Podcast Timestamp Support

- `agents/podcast-host-summary.js`
  - Extraction contract now requests `source_timestamp` per future when transcript lines include timestamps.
  - Host-summary vault notes now render a `Time` column.
  - Quote bullets include the timestamp when available.

- `scripts/build-podcast-narratives.js`
  - Parses and displays `Time` from future rows.
  - Carries timestamps into Best Bets / Clear Leans and the full futures table.

- `tests/unit/podcastHostSummary.test.js`
  - Updated assertions for the `Time` column and timestamped quote rendering.

Important limitation: existing processed podcast summaries do not already contain timestamp fields, so current report output still says timestamp unavailable for those rows. To populate timestamps, rerun or resync the host-summary extraction from timestamped/diarized transcript segments. Do not do that in a fresh session without confirming whether live model/API calls are allowed.

## Generated / Updated Artifacts

- Main regenerated offline corpus report:
  - `.nfl/portfolio/portfolio-2026-07-22-corpus-supplemental-outperformers.html`
  - `.nfl/portfolio/portfolio-2026-07-22-corpus-supplemental-outperformers.md`
  - `.nfl/portfolio/portfolio-2026-07-22-corpus-supplemental-outperformers.raw.json`

- Podcast narrative index:
  - `docs/podcast-narratives/index.html`
  - `docs/podcast-narratives/index.json`

## Verification Run

All offline:

```powershell
node --check agents\portfolio-synthesize.js
node --check agents\podcast-host-summary.js
node --check scripts\build-podcast-narratives.js
npx.cmd vitest run tests/unit/podcastHostSummary.test.js
npm.cmd run podcast-narratives
node scripts\run-portfolio-corpus.js --scenario supplemental-outperformers
npm.cmd run test:portfolio-corpus
npm.cmd run test:futures-dossier
git diff --check
```

Observed results:

- Portfolio corpus: 5/5 passed.
- Futures dossier conformance: passed.
- Podcast host-summary unit test: 25/25 passed.
- Syntax checks: passed.
- `git diff --check`: passed, with a warning that `agents/portfolio-synthesize.js` line endings may normalize from CRLF to LF when Git touches it.

## Known Worktree State

The worktree remains intentionally dirty. Do not revert unrelated files.

Known modified files in this session/workstream include:

- `agents/portfolio-synthesize.js`
- `agents/podcast-host-summary.js`
- `scripts/build-podcast-narratives.js`
- `tests/unit/podcastHostSummary.test.js`
- `package.json`
- generated `docs/podcast-narratives/`

There are also prior uncommitted P0/P1/P2 futures-audit and corpus files from earlier in the day. Treat them as intentional unless Andy asks otherwise.

## Recommended Next Steps

1. Inspect the new report UX in the browser and decide whether team sections should default open or closed. Current behavior: team sections default open; lower sections default closed.
2. Add a small navigation/table-of-contents at the top for all teams with final cards, especially once more than one team survives validation.
3. Add a timestamp backfill plan before rerunning podcast host summaries:
   - Confirm which `podcast_transcripts` rows have `speaker_segments` with start times.
   - Decide whether to rerun `agents/podcast-host-summary.js --overwrite` for all rows or only rows tied to futures-report evidence.
   - This will require live model/API calls unless an offline extraction cache exists.
4. Consider adding a source-evidence quality badge in portfolio cards:
   - `Named Expert + Timestamp`
   - `Named Expert, No Timestamp`
   - `Unattributed Speaker`
   - `Episode-only / Article-only`
5. Keep improving the report as a decision-support dashboard, but do not run a fresh live analyst recommendation until the audit P0 items remain satisfied and Andy explicitly approves the live call.

## Resume Prompt

```text
Resume Platinum Rose NFL in E:\dev\projects\NFL_Dashboard. Read HANDOFF_PROMPT.md, WORKING-CONTEXT.md, and handoffs/2026-07-22-1635.md. Current focus: offline futures portfolio report UX and podcast-source attribution are improved; recommendations render once by collapsible team section, lower report sections are collapsible, podcast narrative summaries exist under docs/podcast-narratives, and upstream host-summary extraction can now request source_timestamp. Do not make live model/API calls, write to Supabase, persist analyst output, or modify open parlay slots without explicit approval. Recommended next: inspect UX, add report TOC/source-quality badges, and plan timestamp backfill before any host-summary overwrite.
```
