# BRANCH-CONSOLIDATE-MAIN — Step 1 inventory (read-only)

**Date:** 2026-09-23 15:53 → 16:30 PT · **Author:** Claude · **Task:** `handoffs/2026-09-23-1455-claude-task-branch-consolidation-main.md` step 1
**Nothing merged, committed or pushed.** Your checkout (`wip/yahoo-sync`, dirty) was not touched: no reset/clean/stash/switch; the checkout was only read with `--no-optional-locks`.

## 0. Live state (fetched 15:54 PT; live git beats the task snapshot)

| Ref | SHA | Note |
|---|---|---|
| origin/main | `2412d13` | GitHub automation branch |
| origin/wip/yahoo-sync | `56193e1` | = local wip HEAD → **pushed** |
| merge-base | `215f1a3` | as in task file |
| local `main` | `b14e2b0` | stale, 8 behind origin/main (fast-forward only) |
| local `main-merge-picks` | = `2412d13` | identical to origin/main → deletable later |
| local `feat/prop-availability-tooling` | `9cef1a7` | fully contained in wip (0 unique) → deletable later |

- **main ahead 22 / wip ahead 47** (task file said 46; +`56193e1`).
- **Equivalent pairs: 11, not 7.** git marks 9 as patch-identical (6 of the 7 cherry-picks plus main's `7b350eb`/`2edda4d`/`a7fa5ba` = wip's `f9a5fda`/`f388edb`/`328132c`); `a3f6678`≈`1e35dda` and `c4b6126`≈`7d49901` have identical resulting file content (hunk offsets/metadata differ). **Real main-only work = 11 commits; real wip-only = 36.**
- Worktree created: `E:\dev\projects\NFL_Dashboard-consolidate` (detached at `2412d13`). A background `worktree add` got killed at 27% when the device link reset; finished by `read-tree` + chunked `checkout-index`, and cleared the worktree's own stale `index.lock`/`locked` (delete permission on E:\dev granted this session; no git process was running). Its index stat cache is cold, so the first `git status` there takes more than 60 s. Run `git update-index --refresh` once before step 2.
- Device-link notes: `git status` in the main checkout takes more than 90 s and kills the shell call; background jobs die when the link reconnects. For step 2, run long git operations in chunks of 160 s or less, or from Andy's own shell.

## a) Commit classification

### main-only (22)
| Commit | Class | Status vs wip |
|---|---|---|
| 1603724, 1988150, 836a872, cc667c2, 55dcf9a, 2412d13 | code+test | **patch-identical** to wip 229042d, d438484, de8145e, 75e85a5, 40d2eb1, 6020262 |
| a3f6678 | code+test+docs | = wip 1e35dda in content (hunk offsets differ only); final files identical |
| 7b350eb, a7fa5ba | code | patch-identical to wip f9a5fda, 328132c; **wip 55a582f supersedes both** (Out ≠ Burnt) |
| 2edda4d | docs | patch-identical to wip f388edb |
| c4b6126 | code+test | same file content as wip 7d49901 |
| 6195892 survivor/waiver/SC-CLV tooling | code (9 new files) | **main-only**. Not on wip, but identical copies sit untracked in the checkout. `scripts/sync-yahoo-survivor.mjs` is run by the Yahoo_Survivor_Sync scheduled task |
| c085cc4 committee/scoped dossier/provenance (Rev 20) | code+tests (21 new files) | **main-only**; identical untracked copies in checkout |
| 7623711 toolbox launcher / stale SC week / waiver tools | code (4 files) | **main-only**; merges cleanly with wip 881d28a (different hunks) |
| 48d05d0 article-intel bet tiers | code | **main-only**; checkout copy = main |
| 7aa3ba3 portfolio preflight/synthesize + WBA prompt | code+test+docs | **main-only** code (checkout copy = main); its WBA-prompt edit is superseded by wip 67d2176 (prompt neutralized) |
| 71a4315 in-season crons + npm scripts + acorn + vite route | **workflow**+build | **main-only; already live on GitHub since 9/21**. Checkout's "uncommitted workflow edits" are byte-identical to this commit |
| 4f298b7 scheduled-task hiding utility | code (Windows) | 6 of 7 files identical to wip's 7fc41b7; `.ps1` superseded by wip (+8 tasks) |
| 8063e0c session log (.atlas, HANDOFF) | docs | HANDOFF part superseded by wip trim (all but 1 line archived; that line = a feature gap since fixed); `.atlas*` parts: checkout has a superset |
| 32fadd7 lessons-learned/specs/audits | docs (+1 py script) | main-only except `BETTING_LESSONS_LEARNED.md` (wip ⊇ main) |
| b14e2b0 futures pipeline spec | docs | main-only |
| 41bbfcd auto schedule update | data (`public/schedule.json`) | main-only; **newer than wip's**; generated by workflow, so keep main's |

### wip-only, unique (36)
| Class | Commits |
|---|---|
| code | 27bf77a yahoo sync guard · f71df4a BEO/BKR live parsers · 55a582f live-tracker Out≠Burnt (**supersedes main's tracker behaviour, and removes the hard-coded Week-1 ESPN event ids 401872656/657 that main still has**) · e02482e bankroll RR partial wins · 7cd7005 synthesis preflight · caec16c preflight regex · 53af810 Gemini retry · 881d28a toolbox PM refresh/PM chain · f34045c build-handoff hook |
| code + workflow | 7fd19fa Gemini diarization default (**podcast-ingest.yml: comment-only change**) |
| automation (Windows) | 7fc41b7 grok scanner + 13 hidden scheduled-task .cmd + setup ps1 |
| data | 465a153 BUF SB ticket/promos · 8c256fd Wk3 futures captures/PM/secondary · 45c52e7 BKR lines · 21bd021 promos |
| docs + scratch analysis | 4236581, 445e436 (futures/Circa: data/futures-imports + scratch/*.mjs) |
| docs / governance / handoffs | 5408b4d, 70c36ec, 74aa512, 103952c, 213c15c, c03fbe2, 67d2176, a0ad4b4, 4f30082, b45fe0a, 0d2080b, f95e460, 85f70be, ff696cf, 2ff23dd, 00821c2, e63adba, 32a981e, 56193e1 |

## b) Files changed on both sides since merge-base (31)

**24 are already byte-identical** on both branches (all pick-extraction/intel libs + tests, `add-placed-wager.mjs`, ManualGradeModal/PicksTracker/picksDatabase, 5 hidden-task .cmd + run-hidden.vbs, 3 fantasy docs). No action.

In-memory trial merge (`git merge-tree`; changes nothing on disk) of `origin/main` into wip: **6 files conflict, 1 auto-merges**. Everything else merges cleanly.

| File | main changed | wip changed | Proposed resolution | Risk |
|---|---|---|---|---|
| `scripts/generate-live-tracker.mjs` (4 hunks) | 7b350eb+a7fa5ba (toggles, Push, Out=Burnt, **hard-coded Wk1 event ids**) | same 2 patches + 55a582f (Out informational, Hide Injured Legs) + Wk1-id removal | **Take wip** (main has no lines wip lacks except the superseded behaviour and the Wk1 stat-leak bug) | Med (live-tracker used Sundays; eyeball one render after merge) |
| `scripts/toolbox-app-server.mjs` | 7623711: no double tab, fantasy tool cases | 881d28a: PM Refresh, PM chain, current-week tracker | **Auto-merge result** (both sets). *Checkout copy is a further superset:* + `launch-betonline-props` / `launch-bookmaker-props` (not on either branch) | Low |
| `scripts/windows/hide-nfl-task-windows.ps1` (add/add) | 4f298b7: 5 tasks, UTF-8 BOM | 7fc41b7: 13 tasks, no BOM | **Take wip** | Low |
| `HANDOFF.md` | 8063e0c appended session log | trimmed to index; legacy archived | **Take wip** | Low |
| `TASK_BOARD.md` | 3 rows | same 3 rows + 3 | **Take wip** (strict superset) | Low |
| `agents/dev/WEEKLY_BETTING_ANALYST_PROMPT.md` (2 hunks) | 7aa3ba3 +107/−19 | 67d2176 neutralized to reference-only | **Take the checkout copy** (wip's neutral header + most of main's body; the 15 main lines missing are ones wip deliberately removed). Fallback: wip | Low |
| `docs/BETTING_LESSONS_LEARNED.md` (add/add) | 32fadd7: 67 lines | 185 lines, contains all of main's | **Take wip** | Low |

Also covered by `git merge-tree` (no conflict): `package.json`, `package-lock.json`, `vite.config.js` → main's (npm scripts, acorn, tracker route); `public/schedule.json` → main's.

## c) Dirty checkout — 221 tracked modifications (all `M`) + 1,092 untracked

| Bucket | n | Files | Proposal |
|---|---|---|---|
| **Already on main** (byte-identical to origin/main) | 15 | `.github/workflows/{injury-ingest,odds-ingest,research-intel-ingest}.yml`, package.json, package-lock.json, vite.config.js, public/schedule.json, agents/portfolio-{preflight,synthesize}.js, scripts/{build-article-intel-review.js,sync-live-market-lines.mjs,youtube-podcast-sweep.js}, tests/unit/portfolioPreflightScanner.test.js, packages/m6-podcast-service/…/experts_roster.json | Nothing to do. After the merge they match HEAD and drop out of `status` |
| **Superset of main+wip (checkout is newest)** | 5 | `scripts/toolbox-app-server.mjs` (+ sportsbook-capture launch tasks), `scripts/toolbox-dashboard.html` (+ Live Sportsbook Capture section; **lacks main's Fantasy Tools buttons**, so the merge should keep both), `agents/dev/WEEKLY_BETTING_ANALYST_PROMPT.md`, `.atlas/lessons-learned.md` (+S245 lessons), `.atlas-bridge/memory.json` (session 14) | **New, worth committing** on the consolidate branch after review. toolbox-app-server is live code for the toolbox server |
| **Genuinely new code** | 1 | `scripts/build-player-props-intel.js` (+384/−1473: drops the hard-coded Week-1 PLAYER_TEAM_MAP, derives the week) | **Worth committing** after a test run; not on either branch |
| **Regenerable / pipeline output** | 197 | data/podcasts/m6-diarized (113 + 1), data/fantasy boxscores+rosters (17), expert-dossiers (14) + docs/antigravity/expert-dossiers (15), data/generated (6), prediction-markets data+docs (7), projected-starters (2+1), player-availability (1+2), secondary-matchups w01 (1+2), research-intel (5), shadow-harness (2), supercontest (1), alpha packet (data + **public/alpha**), **public/yahoo-live-rosters.json**, article-intel-review docs (2), podcast deep-dive index (2) | Leave as local state; the scheduled tasks rewrite them. Don't include them in the merge. `public/*` ones ship on the next deploy only if committed, so leave them uncommitted |
| **Needs Andy** | 1 | `data/official-picks/platinum-rose-ai-2026.json` (+754/−1) | Official-picks lane: don't commit without explicit OK |
| **Discard candidates** (review, don't delete) | 3 | `scratch/even_money_week1_bets_summary.{html,docx}`, `preview/newsletter_live_preview.html` | Old Week-1 scratch/preview; leave or commit as archive, Andy's call |

**Untracked (1,092):** mostly pipeline output (data/podcasts 161, podcast deep-dives 134, logos 33, article summaries 56, …). **36 untracked files are byte-identical to files main adds** (all of 6195892 + c085cc4, 2 audits, 2 specs, the futures spec, transcribe_twitter_video.py …). **These will block `git switch main`** ("untracked working tree files would be overwritten") even though they're identical. See step 5 in e).

**Uncommitted `.github/workflows` edits:** all 3 = main's `71a4315` exactly, so nothing new and they're already live. **`scripts/toolbox-app-server.mjs` vs main's 7623711:** checkout ⊇ main ⊇ nothing lost; checkout = main + wip 881d28a + 2 new sportsbook launch tasks.

## d) What goes live on promotion (push of the merge to main)

- **Workflows:** effectively nothing. main's in-season crons (odds every 4 h, injuries at inactive windows, Sunday intel sweep) are **already live since 9/21**. The only wip workflow diff is a comment in `podcast-ingest.yml`. No new secrets, no `ref:` changes. `ci.yml`/`smoke-test.yml` run on push to main; `deploy.yml` runs via `workflow_run` after CI on main.
- **CI gate:** the push runs the full test suite on wip's code for the first time on main (wip adds ~10 test files: pickWeekScope, intelRevisions, collegeFootballFilter, addPlacedWager, …). **A red CI blocks deploy**, so run `npx vitest run` + `npm run build` in the worktree before pushing.
- **public/ + deploy:** `src/` is **identical** on both branches; public/ diff = `schedule.json` only (keep main's). Deployed app is **unchanged** unless we choose to commit the checkout's `public/alpha` / `public/yahoo-live-rosters.json` (recommend: don't).
- **Automation code that changes on main:** pick-extraction (already same), research-intel-ingest (already same), podcast ingest gets **Gemini-default diarization + retry** (7fd19fa/53af810): GitHub podcast runs switch from AssemblyAI to Gemini for multi-host shows (needs `GEMINI_API_KEY` secret, already referenced by the workflow).
- **Supabase schema/migrations:** **none** in main..wip (no `supabase/` or `.sql` changes).
- **Other:** `CLAUDE.md`, `agents/pick-extraction.js`, `src/lib/apiConfig.js` reference `raw.githubusercontent.com/andrewlrose/NFL_Platinum_Rose/main/…` while `origin` is `platinum-rose/NFL_Platinum_Rose`. It works today via the GitHub redirect; flag for step 6 (low).

## e) Proposed step 2 merge plan

1. In the worktree: `git update-index --refresh`; `git switch -c consolidate/main-2026-09 origin/wip/yahoo-sync` (worktree only).
2. `git merge --no-ff origin/main` → resolve the 6 conflicts per table b) (`git checkout --theirs/--ours` per file = mechanical).
3. Separate, reviewed commit on top ("carry-over from dirty checkout"): copy in the 5 superset files + `build-player-props-intel.js` from the checkout; restore main's Fantasy Tools block in `toolbox-dashboard.html`. Nothing from the regenerable/official-picks buckets.
4. Gates: `npx vitest run` (full), eslint on touched code, `node --check` agents/scripts, `npm run build`, one `generate-live-tracker.mjs` render. Chunk to ≤160 s per call or run from Andy's shell.
5. **Review gate with Andy** (workflows = none new; public = none; schema = none), then Andy: `git push origin consolidate/main-2026-09:main` (fast-forward from 2412d13), and `git tag archive/wip-yahoo-sync-2026-09-23 56193e1 && git push origin archive/wip-yahoo-sync-2026-09-23`.
6. **Switch the checkout (Andy present; pause scheduled tasks for ~5 min):** move the 36 identical untracked files aside (`mv` into `_switch_hold/`, no delete) → `git switch main` after `git fetch` (tracked mods carry over; the 15 "already on main" files become clean) → verify with hash-object → remove `_switch_hold/`. No reset/stash needed. Alternative: `git symbolic-ref HEAD refs/heads/main` + `git read-tree main` updates HEAD and index only, never files. Andy should OK that path explicitly because it rewrites the index.
7. Step 6 of the task (governance): only 2 tracked files mention `wip/yahoo-sync` outside handoffs (HANDOFF.md, TASK_BOARD.md); CLAUDE.md/AGENTS.md have no wip refs but their push-recovery text assumes main (already correct). Update HANDOFF_PROMPT/WORKING-CONTEXT/synthesis prompt per plan. Local cleanup later: fast-forward local `main`, delete `main-merge-picks` and `feat/prop-availability-tooling` (both fully merged).

**Codex split:** steps 1–4 are mechanical (≈80% delegable: merge, conflict picks per table b, carry-over copy, test/lint/build runs, conflict report). Keep with Claude + Andy: carry-over review (3), push/tag (5), checkout switch (6), governance edits (7). Estimated step 2 effort: 1 session (~1–2 h), dominated by slow git I/O over the device link.

## Artifacts left on disk (outside the repo, safe to delete)
`E:\dev\projects\{dirty-tracked.txt, dirty-bucketed.tsv, dirty-paths.txt, dirty-hashes.txt, dirty-numstat.txt, untracked.txt, lt-*.txt, wt-*.log, status-*.txt}`. Worktree `E:\dev\projects\NFL_Dashboard-consolidate` kept for step 2.
